---
title: "Algebraic Retrieval: Composable Search for Agents"
title_zh: 代数检索：面向智能体的可组合搜索
authors: Damian Delmas
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.19482v1"
tags: ["query:agent"]
score: 8.0
evidence: 面向AI智能体的可组合搜索策略
tldr: "AI智能体在检索时通常难以按需组合相关性、资格约束与排序偏好。本文提出代数检索，将检索策略写成可组合的数学查询，并在PEM基础上暴露向量与分数运算，让智能体组合、检查并修订程序。在含11,429篇文档的Vaswani公开集上，SQL与PyTerrier等实现选出相同文档集，分数差低于1e-6，仅一对并列项排序不同。该工作以执行一致性验证了可组合检索的可行性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.19482v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1694, \"height\": 333, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.19482v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1227, \"height\": 339, \"label\": \"Table\"}]"
motivation: AI智能体需要按查询动态组合相关性、约束和排序偏好，但传统检索接口固定，难以在执行中检验并修订检索策略。
method: 提出代数检索，在PEM基础上把向量与分数运算暴露为可组合操作，使智能体在查询时组合并修订检索程序。
result: "在11,429篇文档的Vaswani公开集上，各程序经SQL与PyTerrier等实现选出相同文档集，分数差低于1e-6，仅一对并列项排序不同。"
conclusion: 该工作以执行一致性而非检索质量或智能体行为为评估目标，为智能体可组合检索提供了可验证的实现路径。
---

## 摘要
代数检索使 AI 智能体能够在查询时组合搜索策略。相关性准则、资格约束和排序偏好可以一起表达在一个数学查询中。查询接口暴露可用操作，因此智能体可以针对当前问题将它们组合起来，并在检查结果后修改程序。我们评估的是执行一致性，而非智能体行为或检索质量。基于程序化嵌入调制（PEM），它在检索过程中暴露向量和分数算术，我们展示了对比评分、候选池重排序和加权排序作为可组合查询，并给出可执行的 SQL 和 PyTerrier 对应实现。在公开的 11,429 篇文档的 Vaswani 测试集上，每个程序的实现选择相同的文档集，分数差异低于 1e-6；有一对并列项在不同评分路径下的排序不同。

## Abstract
Algebraic Retrieval lets AI agents compose search strategies at query time. Relevance criteria, eligibility constraints, and ranking preferences can be expressed together in a mathematical query. The query surface exposes available operations, so an agent can combine them for the question at hand and revise a program after inspecting results. We evaluate execution parity, not agent behavior or retrieval quality. Building on Programmatic Embedding Modulation (PEM), which exposes vector and score arithmetic during retrieval, we demonstrate contrastive scoring, candidate-pool reranking, and weighted ranking as composable queries, alongside executable SQL and PyTerrier counterparts. On the public 11,429-document Vaswani fixture, each program's implementations select the same document set with score differences below 1e-6; one tied pair orders differently across scoring paths.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义
- **研究动机**：传统检索接口通常只暴露“一个查询 + 少量参数”，AI 智能体难以在查询时动态组合相关性准则、资格约束和排序偏好，也难以在执行后检查结果并修订检索策略。
- **核心问题**：如何让智能体把检索策略写成可组合、可检查、可修订的程序，而不是固定的一次性搜索调用。
- **整体含义**：论文提出“代数检索”（Algebraic Retrieval），在 Programmatic Embedding Modulation（PEM）暴露向量与分数算术的基础上，把检索表达为数学查询。智能体可选择评分方向、合格记录集合和逐记录权重，并在查看结果后修改表达式。
- **评估定位**：论文明确评估的是**执行一致性**，而非智能体行为、检索质量或学习嵌入效果。

## 2. 方法论
- **核心思想**：将检索写成对相似度分数的代数运算，并在查询时组合评分、掩码、权重和 top-k 选择。
- **基本记号与操作**：
  - 嵌入矩阵 \(E\) 与查询向量 \(q\) 做矩阵乘法 \(E @ q\)，在单位向量下等价于余弦相似度，产生“分数—记录身份”关系 \(S\)。
  - `+`、`−` 组合不同查询方向的分数，标量 `*` 缩放贡献。例如 \((E @ q1) - 0.5(E @ q2)\) 表示偏好 q1 方向并抑制 q2 方向。
  - 掩码 \(m\) 表示合格身份集合，\(m ▷ S\) 删除其他记录。
  - 权重关系 \(w\) 为每个合格身份提供标量，\(w ⊙ S\) 乘到分数上；缺失权重视为 0，零权重保留记录但分数为 0。
  - \(\tau_{10}(S)\) 选择最多 10 条结果，按分数降序、身份升序排列；ASCII 写作 `top(10, S)`。
- **架构流程**：
  - 智能体通过 `algebra('<expression>')` 提交程序，表达式也可嵌入更大的 SQL 查询中。
  - 运行时使用 materializer 执行检索操作，并将结果存入临时 SQL 表；SQLite 解析命名操作数，NumPy 执行
