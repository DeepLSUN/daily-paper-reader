---
title: Forward-Free LLM Depth Pruning via Weight Redundancy
title_zh: 免前向传播的基于权重冗余的LLM深度剪枝
authors: "Vincent-Daniel Yun, Woosang Lim"
date: 2026-09-09
pdf: "https://arxiv.org/pdf/2609.09883v1"
tags: ["query:llm"]
score: 9.0
evidence: 无需前向传播的深度剪枝以降低LLM推理成本
tldr: 深度剪枝能降低LLM推理成本，但现有无前向方法逐块独立打分，忽略层间相似性。本文提出WRP，直接从检查点权重估计层间冗余，比较注意力输出与MLP下投影权重的相似度，并结合相对投影尺度。在多种剪枝设置、模型家族和下游任务中，WRP稳定优于无前向幅度剪枝，逼近基于激活的方法。贡献在于无需校准数据和前向传播即可选块，为无前向深度剪枝提供权重冗余新视角。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.09883v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 878, \"height\": 405, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09883v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1819, \"height\": 343, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09883v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1802, \"height\": 370, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.09883v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1805, \"height\": 305, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.09883v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1848, \"height\": 1384, \"label\": \"Table\"}]"
motivation: 现有无前向深度剪枝逐块独立打分，未度量Transformer块间相似性；基于激活的方法又需校准前向，成本较高。
method: WRP从检查点权重估计层间冗余，比较各层注意力输出权重与MLP下投影权重，融合成对相似度和相对投影尺度，构建全对相似矩阵以分组选层。
result: 在多种剪枝设置、模型家族和下游任务上，WRP持续优于现有无前向幅度剪枝，并接近基于激活的剪枝方法。
conclusion: 无需校准数据或前向传播即可完成LLM深度剪枝，为利用权重冗余选择Transformer块提供了有效方案。
---

## 摘要
深度剪枝通过移除完整的Transformer块来降低大语言模型（LLM）的推理成本。基于激活的方法在校准数据上进行前向传播以收集隐藏状态，而现有的免前向方法则分别对每个Transformer块进行打分，而不度量块之间的相似性。我们提出权重冗余剪枝（Weight-Redundancy Pruning, WRP），一种免前向的深度剪枝方法，它从检查点权重中估计层间冗余，以在无需校准数据或模型前向传播的情况下选择块。WRP比较各层的注意力输出权重和MLP下投影权重，并将它们的成对相似性与相对投影尺度信息相结合。得到的全对相似度矩阵用于指导层分组和块选择。在多种剪枝设置、模型系列和下游任务中，WRP始终优于现有的免前向幅度剪枝，并接近基于激活方法的性能。

## Abstract
Depth pruning reduces large language model (LLM) inference cost by removing complete Transformer blocks. Activation-based methods collect hidden states through forward passes on calibration data, while existing forward-free methods score each Transformer block separately without measuring similarity between blocks. We propose Weight-Redundancy Pruning (WRP), a forward-free depth-pruning method that estimates inter-layer redundancy from checkpoint weights to select blocks without calibration data or model forward passes. WRP compares attention output and MLP down-projection weights across layers and combines their pairwise similarities with relative projection-scale information. The resulting all-pairs similarity matrix guides layer grouping and block selection. Across multiple pruning settings, model families, and downstream tasks, WRP consistently outperforms existing forward-free magnitude pruning and approaches the performance of activation-based methods.

---

## 论文详细总结（自动生成）

# 论文总结：Forward-Free LLM Depth Pruning via Weight Redundancy

## 1. 核心问题与整体含义

- **研究动机**：LLM 规模持续增大，推理阶段的内存与计算成本显著上升；而预训练模型通常只提供少量固定尺寸，难以匹配用户硬件预算。
- **深度剪枝**：通过移除完整 Transformer 块来灵活降低模型深度，同时保留标准稠密算子，是一种适配资源预算的压缩方式。
- **现有问题**：
  - **基于激活的方法**（如 ShortGPT、LLM-Streamline、LoRP）需要在校准数据上做前向传播并收集隐藏状态，可能在大模型上超出 GPU 内存。
  - **免前向方法**（如 Shortened LLaMA 的 Mag/Mag+）直接从检查点权重按幅度独立给每个块打分，**没有度量块与块之间的相似性或冗余**。
- **整体含义**：本文提出 Weight-Redundancy Pruning（WRP），证明仅利用检查点权重中的层间冗余信息，也能在无需校准数据、无需前向传播、无需梯度或内部通道匹配的情况下，有效完成 LLM 深度剪枝。

## 2. 方法论

- **核心思想**：Transformer 块对隐藏状态的更新主要来自注意力输出投影 \(W^o\) 和 MLP 下投影 \(W^{down}\)，二者共享隐藏状态输出空间。因此可跨层比较这些权重，估计层间冗余。
- **权重空间描述子**：
  - 对 \(W^o\) 和 \(W^{down}\) 做中心化：\(H_d = I_d - \frac{1}{d}\mathbf{1}\mathbf{1}^\top\)，得到 \(\tilde{W}\)。
  - 构造输出空间 Gram 矩阵 \(G = \tilde{W}\tilde{W}^\top\)，并用线性 CKA 计算跨层相似度。
  - 分别得到注意力输出相似度 \(s^o_{ij}\) 和 MLP 下
