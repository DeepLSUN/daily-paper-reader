---
title: Understanding LLM Quantization through Activation-Guided Compensation and Orthogonal Residuals
title_zh: 通过激活引导补偿与正交残差理解LLM量化
authors: "Yamato Narita, Issei Sato"
date: 2026-09-18
pdf: "https://arxiv.org/pdf/2609.21450v1"
tags: ["query:llm"]
score: 8.0
evidence: 面向推理效率的LLM量化分析
tldr: 针对LLM后训练W4A4量化中激活离群值降低有效量化精度的问题，本文将局部权值-激活量化误差分解为激活引导的权值补偿项与正交残差，并用持续通道离群量和常规激活量给出残差上界。基于此，论文推导随机Hadamard旋转、符号选择与通道缩放的准则，解释随机符号抑制离群通道相长干涉、二阶矩平衡导出L2缩放并松弛为SmoothQuant式L∞缩放。在8个Llama和Mistral模型上以免反向传播配置验证，性能与梯度训练的SpinQuant相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.21450v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 654, \"height\": 342, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.21450v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 521, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21450v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 740, \"height\": 273, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.21450v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1447, \"height\": 1394, \"label\": \"Table\"}]"
motivation: W4A4后训练量化受激活离群值制约，现有权值优化、通道缩放与正交旋转各修复何种误差分量及其关系尚不清晰。
method: 将局部量化误差分解为激活引导权值补偿项与正交残差，用持续离群通道和常规激活量给出残差上界，并推导Hadamard旋转、符号选择与通道缩放准则。
result: 在8个Llama和Mistral模型上以无反向传播配置验证，性能与梯度训练的SpinQuant相当。
conclusion: 该分解厘清了权值补偿与变换设计的误差边界，为免训练W4A4量化提供可解释准则。
---

## 摘要
训练后权重-激活量化降低了大型语言模型的内存与推理成本，但激进的W4A4量化仍然困难，因为激活离群值会降低有效量化分辨率。尽管权重优化、逐通道缩放和正交旋转能够缓解这一问题，但它们所处理的误差成分及其相互关系仍不明确。通过将局部权重-激活量化误差精确分解为激活引导的权重补偿项与正交残差，我们利用持续性的逐通道离群值和常规激活量对残差进行界定。该分解阐明了哪些误差成分可通过权重补偿处理，哪些需要变换设计。随后，我们利用残差界推导出应用随机Hadamard旋转、符号选择和通道缩放的实用准则。特别地，该分析解释了随机符号如何抑制持续离群通道之间的相长干涉，采样多种符号模式如何改进变换选择，以及二阶矩平衡如何导出L2缩放规则，而进一步松弛则恢复SmoothQuant风格的L∞缩放。我们在八个Llama和Mistral模型上通过无反向传播配置评估这些准则，获得了与梯度训练的SpinQuant相当的性能。

## Abstract
Post-training weight-activation quantization reduces the memory and inference costs of large language models, but aggressive W4A4 quantization remains difficult because activation outliers degrade effective quantization resolution. Although weight optimization, channel-wise scaling, and orthogonal rotation mitigate this problem, the error components they address and their relationship remain unclear. Using an exact decomposition of local weight-activation quantization error into an activation-guided weight compensation term and an orthogonal residual, we bound the residual using persistent channel-wise outlier and regular activation quantities. This decomposition clarifies which error components can be addressed by weight compensation and which require transformation design. We then use the residual bounds to derive practical guidelines for applying randomized Hadamard rotation, sign selection, and channel scaling. In particular, the analysis explains how random signs suppress constructive interference among persistent outlier channels, how sampling multiple sign patterns can improve transformation selection, and how second-moment balancing leads to an $L_2$ scaling rule while a further relaxation recovers SmoothQuant-style $L_\infty$ scaling. We evaluate these guidelines through backpropagation-free configurations across eight Llama and Mistral models, obtaining performance competitive with gradient-trained SpinQuant.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义

- **研究背景**：大语言模型（LLM）部署受内存与推理成本限制，后训练量化（PTQ）可用少量校准数据降低开销；但激进 W4A4（4-bit 权重 + 4-bit 激活）仍很困难。
- **核心痛点**：激活中的大离群值会扩大量化范围，降低普通数值的有效量化分辨率；尤其是“持续通道级离群值”（persistent channel-wise outliers）会在多个 token 上反复主导 per-token 量化尺度。
- **现有方法分工不明**：权重优化（如 GPTQ/GPTAQ）、通道缩放（如 SmoothQuant）、正交旋转（如 QuaRot/SpinQuant）都能缓解问题，但它们分别修复哪一类量化误差、彼此如何互补，缺乏统一解释。
- **论文目标**：通过局部权重-激活量化误差的精确分解，区分“可由权重补偿处理的误差”与“必须由变换设计处理的误差”，并据此推导旋转、符号选择、通道缩放的实用准则。
- **整体含义**：论文试图把原本分散的权重量化、旋转、缩放技术统一到一个误差分解框架下，为免反向传播的 W4A4 量化配置提供理论依据。

## 2. 方法论

### 2.1 问题设定与变换形式

- 对线性层 \(Y = XW^\top\)，引入可逆变换 \(P\)，令 \(Z = XP\)，\(V = WP^{-\top}\)，保持全精度输出 \(XW^\top = ZV^\top\)。
- 激活采用 no-clipping 动态 per-token 对称均匀量化：对每个 token，尺度由 \(\|Z_t\|_\infty / q_A\) 决定，\(q_A = 2^{b_A-1}-1\)。
- 权重量化 \(Q_W(V)\) 不限定具体规则，可以是 GPTQ、GPTAQ 等优化式量化。
- 局部重构目标为：
  \[
  J(P,\tilde V)=\frac1T\|\tilde Z\tilde V^\top - ZV^\top\|_F^2
  \]
  衡量单层输出因激活和权重量化产生的误差。
- 变换形式设为 \(P=\Lambda^{-1}DH^\top\)：\(\Lambda\) 为通道缩放，\(D\) 为对角符号矩阵，\(H\) 为归一化 Hadamard 矩阵。

### 2.2 持续通道离群值分解

- 将激活分解为：
  \[
  X = X_{co}+X_{reg}
  \]
  - \(X_{co}\)：持续通道级离群分量，每个离群通道在所有 token 上取共享水平 \(L_k\)。
  - \(X_{reg}\)：剩余 token 相关变化与普通通道激活。
- 该“持久稀疏 CO 模型”用于分析旋转和缩放对离群结构的处理效果。

### 2.3 定理 1：局部量化误差精确分解

- 量化误差被精确分解为：
  - **AGWC 项**：activation-guided weight compensation，激活引导的权重补偿项。
  - **正交残差项**：位于量化激活列空间的正交补中，权重补偿无法改变。
- 直觉：
  - 对固定变换，权重优化可降低 AGWC 项；
  - 正交残差只能通过变换设计（旋转、缩放）改善。
- 正交残差进一步用两个量上界：
  - \(J_{co}(\Lambda,D)\)：持续离群通道相关量。
  - \(J_{reg}(\Lambda,D)\)：常规激活相关量。
- 该分解为“权重补偿 + 变换设计”提供了明确边界。

### 2.4 定理 2：Hadamard 旋转与符号选择

- 固定旋转 \(D=I\) 时：
  \[
  J_{co}(\Lambda,I)\le T\frac{N_{co}(X)^2}{d}L_{\Lambda,\max}^2
  \]
  即对离群通道数呈二次依赖，原因是多个离群通道在固定 Hadamard 符号下可能发生相长干涉。
- 随机符号 \(D\) 为独立 Rademacher 符号时，高概率下：
  \[
  J_{co}(\Lambda,D)\lesssim 2T\frac{N_{co}(X)}{d}L_{\Lambda,\max}^2(\log(2d)+\log(1/\delta))
  \]
  即依赖从 \(O(N_{co}^2)\) 降到 \(O(N_{co}\log d)\)。
- 采样多个符号矩阵 \(D^{(1)},\dots,D^{(N_s)}\) 时，最佳候选的 \(J_{co}\) 进一步受益：
  \[
  \min_{D\in\mathcal D_{N_s}}J_{co}\lesssim 2T\frac{N_{co}(X)}{d}L_{\Lambda,\
