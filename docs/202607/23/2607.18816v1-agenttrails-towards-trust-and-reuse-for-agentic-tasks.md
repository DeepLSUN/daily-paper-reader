---
title: "AgentTrails: Towards Trust and Reuse for Agentic Tasks"
title_zh: "AgentTrails: 迈向代理任务的信任与复用"
authors: "Eden Wu, Sonia Castelo, Yurong Liu, Cláudio T. Silva, Juliana Freire"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.18816v1"
tags: ["query:agent"]
score: 9.0
evidence: 从LLM智能体轨迹中提取结构化和溯源信息的系统
tldr: LLM驱动的代理在复杂任务中产生大量轨迹日志，但这些日志仅按时间顺序记录，隐藏了动作与工件间的数据流依赖，限制了开发者对代理行为的理解、比较、调试和复用。AgentTrails通过将原始轨迹转化为结构化溯源图，建模工具调用为计算动作，输入输出为数据工件，并支持多执行对比构建商图，从而揭示隐藏依赖、对齐差异执行、提取重复模式，提升代理任务的可信度与复用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18816v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1618, \"height\": 398, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18816v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 218, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18816v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 876, \"height\": 188, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18816v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 885, \"height\": 294, \"label\": \"Figure\"}]"
motivation: 现有轨迹日志按时间序列记录，隐藏数据流依赖，阻碍代理行为的理解、比较、调试和复用。
method: AgentTrails将原始轨迹转化为结构化溯源图，建模工具调用与数据工件，并通过商图对齐跨轨迹的重复结构和模式。
result: 在真实代理轨迹上，AgentTrails成功揭示隐藏依赖、对齐差异执行，并提取出重复的工具使用模式。
conclusion: AgentTrails通过结构化溯源表示增强了代理任务的可信度和复用性，支持开发者深入理解和分析代理行为。
---

## 摘要
基于大语言模型的代理通过调用工具、查询数据库、执行代码以及操作中间产物，日益广泛地处理复杂任务。这些代理遵循的执行轨迹通常存储为按时间顺序的日志，掩盖了底层的数据流——即其动作与所创建及操作产物之间的依赖关系。这限制了开发者理解代理轨迹、比较执行情况、调试失败以及复用计算的能力。我们提出AgentTrails，一个用于代理溯源与意义建构的原型系统。AgentTrails将原始轨迹转换为结构化溯源图，其中工具调用被建模为计算动作，输入输出被建模为数据产物。该系统通过将多个溯源图放置在共享画布上，并构建联合商图（对齐轨迹间重复出现的工具、产物及依赖结构），支持对执行情况的比较。在此表示基础上，AgentTrails支持模式提取、下游分析以及技能抽象。我们通过真实世界代理轨迹演示AgentTrails，表明它能够揭示隐藏依赖、对齐发散执行，并发现超越时间顺序日志的重复工具使用模式。

## Abstract
LLM-powered agents increasingly tackle complex tasks by invoking tools, querying databases, executing code, and manipulating intermediate artifacts. These agents follow trajectories that are typically stored as chronological logs, obscuring the underlying dataflow -- the dependencies between their actions and the artifacts they create and manipulate. This limits developers' ability to understand the agents' trails, compare executions, debug failures, and re-use the computations. We present AgentTrails, a prototype system for agent provenance and sensemaking. AgentTrails converts raw trajectories into structured provenance graphs, where tool calls are modeled as computational actions and inputs and outputs as data artifacts. The system supports the comparison of executions by placing multiple provenance graphs on a shared canvas and constructing a joined quotient graph that aligns recurring tools, artifacts, and dependency structures across trajectories. On top of this representation, AgentTrails supports pattern extraction, downstream analysis, and skill abstraction. We demonstrate AgentTrails on real-world agent trajectories, showing that it reveals hidden dependencies, aligns divergent executions, and surfaces recurring tool-use patterns beyond chronological logs.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于大语言模型（LLM）的智能代理在执行复杂任务时产生大量轨迹日志，但这些日志仅按时间顺序记录，隐藏了工具调用与中间产物之间的数据流依赖关系。这种表示方式导致开发者难以理解代理行为、比较不同执行过程、调试失败以及复用已有计算。
- **整体含义**：当前对代理轨迹的分析主要停留在原始文本序列层面，缺乏结构化、可依赖的抽象表示，限制了代理系统的可信度和可复用性。论文提出通过构建和比较溯源图来解决这一痛点，从而提升对代理行为的理解、信任和复用。

## 2. 论文提出的方法论

### 核心思想
将原始轨迹转化为结构化溯源图（provenance graph），其中工具调用建模为计算动作（活动），输入/输出/中间产物建模为数据实体，并显式表示生产者-消费者依赖关系。在此基础上，通过构建多轨迹联合商图（joined quotient graph）来对齐不同执行过程，提取重复模式和异常分支。

### 关键技术细节
1. **轨迹规范化和工具覆盖总览**：将每条轨迹标准化为动作序列，保留参数、响应、标识符等元信息。利用UpSet视图展示多轨迹中工具调用频率、轨迹级指标（得分、成本、延迟等），支持过滤和选择。
2. **单轨迹溯源图构建**：
   - 从轨迹中提取实体（输入、输出、文件、返回值等）和活动（工具调用）。
   - 依赖推断分两步：
     - **精确证据边**：如果后续工具调用的参数中显式引用了先前实体的标识符（ID、路径、文件名、URL等），则添加确定的有向边。
     - **语义候选边**：从共享语义值（查询词、表名、域名对象）中提取弱候选依赖，通过受约束的LLM精炼步骤验证并生成图补丁操作，确保只接受符合时间顺序和模式的有效边。所有边在审计时保持可区分。
3. **多轨迹联合商图构建**：
   - 将每个活动抽象为活动胶囊（activity capsule），描述工具身份、输入输出结构、局部图上下文和上游根路径。
   - 对所有胶囊进行聚类，相似度函数为加权和形式，包含多个特征比较（工具名称、输入输出结构、上下文等）。
   - 每个聚类成为一个联合活动节点，原始边根据聚类映射合并为联合边，记录支持迹、出现频次、多样性等信息。
   - 高支持结构表示重复工作流模式，低支持结构表示迹特异分支或异常。
4. **交互式可视化与协作助手**：提供单迹视图、联合视图、过滤/修剪功能，以及基于LLM的溯源助手（copilot）辅助解释已构建的图。

## 3. 实验设计

- **使用数据集/场景**：
  1. **SciAgentGym Physics 任务27**：求解假想基态氢原子（电子自旋3/2）的超精细跃迁频率。包含结构化的工具调用、数值输出、可视化文件。
  2. **Discovera 基因集探索任务 s2m_task_016**：执行Signature-to-Mechanisms分析，包括数据导入、过滤、合并、差异分析和富集分析。
- **Benchmark**：论文未使用现有正式benchmark，而是以任务本身的预期工具序列作为参考。另外，手注了10条轨迹的234条黄金依赖边用于初步评估。
- **对比方法**：未与其他方法进行系统性定量对比，主要展示了AgentTrails在案例中能揭示原始日志无法显示的依赖结构。

## 4. 资源与算力

- 论文中**未明确说明**使用了任何GPU型号、数量、训练时长等算力信息。系统原型主要基于LLM推理进行辅助精炼，但未提供具体计算资源配置。

## 5. 实验数量与充分性

- **实验数量**：两个使用场景演示，加上一个手注10轨迹、234条黄金边的初步依赖推断评估。没有进行大规模消融实验或定量比较。
- **充分性评估**：作为初步工作（preliminary work），实验数量较少，未覆盖长轨迹、大规模迹集合、不同代理框架和任务类型。论文明确指出许多问题（如依赖质量基准、胶囊相似度权重调优、可扩展性）尚未解决。因此实验**不够充分**，但符合论文自身定位（原型系统初步验证）。

## 6. 论文的主要结论与发现

- AgentTrails能够有效从原始时间顺序日志中重建显式溯源图，揭示隐藏的依赖结构（如扇入/扇出计算模式、中间产物跨步骤复用）。
- 通过构建联合商图，可以对齐不同执行过程，区分共享工作流核心路径与迹特异偏差分支，帮助识别低效或错误行为。
- 在真实科学代理任务上，溯源图比原始日志更能支持单迹验证和多迹比较，提升了代理任务的可信度和复用性。

## 7. 优点

- **新颖的溯源框架**：将代理轨迹视为可重构的溯源图，而非简单序列，提供结构化视角。
- **后验重建无需仪器化**：能够从任何原始日志中提取依赖，不依赖特定代理框架或工具模式，具有广泛适用性。
- **多迹对齐与比较**：通过活动胶囊聚类构建联合商图，解决了不同执行间因随机性和重复/缺失步骤导致的难以直接比较的问题。
- **可审计性**：精确边、语义候选边和LLM精炼边保持可区分，用户可追溯每条依赖的来源。
- **交互式可视化与协作**：提供UpSet总览、单迹图、联合图分层视图，并集成LLM助手辅助解释。

## 8. 不足与局限

- **缺乏黄金依赖基准**：没有公开可用的标准数据集来评估依赖推断的精确率和召回率。手注的10条轨迹样本量太小。
- **活动胶囊相似度权重未调优**：特征权重 \( w_m \) 需通过实验或学习确定，当前未提供方法。
- **可扩展性问题未评估**：对长轨迹（数千步）或大规模轨迹集合（数百条）的构建和可视化性能未作测试。
- **依赖推断质量有限**：依赖候选主要靠精确匹配和简单语义匹配，复杂依赖（如通过隐式上下文、LLM推理间接使用）可能缺失；LLM精炼步骤可能引入模型偏差，且受限于LLM的上下文窗口。
- **评估不够充分**：仅两个使用场景演示，缺少定量指标（如依赖发现准确率、对齐质量度量），也未与现有方法（如PROV-AGENT、AgentLens等）进行对比。
- **未覆盖所有代理任务类型**：主要关注工具调用型代理，未讨论多代理协作、对话型或纯推理型轨迹。
- **初步工作，许多问题开放**：如依赖重建质量、对齐抽象方式、可扩展性、权重调优等，均留作未来工作。

（完）
