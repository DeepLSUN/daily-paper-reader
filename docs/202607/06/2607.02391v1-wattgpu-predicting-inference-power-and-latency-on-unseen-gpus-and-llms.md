---
title: "WattGPU: Predicting Inference Power and Latency on Unseen GPUs and LLMs"
title_zh: WattGPU：在未见GPU和大语言模型上预测推理功耗与延迟
authors: "Mauricio Fadel Argerich, Jonathan Fürst, Marta Patiño-Martínez"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02391v1"
tags: ["query:llm"]
score: 9.0
evidence: 预测LLM在未知GPU上的推理功耗和延迟
tldr: "大语言模型推理部署需要匹配高效GPU，但现有工具依赖详尽分析且难以泛化。WattGPU仅利用公开的LLM元数据和GPU规格，预测平均功耗和Token间延迟，无需硬件访问。在42个LLM和8个GPU的测试中，对未见GPU的功耗预测误差低至3.4%，延迟预测误差8.5%，保持强排序相关性。相比TDP和roofline基线，误差降低2-4倍，为数据中心优化提供实用工具。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02391v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 775, \"height\": 611, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02391v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1298, \"height\": 532, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02391v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1623, \"height\": 563, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02391v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 951, \"height\": 721, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02391v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1324, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02391v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 985, \"height\": 463, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02391v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 998, \"height\": 394, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02391v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 916, \"height\": 1529, \"label\": \"Table\"}]"
motivation: 现有预测模型需要详尽分析且难以泛化到未见过GPU和LLM，部署优化缺乏高效工具。
method: WattGPU利用公开LLM元数据和GPU规格，构建功耗和延迟预测模型，无需硬件分析即可泛化。
result: "功耗误差低至3.4%，延迟误差8.5%，排序相关性大于等于0.76。"
conclusion: 相比物理基线误差降低2-4倍，开源工具助力数据中心快速选择最优GPU-LLM组合。
---

## 摘要
大语言模型（LLM）推理工作负载正迅速成为数据中心能耗增长的贡献者。优化这些部署需要将特定LLM与最高效的GPU匹配，但运营商目前缺乏在不详尽分析每种组合的情况下进行匹配的工具。虽然存在一些预测模型，但它们仍需要分析数据，并且难以泛化到训练中未见的硬件。为了解决这个问题，我们提出了 \textit{WattGPU}，包含两个预测模型：平均GPU功耗和令牌间延迟（ITL）。我们的方法仅利用公开可用的LLM元数据和GPU规格，无需硬件访问或分析，同时能够泛化到未见过的NVIDIA服务器级GPU和LLM。我们在包含42个开源LLM（0.1B–27B参数）和8个GPU的数据集上，通过严格的留一GPU和留一LLM交叉验证评估了我们的模型，覆盖离线和服务场景。在未见GPU上，平均功耗模型在离线和服务场景下的中位数绝对百分比误差分别为≤3.4%和≤13.5%，而延迟模型在服务模式下≤8.5%，两者在服务场景下均保持了强GPU排名相关性（Kendall τ≥0.76）。与标准的物理基准——负载缩放热设计功耗（TDP）用于功耗和屋顶线模型用于延迟相比，我们的模型在未见LLM-GPU组合的服务场景下将中位数绝对百分比误差降低了约4倍，对于完全未见的GPU则降低了约2倍。WattGPU的数据和代码公开于 https://github.com/maufadel/wattgpu。

## Abstract
Large Language Model (LLM) inference workloads are a rapidly growing contributor to data center energy consumption. Optimizing these deployments requires matching specific LLMs to the most efficient GPUs, but operators currently lack the tools to do so without exhaustively profiling each combination. While some predictive models exist, they still require profiling data and struggle to generalize to hardware unseen during training. To address this, we introduce \textit{WattGPU}, featuring two predictive models for mean GPU power draw and Inter-Token Latency (ITL). Our approach leverages only publicly available LLM metadata and GPU specifications, eliminating the need for hardware access or profiling while enabling generalization to unseen NVIDIA server-grade GPUs and LLMs. We evaluate our models using rigorous leave-one-GPU-out and leave-one-LLM-out cross-validation on a dataset of 42 open-source LLMs (0.1B--27B parameters) and 8 GPUs under both offline and server scenarios. The mean power draw model achieves a median absolute percentage error of $\leq3.4\%$ for offline and $\leq13.5\%$ for server scenarios on unseen GPUs, while the latency model achieves $\leq8.5\%$ in server mode, both maintaining strong GPU ranking correlations for server scenarios (Kendall $τ\geq0.76$). Compared to standard physically grounded baselines -- Load-Scaled Thermal Design Power (TDP) for power draw and roofline for latency -- our models reduce median absolute percentage error by approximately 4$\times$ on unseen LLM-GPU combinations for server scenarios or approximately 2$\times$ for completely unseen GPUs. WattGPU's data and code are publicly available at https://github.com/maufadel/wattgpu.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：LLM推理工作负载快速增长，成为数据中心能耗的主要贡献者。运营商需要将特定LLM匹配到最高效的GPU以节省能源，但现有工具要求对每个LLM-GPU组合进行详尽的实际分析（profiling），成本高昂且需要硬件访问，无法泛化到未见过（unseen）的GPU或LLM。
- **整体含义**：WattGPU旨在填补这一空白，通过仅利用公开可用的LLM元数据和GPU规格，预测平均GPU功耗和令牌间延迟（ITL），从而无需任何硬件分析即可做出部署决策，降低运营成本和碳足迹。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用基于公开数据的回归模型（XGBoost）直接预测LLM-GPU对的功耗和延迟特征，不依赖运行时指标。
- **特征工程**：
  - **GPU特征**：基础时钟频率、升压比、晶体管数量、内存带宽、发布年份、内存大小、FP16 TFLOPS、内存类型等。
  - **LLM特征**：模型类型、参数数量（对数变换）、层数、KV头数、注意力头数、隐藏层大小等。
  - **衍生特征**：
    - **升压比**：GPU升压时钟与基础时钟之比。
    - **带宽延迟**：模型大小（FP16字节）除以GPU内存带宽，表示读取所有权重所需时间。
    - **计算延迟**：理论下限，公式为：\( C_{\text{lat}} = \frac{2 \times P_{\text{model}} \times 10^9}{\text{GPU FP16 FLOPS}} \)，其中\( P_{\text{model}} \)为参数数量（十亿）。
- **模型选择与训练**：
  - 均使用XGBoost回归器，超参数通过网格搜索确定（max_depth、reg_lambda、n_estimators）。
  - **功耗模型**：目标变量归一化为GPU功耗除以其TDP，使模型学习规模无关的功耗行为，增强泛化能力。
  - **延迟模型**：直接预测ITL。
- **关键创新**：首次在未见GPU上评估预测模型；完全基于公开元的特征工程；使用留一法和交叉验证严格测试泛化性。

## 3. 实验设计：数据集、场景、基准方法

- **数据集**：使用“Watt Counts”数据集（公开），原始包含50个LLM和10个NVIDIA GPU，本研究排除2个消费级GPU（RTX 4090、3090）及Mixture-of-Experts模型，最终使用**42个密集LLM**（0.125B–27B参数）和**8个服务器级GPU**（Tesla V100 SXM2, T4, A100 SXM4, A30, L40S, L4, H100 NVL, H200 NVL）。
- **场景**：
  - **离线（Offline）**：大批量请求并发，GPU满负载运行。
  - **服务器（Server）**：请求按泊松分布到达，分为低负载（λ=0.017）和高负载（λ=0.33）两种子场景。
- **基准方法**：
  - **功耗预测**：朴素TDP（假设功耗等于TDP）和负载缩放TDP（线性缩放因子ρ，通过扫描0.1步长选择最优ρ）。
  - **延迟预测**：屋顶线（Roofline）基线，近似为将LLM的FP16权重从内存流式传输一次所需时间：\( T_{\text{ITL}} = \frac{2 \times P_{\text{model}} \times 10^9}{\text{Memory Bandwidth} \times \text{Batch Size}} \)。批量大小：服务器=1，离线=256。
- **评估策略**（三种，难度递增）：
  1. **5折交叉验证（CV）**：同一GPU和LLM在训练中见过，但该组合未见。
  2. **留一GPU（LOGO）**：每个GPU单独作为测试集。
  3. **留一LLM（LOLO）**：每个LLM单独作为测试集。
- **评估指标**：中位数绝对百分比误差（MdAPE）、Pearson相关系数r、Kendall τ（分别计算GPU排名和LLM排名）。

## 4. 资源与算力

- **文中未明确说明训练消耗的算力**（GPU型号、数量、训练时长等）。
- 仅提到使用XGBoost进行训练，特点是效率较高，但具体计算资源未给出。数据集由已公开的Watt Counts提供，模型训练可能在一台普通GPU或CPU上进行，但论文未声明。

## 5. 实验数量与充分性

- **实验数量**：覆盖42个LLM × 8个GPU = 336个基础组合，每个组合在离线、低负载服务器、高负载服务器三种场景下重复测量，数据量充足。
- **评估策略**：三种验证方式（CV、LOGO、LOLO），对每种方法分别报告多个指标，说明结果具有统计可靠性。
- **基准对比**：功耗模型对比了两种TDP基线；延迟模型对比了屋顶线基线。基线均为无学习型，保证了对比公平。
- **是否充分**：实验设计系统全面，覆盖了泛化到未见GPU和未见LLM的核心场景。但局限是仅限NVIDIA服务器级GPU，未覆盖消费级、AMD或Intel GPU；仅限密集LLM，未覆盖MoE或量化模型；未涉及多GPU部署。总体而言，在给定目标范围内是充分的。

## 6. 主要结论与发现

- **功耗预测**：
  - CV下离线MdAPE=1.6%，服务器MdAPE=5.5%；LOGO下离线3.4%、服务器13.5%；LOLO下离线2.0%、服务器6.7%。
  - 在服务器场景下GPU排名Kendall τ≥0.76（LOGO），优于TDP基线（τ=0.60）。
- **延迟预测**：
  - CV下服务器MdAPE=4.9%，LOGO服务器8.5%；离线更困难（LOGO MdAPE=24.9%）。
  - 服务器场景GPU排序τ≥0.78（LOGO），但离线LOGO下屋顶线基线的GPU排序τ（0.82）超过模型（0.72），而模型的绝对误差仍远低于屋顶线（24.9% vs 80.2%）。
- **基准对比**：模型相比最强基线（负载缩放TDP和屋顶线）在未见LLM-GPU组合的服务场景下误差降低约4倍，完全未见GPU时降低约2倍。
- **特征重要性**：功耗模型中“场景”最重要，其次是内存带宽相关特征；延迟模型中“带宽延迟”最重要（超过场景），确认ITL为内存受限。

## 7. 优点

- **零硬件分析**：仅凭公开元数据即可预测，极大降低部署优化门槛。
- **强泛化能力**：首次在未见GPU上验证，离线功耗误差仅3.4%，服务器功耗误差13.5%，具备实用价值。
- **排序质量高**：服务器场景下GPU排名Kendall τ≥0.76，可直接用于选取最节能GPU。
- **开源与可复现**：代码、数据、模型均公开，便于验证和扩展。
- **物理可解释性**：特征工程基于硬件和模型物理特性（带宽、计算延迟），便于理解预测依据。

## 8. 不足与局限

- **实验覆盖**：
  - 仅限NVIDIA服务器级GPU（8款），未覆盖消费级、AMD、Intel或其他加速器。
  - 仅限密集式LLM（42个），未涉及MoE、量化模型。
  - 仅限单GPU部署，不支持多GPU并行。
- **离线延迟预测困难**：LOGO下ITL MdAPE=24.9%，主要因为静态特征无法捕捉vLLM的连续批处理行为，需引入工作负载感知特征（如KV缓存用量）才能改进，但会牺牲完全基于公开数据的前提。
- **场景限制**：仅覆盖两类服务器负载（低λ=0.017、高λ=0.33），未涵盖更广泛的负载分布。
- **风险**：对新型GPU架构（如发布年份较新且特征空间外插）的预测能力未知。
- **应用限制**：模型仅预测平均值，不提供分布或置信区间；实时动态调整场景（如突发请求）可能超出模型能力。

（完）
