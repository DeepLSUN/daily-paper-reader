---
title: Distilled Reinforcement Learning for LLM Post-training
title_zh: 蒸馏强化学习用于大语言模型后训练
authors: "Chen Wang, Zhaochun Li, Jionghao Bai, Yining Zhang, Hexuan Deng, Ge Lan, Yue Wang"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17247v1"
tags: ["query:post-train"]
score: 9.0
evidence: 蒸馏强化学习用于LLM后训练
tldr: "大语言模型后训练中，强化学习依赖粗粒度结果监督导致信用分配困难，在线策略蒸馏无条件模仿教师带来知识转移困境。本文提出蒸馏强化学习，将教师监督整合到RL目标中，通过反向重要性采样、负样本重置和序列级几何归一化，实现选择性知识转移。实验表明，在族内和跨族蒸馏场景下，该方法在pass@1和pass@k上显著优于标准RL和在线策略蒸馏。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17247v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1407, \"height\": 576, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1450, \"height\": 608, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1446, \"height\": 341, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 589, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1452, \"height\": 721, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17247v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 589, \"height\": 398, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17247v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 468, \"height\": 134, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 654, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1442, \"height\": 573, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1443, \"height\": 549, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17247v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1273, \"height\": 1550, \"label\": \"Table\"}]"
motivation: 现有RL后训练信用分配困难且难以获取新知识，在线策略蒸馏在教师差异大时指导无效，亟需一种能选择性转移知识的方法。
method: 提出Distilled RL，包含带裁剪的反向重要性采样、负样本重置和序列级几何归一化，将教师监督融入RL目标实现细粒度选择性知识转移。
result: "在族内和跨族蒸馏实验中，Distilled RL在pass@1和pass@k上均显著优于标准RL和在线策略蒸馏。"
conclusion: Distilled RL有效结合教师监督与强化学习，实现了更高效的知识转移，为LLM后训练提供了新范式。
---

## 摘要
大语言模型（LLM）后训练对于提升推理、适应性和对齐能力至关重要。现有方法主要遵循两种范式：强化学习（RL）和同策略蒸馏（OPD）。然而，RL依赖粗粒度的结果监督，导致信用分配困难且获取新知识的能力有限。同时，OPD通过KL散度无条件匹配教师logits，这造成了一个困境：相似的教师提供的新知识很少，而差异显著的教师往往产生无效指导，极大限制了OPD在族内蒸馏中的应用。我们提出了蒸馏强化学习（Distilled RL），它将教师监督整合到RL目标中，以提供细粒度指导，选择性传递新知识并避免无条件模仿。Distilled RL包含三个组成部分：带裁剪的反向重要性采样、负样本重置以及序列级几何归一化。通过一个简洁且可解释的案例研究，我们证明了Distilled RL能够有效地从教师模型向学生模型传递先前不可用的知识。在族内和跨族蒸馏设置下的广泛实验表明，Distilled RL在pass@1和pass@k指标上均显著优于标准RL和OPD。我们的代码可在 https://github.com/597358816/Distilled-RL 获取。

## Abstract
Large language model (LLM) post-training is essential for improving reasoning, adaptation, and alignment. Existing methods mainly follow two paradigms: reinforcement learning (RL) and on-policy distillation (OPD). However, RL relies on coarse-grained outcome supervision, resulting in difficult credit assignment and limited capability to acquire new knowledge. OPD, meanwhile, unconditionally matches teacher logits through KL divergence, which creates a dilemma: similar teachers provide little new knowledge, while substantially different teachers often yield ineffective guidance, largely restricting OPD to within-family distillation. We propose Distilled Reinforcement Learning (Distilled RL), which integrates teacher supervision into the RL objective to provide fine-grained guidance, selectively transfer new knowledge and avoid unconditional imitation. Distilled RL contains three components: reverse importance sampling with clipping, negative sample reset, and sequence-level geometric normalization. Through a concise and interpretable case study, we demonstrate that Distilled RL can effectively transfer previously unavailable knowledge from a teacher model to a student model. Extensive experiments across both within-family and cross-family distillation settings show that Distilled RL substantially outperforms standard RL and OPD in terms of both pass@1 and pass@k. Our code is available at https://github.com/597358816/Distilled-RL.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：大语言模型（LLM）后训练是提升推理、指令遵循和任务适应能力的关键环节。当前主流后训练方法分为两大类：强化学习（RL）和同策略蒸馏（OPD）。RL（如GRPO）使用规则化结果奖励作为粗粒度信号，导致信用分配困难（只能给整个序列一个奖励，无法区分有用/无用token），且难以从教师模型获取学生策略中不存在的新知识。OPD通过KL散度让学生无条件模仿教师分布，但当教师与学生差异较大时（例如跨模型家族），指导变得噪声大、难优化；而当教师与学生相似时，知识迁移有限且容易过早收敛。
- **核心问题**：如何将教师监督与RL目标有机结合，实现选择性、细粒度的知识迁移，避免无条件模仿带来的陷阱，同时保留RL持续优化的能力。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**蒸馏强化学习（Distilled RL）**，将教师对生成token的偏好直接整合到RL的梯度中，而非使用独立的KL散度损失。通过在RL目标中引入基于**反向重要性采样**的权重，选择性放大或缩小每个token的更新信号。
- **关键技术细节**：
  1. **反向重要性采样与裁剪**：定义教师与旧学生策略的token级比例 \( \rho_{i,t} = \frac{\pi_{\text{teacher}}(o_{i,t} | \cdot)}{\pi_{\theta_{\text{old}}}(o_{i,t} | \cdot)} \)，并裁剪至 \( [1/\epsilon_\rho, \epsilon_\rho] \) 防止极端值。
  2. **负样本重置**：仅对优势为正（\( A_i > 0 \)）的响应应用教师权重；对负优势响应，将权重设为1（即退化为原始RL目标）。避免教师信号在错误方向上误导学生。
  3. **序列级几何归一化**：对每个响应内的裁剪后比例，计算几何均值并归一化，使得整个响应的几何平均乘积为1。消除教师/学生似然度的系统性偏差，保留相对偏好，防止长响应被过度抑制。
- **最终的目标函数**（式3）：
  \[
  J_{\text{Distilled RL}}(\theta) = \mathbb{E}_{q, \{o_i\}} \left[ \frac{1}{G} \sum_i \frac{1}{|o_i|} \sum_t \min\left( r_{i,t}(\theta) w_{i,t} A_i, \text{clip}(r_{i,t}, 1-\epsilon_{\text{low}}, 1+\epsilon_{\text{high}}) w_{i,t} A_i \right) \right]
  \]
  其中 \( w_{i,t} \) 如上所述。

### 3. 实验设计：数据集、benchmark、对比方法

- **训练数据**：DAPO-17K（数学推理提示集）。
- **评估benchmark**：
  - 数学推理：AIME24/25/26, CMIMC25, HMMT25, AMC23, GSM8K, MATH500, Minerva Math, Olympiad（共10个基准）。
  - 知识密集泛化：MMLU-Pro 和 SuperGPQA（固定500子集）。
- **对比方法**：
  - 基模型（不加任何后训练）
  - OPD（同策略蒸馏，使用反向KL散度）
  - RL（GRPO）
  - OPD+RL（简单混合损失，比例1:1）
  - Distilled RL（本文方法）
- **学生模型**：DeepSeek-R1-Distill-Qwen-1.5B (DSQW-1.5B, 跨家族蒸馏)、Qwen3-4B（族内蒸馏）、Qwen3-1.7B。
- **教师模型**：Qwen3-8B-GRPO（用于所有蒸馏设置）。

### 4. 资源与算力

- 论文中**未明确说明**具体使用的GPU型号、数量及训练时长。仅提到在EasyR1和VeRL框架上实现，训练配置包括：全局batch size 128，微批大小64，每GPU微批大小2，rollout组大小8，最大提示长度2048，最大生成长度8192，训练步数160步（附录表5）。未提及硬件资源。

### 5. 实验数量与充分性

- **实验数量**：
  - 主实验（表2）：在3个学生模型上，对比4种方法+基模型，报告10个数学基准的pass@1，共约15个条件（模型×方法）×10 = 150+个数据点。
  - 泛化实验（表3）：在DSQW-1.5B和Qwen3-4B上报告pass@1和pass@16，在6个基准上对比。
  - 训练动态分析（图2、3、5）：可视化OPD、RL、OPD+RL、Distilled RL的奖励、熵、长度、平均pass@1曲线。
  - 消融实验（表4）：在DSQW-1.5B和Qwen3-4B上移除负样本重置或几何归一化，在12个基准上报告结果。
  - 可解释性案例（图4、6）：通过温度控制的教师验证熵转移能力。
- **充分性与公平性**：
  - 实验覆盖了族内和跨家族蒸馏场景，对比了当前主流基线，消融实验验证了每个组件的必要性。
  - 评估集包含多个竞赛级数学推理和知识密集型任务，避免了单一overfitting。
  - 但未与最新RLHF/DPO变体（如PPO、DPO）对比，也未在更大模型（如7B、13B）上验证；训练数据仅限DAPO-17K，泛化性存疑。整体设计合理，但覆盖面可进一步扩展。

### 6. 论文的主要结论与发现

1. **Distilled RL在所有设置下均优于RL、OPD和OPD+RL**：在DSQW-1.5B上平均pass@1提升+4.73（vs OPD）、+3.14（vs RL）；在Qwen3-4B上平均58.96，超越所有基线。
2. **跨家族蒸馏中优势更明显**：DSQW-1.5B（跨家族）下OPD表现弱于RL，而Distilled RL进一步提升，证明选择性权重比无条件KL更鲁棒。
3. **训练动态更优**：Distilled RL保持持续稳定的奖励增长、合理熵水平和响应长度，而OPD快速饱和、RL上升慢、OPD+RL无明显增益。
4. **消融实验证实负样本重置和几何归一化不可或缺**：移除负样本重置导致大幅下降（‑6.39~‑8.81点）；移除几何归一化也造成一定下降（‑1~‑2点）。
5. **可解释案例表明Distilled RL能传递教师分布属性（如熵）**，而纯RL无法做到，说明方法具备学习新知识的能力。

### 7. 优点

- **方法创新性**：将教师指导直接融入RL梯度而非独立惩罚项，概念简洁且理论动机清晰。
- **设计合理**：反向重要性采样（教师决定学生token的相对偏好）+ 负样本重置（避免错误方向）+ 几何归一化（保持序列量级一致），三者协同有效。
- **实验充分**：多模型、多基准、多指标（pass@1, pass@k, 训练动态, 消融, 可解释案例），结论稳健。
- **可解释性**：通过熵控制实验直观展示了知识迁移过程，增加了方法可信度。
- **实用性**：在族内和跨家族场景均有效，且无需改变基础RL框架，便于在现有管线中集成。

### 8. 不足与局限

- **教师能力假设**：方法隐式假设当学生正确时教师也足够胜任，但实际中教师不一定在所有问题上优于学生。论文在附录A中承认此局限，并建议未来引入教师侧rollout或置信度门控。
- **实验覆盖范围有限**：仅测试了数学推理和两个知识基准；未涵盖编程、对话、长文本生成等任务。教师模型仅用Qwen3-8B，未探索其他规模或家族的教师。
- **对比基线不够先进**：未与最新的RLHF方法（如DPO、PPO）或蒸馏变体（如MiniLLM）对比，也未与强化学习+过程奖励模型方法对比。
- **未报告计算成本**：缺少显存、时间、GPU型号等细节，不利于复现和公平比较效率。
- **潜在偏差**：训练数据DAPO-17K规模较小（17k提示），可能不足以充分展示大规模下方法的优势。
- **未来工作方向**：需要处理教师能力不确定的情况，以及更复杂的任务场景。

（完）
