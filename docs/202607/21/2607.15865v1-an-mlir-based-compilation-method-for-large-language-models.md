---
title: An MLIR-Based Compilation Method for Large Language Models
title_zh: 基于MLIR的大型语言模型编译方法
authors: "Pengchao Hu, Zhibin Xin, Yifan Chen, Yangyang Zhou, Liang Wang"
date: 2026-07-17
pdf: "https://arxiv.org/pdf/2607.15865v1"
tags: ["query:llm"]
score: 9.0
evidence: 大语言模型推理编译优化
tldr: 大语言模型在AI加速器上部署面临模型导入和自回归推理调度的两大挑战。本文提出基于MLIR的编译方法，设计高层图方言TopOp和目标硬件方言TpuOp，通过逐层降级将模型语义映射到芯片决策。每个Transformer层被分为prefill、prefill_kv和decode三阶段静态编译，以适配不同计算特性。该方法在TPU-MLIR和LLM-TPU项目中实现，支持Qwen、Llama等模型系列及GPTQ、AWQ等量化，有效解决了LLM高效部署问题。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15865v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 664, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1493, \"height\": 1230, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 692, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1375, \"height\": 1003, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15865v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 502, \"height\": 927, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15865v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1810, \"height\": 616, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1810, \"height\": 757, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15865v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1373, \"height\": 284, \"label\": \"Table\"}]"
motivation: 解决LLM在专用硬件上部署时模型导入困难以及有限片上内存下自回归推理调度效率低的问题。
method: 使用MLIR，设计TopOp和TpuOp两个方言，模型从TopOp逐层降级到TpuOp，并将每个Transformer层拆分为prefill、prefill_kv、decode三阶段静态编译。
result: 实现了TPU-MLIR编译器和LLM-TPU部署项目，支持Qwen、Llama、InternVL、MiniCPM-V等模型及GPTQ、AWQ、AutoRound等多量化形式。
conclusion: 提出基于MLIR的编译架构，通过方言分层和三阶段编译，高效支持LLM在AI加速器上的部署，具备框架和芯片无关性。
---

## 摘要
大型语言模型（LLMs）已成为现代AI加速器上的主要工作负载，但在专用硬件上部署它们仍面临两个核心挑战：如何将训练好的模型导入编译器友好的中间表示，以及如何在有限的片上内存下高效调度自回归推理循环。本文提出了一种基于MLIR（多层次中间表示）的大型语言模型编译方法，使用两个运算符方言进行说明，即TopOp和TpuOp。TopOp作为高层图方言，独立于源框架和目标芯片，负责表达模型语义；TpuOp作为目标硬件方言，携带芯片相关决策，如量化、层组和内存布局。模型首先表示为TopOp，然后逐层降低到TpuOp，最后生成可部署的二进制文件。此外，每个Transformer层被分为三个阶段进行静态编译：预填充、预填充kv（带有历史键值缓存的预填充）和解码，以适应提示并行处理和逐token生成的不同计算特性。该方法已在TPU-MLIR编译器（https://github.com/sophgo/tpu-mlir）和LLM-TPU部署项目（https://github.com/sophgo/LLM-TPU）中实现，支持包括Qwen、Llama、InternVL和MiniCPM-V系列在内的多种生成模型，以及GPTQ、AWQ和AutoRound等多种量化和部署形式。

## Abstract
Large Language Models (LLMs) have become the dominant workload on modern AI accelerators, yet deploying them on specialized hardware still faces two core challenges: how to import a trained model into a compiler-friendly intermediate representation, and how to efficiently schedule the autoregressive inference loop under limited on-chip memory. This paper presents an MLIR (Multi-Level Intermediate Representation) based compilation method for large language models, illustrated using two dialects of operators, TopOp and TpuOp. TopOp serves as a high-level graph dialect that is independent of both the source framework and the target chip, and is responsible for expressing model semantics; TpuOp serves as the target hardware dialect, carrying chip-related decisions such as quantization, layer groups, and memory layout. A model is first represented as TopOp, then lowered layer by layer to TpuOp, and finally a deployable binary is generated. In addition, each Transformer layer is split into three stages for static compilation: prefill, prefill_kv (prefill with historical key-value cache), and decode, so as to accommodate the different computational characteristics of prompt-parallel processing and per-token generation. The method has been implemented in the TPU-MLIR compiler{https://github.com/sophgo/tpu-mlir} and the LLM-TPU deployment project\footnote{https://github.com/sophgo/LLM-TPU}, supporting a variety of generative models including the Qwen, Llama, InternVL, and MiniCPM-V series, as well as multiple quantization and deployment forms such as GPTQ, AWQ, and AutoRound.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大型语言模型（LLM）在专用AI加速器上部署面临两个主要挑战：
  1. 如何将训练好的模型从PyTorch、HuggingFace等框架导入到编译器友好的中间表示（IR）中。
  2. 如何在有限片上内存下高效调度自回归推理循环（即预填充和解码阶段的计算特性差异大）。
- **整体含义**：传统的为每个硬件手工编写算子库的方法成本高、难以跟上模型快速演进。本文提出基于MLIR（Multi-Level Intermediate Representation）的编译方法，通过设计高层图方言（TopOp）和目标硬件方言（TpuOp），并引入三阶段静态编译，自动化整个从模型导入到生成可部署二进制的过程，从而降低部署复杂度，提高硬件利用率。

### 2. 论文提出的方法论

- **核心思想**：利用MLIR的可扩展性，定义两个层次的算子方言，并采用“分层降级 + 阶段拆分”的策略。
  - **TopOp**：高度图方言，与源框架和目标芯片无关，专门表达模型语义（如MatMul、RMSNorm、FAttention、MLP等融合算子）。它保持了高层融合边界，允许后续降级时根据芯片特性选择最优融合策略。
  - **TpuOp**：目标硬件方言，携带芯片相关决策，包括数据类型（F32、BF16、INT8等）、量化格式（对称/非对称INT8、AWQ/GPTQ/AutoRound的权重量化）、层组信息（Group Info）、算子拆分信息、地址信息以及目标芯片指令封装。
- **关键技术细节**：
  - **模型导入与TopOp生成**：通过`llm_convert.py`加载HuggingFace模型配置和权重分片，然后按模型家族分发到相应转换器，依次生成TopOp MLIR模块（每个Transformer层独立一个模块）。
  - **从TopOp到TpuOp的降级**：标准pass pipeline将每个TopOp替换为对应的TpuOp，并附加量化参数、层组信息、物理地址等。支持直接编译F32/BF16/F16/INT8以及AWQ/GPTQ/AutoRound预量化模型。降级后继续进行层组切分、片上/片外内存分配和代码生成，最终输出每个模块的目标二进制。
  - **三阶段静态编译**：每个Transformer层编译为三个变体：
    - **Prefill**：处理整个prompt的所有token，输入为`[hidden_states, position_ids]`，输出为`[hidden_states, key_cache, value_cache]`，无历史KV。
    - **Prefill kv**：与prefill数据流相同，但输入增加`history_k, history_v`，内部通过`top.Concat`将新计算的KV与历史缓存拼接，用于多轮对话或长提示分段。
    - **Decode**：每次处理一个新token，输入为`[hidden_states, position_ids, history_k, history_v]`，输出为`[hidden_states, key_cache, value_cache]`。

  运行时按条件选择相应函数：第一轮用Prefill + Decode；后续轮次用Prefill kv + Decode；超长prompt可分段多次调用Prefill kv。

- **典型优化**：
  1. 因果掩码重用小尺寸掩码（如128×128）代替全尺寸掩码，节省带宽。
  2. 位置编码预计算cos/sin表并存储为权重，运行时通过`top.Gather`获取，避免实时计算。
  3. KV缓存地址静态规划，利用地址重用或原地更新实现`Concat`，避免物理拷贝。

### 3. 实验设计

- **使用的数据集/场景**：论文未提及任何具体数据集或Benchmark。它声称该方法已在真实编译器项目（TPU-MLIR和LLM-TPU）中实现，并支持多种模型：Qwen、Llama、InternVL、MiniCPM-V系列，以及多种量化形式（GPTQ、AWQ、AutoRound）。
- **Benchmark与对比方法**：论文中没有设置任何与传统编译框架（如TVM、XLA）或手工优化库的对比实验，也未给出推理延迟、吞吐量、内存占用等量化指标。

### 4. 资源与算力

- 论文**未提及**任何关于GPU型号、数量、训练时长或推理算力资源的信息。仅说明基于Sophgo的专用AI加速器（TPU）实现，但未说明具体硬件配置。

### 5. 实验数量与充分性

- **实验数量**：几乎没有传统意义上的实验。全文仅以方法论描述和项目实现为主，缺乏消融实验、对比实验或性能分析。
- **充分性评判**：不充分。没有定量证据证明该方法的编译效率、推理速度、内存节省等优势。且未与其他编译器或部署框架（如vLLM）进行比较。客观性和公平性难以评估。

### 6. 论文的主要结论与发现

- **主要结论**：基于MLIR的编译方法，通过高层方言TopOp建立框架无关的模型表示，再通过TpuOp承载硬件特定决策，并采用三阶段静态编译策略，能够有效解决LLM在AI加速器上的部署难题。该方法已成功落地于TPU-MLIR和LLM-TPU项目，支持多种主流生成模型和量化格式，证明了其通用性和实用性。

### 7. 优点

- **框架无关性**：TopOp完全独立于PyTorch、TensorFlow等源框架，可适配多种Transformer架构（密集解码器、分组查询注意力、混合专家、视觉语言模型）。
- **模块化编译**：按层、按阶段切分模块，支持并行编译，运行时可按需组合，降低编译复杂度。
- **静态形状保障**：为每个阶段预编译固定形状的变体，避免了动态编译的开销，且能使硬件指令更简洁、内存分配更确定。
- **实用优化技巧**：如因果掩码重用、位置编码预取、KV缓存静态管理，贴合实际硬件特性。
- **广泛的量化支持**：直接编译F32/BF16/F16/INT8以及多种主流量化方法（GPTQ、AWQ、AutoRound），便于部署压缩模型。

### 8. 不足与局限

- **缺乏定量实验**：没有提供推理延迟、吞吐量、编译时间、内存占用等关键性能指标，无法评估方法实际效率。
- **无对比基线**：未与TVM、XLA、TensorRT、vLLM等已有方案比较，无法体现方法相对优势。
- **通用性受限**：论文以Sophgo TPU为例，对其它类型AI加速器（如GPU、NPU）的适用性未讨论。
- **静态形状的灵活性不足**：三阶段静态编译需要预编译多个变体（如不同长度下的decode），当序列长度变化范围大时，需要较多预编译版本，管理成本增加。论文未详细说明如何处理超长上下文（如128K）的场景。
- **无消融实验**：未论证每个设计（如TopOp融合算子、三阶段拆分、预计算优化）的独立贡献。
- **资源信息缺失**：未说明完成编译所需的计算资源和时间，也未提及编译器本身的性能开销。

（完）
