---
title: "OR Else: A Differentiable Trust Region for Policy Optimization"
title_zh: OR Else：一种用于策略优化的可微信赖域
authors: "Chinmay Rane, Kanishka Tyagi, Michael Manry"
date: 2026-07-20
pdf: "https://arxiv.org/pdf/2607.18163v1"
tags: ["query:post-train"]
score: 9.0
evidence: LLM后训练中可微信任区域策略优化
tldr: 针对大语言模型后训练中PPO/GRPO的截断目标在有利方向饱和时导数突变的问题，提出一种平滑单侧饱和规则Output Reset (OR)，在rollout相对token对数比空间中使用OR平方边际损失代替截断策略项，优势符号决定更新方向。在Llama-3.2-1B-Instruct和hh-rlhf数据集上，PPO-OR（GAE）平均奖励比PPO-clip高0.305但跨种子方差更大；GRPO-OR（组相对优势）未提高平均分但方差更小、终末OR残差接近零且过冲比例下降，而两种方法在GAE和组相对优势下rollout与当前策略的对数比位移差异显著。OR改变了优化行为，但奖励效应因比较设置而异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.18163v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1491, \"height\": 828, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18163v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1523, \"height\": 623, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18163v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1512, \"height\": 1184, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18163v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1505, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18163v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1525, \"height\": 535, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.18163v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1529, \"height\": 542, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.18163v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1666, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18163v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1692, \"height\": 449, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18163v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1672, \"height\": 297, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18163v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 262, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18163v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1671, \"height\": 598, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.18163v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1667, \"height\": 861, \"label\": \"Table\"}]"
motivation: PPO/GRPO的截断目标在有利方向饱和导致导数突变，需要更平滑的替代。
method: 使用Output Reset (OR)平方边际损失替换截断策略项，在rollout相对token对数比空间中根据优势符号决定方向。
result: PPO-OR（GAE）平均奖励比PPO-clip高0.305但方差更大；GRPO-OR未提高平均分但更稳定，终末残差和过冲比例下降。
conclusion: OR改变优化行为但奖励效应因优势计算方式而异，在组相对优势下未带来奖励提升。
---

## 摘要
本研究中探讨的PPO和GRPO基线使用裁剪的替代目标，其有利方向饱和会导致标量目标导数发生突变。我们探究输出重置（OR）——一种平滑的单侧饱和规则——是否为大型语言模型的后训练提供了有用的替代方案。PPO-OR和GRPO-OR在相对于rollout的token对数比率空间中，用OR平方边际损失替代裁剪策略项；优势符号决定更新方向，且一个token在越过有利边际后不贡献直接的OR残差。我们在广义优势估计（GAE）下比较PPO-clip与PPO-OR，在组相对优势下比较GRPO与GRPO-OR，使用基于Anthropic hh-rlhf数据集训练的Llama-3.2-1B-Instruct模型，每种方法共享一个奖励模型和三个种子。在GAE下，PPO-OR的平均最终训练时奖励模型得分比PPO-clip高0.305，且观察到跨种子的分布更广。在组相对优势下，GRPO-OR的平均得分并未更高，但观察到分布更窄、终端OR残差接近零、超调比例下降，而匹配的GRPO裁剪目标轨迹仍然可变。两种组相对方法均显示出比GAE方法大得多的rollout到当前对数比率位移，而OR并未持续减少该位移。因此，在两种匹配比较中，OR改变了优化行为，但观察到的奖励效果不同。在G=2时，GRPO-OR的诊断结果并未转化为奖励得分的提升。更大的组是否改变这一结果仍有待研究。报告的得分为训练时奖励模型测量值，而非保留的人类偏好性能。

## Abstract
PPO and the GRPO baseline studied here use clipped surrogate objectives whose favorable-direction saturation introduces an abrupt change in the scalar objective's derivative. We ask whether Output Reset (OR), a smooth one-sided saturation rule, offers a useful alternative for large language model post-training. PPO-OR and GRPO-OR replace the clipped policy term with an OR squared-margin loss in rollout-relative token log-ratio space; the advantage sign determines the update direction, and a token contributes zero direct OR residual after crossing the favorable margin. We compare PPO-clip with PPO-OR under generalized advantage estimation (GAE), and GRPO with GRPO-OR under group-relative advantages, using \texttt{Llama-3.2-1B-Instruct} on Anthropic \texttt{hh-rlhf} with one shared reward model and three seeds per method. Under GAE, PPO-OR has a mean final training-time reward-model score $0.305$ higher than PPO-clip, with a larger observed across-seed spread. Under group-relative advantages, GRPO-OR does not have a higher mean score, but shows a smaller observed spread, a near-zero terminal OR residual, and a declining overshoot fraction, while the matched GRPO clipped-objective trace remains variable. Both group-relative methods exhibit substantially larger rollout-to-current log-ratio displacement than the GAE methods, and OR does not consistently reduce it. Thus, OR changes optimization behavior in both matched comparisons, but the observed reward effect differs between them. At $G=2$, the GRPO-OR diagnostics do not translate into a reward-score gain. Whether larger groups change this outcome remains open. The reported scores are training-time reward-model measurements, not held-out human-preference performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在大型语言模型（LLM）的后训练中，常用的PPO和GRPO方法使用裁剪（clipped）的替代目标函数。当策略更新在有利方向（即优势函数为正或负时，使概率比向有利方向移动）越过裁剪边界后，目标函数的导数会突然变为零，这种**导数突变**可能影响优化稳定性和最终性能。
- **整体含义**：论文探究一种名为**输出重置（Output Reset, OR）**的平滑单侧饱和规则是否能为LLM后训练提供更优的替代方案。OR源自分类任务中的目标重置机制，论文将其移植到策略优化中，形成PPO-OR和GRPO-OR，旨在实现更平滑的梯度过渡，改善优化行为。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：用**OR平方边际损失**替换PPO/GRPO中的裁剪策略项。对于每个token，使用**对数比率** \(\rho_t = \log \pi_\theta(a_t|s_t) - \log \pi_{\theta_{\text{old}}}(a_t|s_t)\) 衡量相对概率变化。优势函数的符号决定更新方向：当 \(\hat{A}_t > 0\) 时提高该token对数概率，当 \(\hat{A}_t < 0\) 时降低；当 \(\hat{A}_t = 0\) 时不更新。
- **关键技术细节**：
  - **OR损失函数**（单token形式）：
    \[
    \ell_{\text{OR},t} = \begin{cases}
    \left[\max\left(0, \alpha_{\text{OR}} - \text{sign}(\hat{A}_t) \rho_t\right)\right]^2, & \hat{A}_t \neq 0 \\
    0, & \hat{A}_t = 0
    \end{cases}
    \]
    其中 \(\alpha_{\text{OR}}\) 为边际超参数。当token在有利方向越过边际时（即 \(\text{sign}(\hat{A}_t)\rho_t > \alpha_{\text{OR}}\)），损失和梯度**连续变为零**，实现平滑饱和。
  - **整体损失**：PPO-OR在GAE（广义优势估计）框架下，结合价值函数MSE损失和熵正则化；GRPO-OR使用组相对优势（G=2），不训练价值函数，额外加入对参考策略的对数比率惩罚 \(\beta L_{\text{ref}}\)。
  - **关键性质**：连续一阶导数（Proposition 1）；样本级更新方向由优势符号决定（Proposition 2）；对优势的缩放不变（仅用符号）。
  - **与PPO裁剪的区别**：OR在log-ratio空间中使用平方单侧边际，而非ratio空间的截断最小值；不使用优势幅度；饱和边界更光滑。

### 3. 实验设计：数据集、场景、benchmark、对比方法

- **数据集**：Anthropic **hh-rlhf**（用于训练奖励模型和策略优化）。奖励模型使用Bradley-Terry损失在8000个偏好对上训练（60-61%准确率）。策略优化使用该数据集中的“human turn”作为prompt。
- **场景**：LLM后训练（RLHF阶段），使用相同的冻结奖励模型评估训练时的奖励得分。
- **对比方法**：
  - 在**GAE家族**内：**PPO-clip**（标准PPO截断目标，\(\epsilon=0.2\)） vs **PPO-OR**（\(\alpha_{\text{OR}}=0.2\)，与截断边界略有不对称）。
  - 在**组相对优势家族**内：**GRPO**（标准GRPO截断目标） vs **GRPO-OR**（OR损失）。两组共享相同的优势估计方式、价值函数有无、响应长度等。
- **模型**：策略为Llama-3.2-1B-Instruct，使用LoRA（r=16, α=32）更新约400万参数。参考策略为冻结的相同模型。奖励模型为冻结的4-bit量化Llama-3.2-1B加上可训练MLP头（约100万参数）。

### 4. 资源与算力

- **GPU**：单块NVIDIA GeForce RTX 4060 Ti，**16.7 GB VRAM**。
- **训练时长**（每个方法的一个代表性种子）：
  - PPO-OR：约**22分钟**（每步1.3秒）
  - PPO-clip：约**42分钟**（每步4.4秒）
  - GRPO：约**77分钟**（每步8.9秒）
  - GRPO-OR：约**49分钟**（每步4.6秒）
- **内存占用**：GAE方法约5.5–6 GB；组相对方法约4 GB（因CPU卸载奖励模型骨干网络和更短响应长度）。论文指出GRPO组大小\(G=2\)是硬件约束下的最大可行设置。
- **精度**：策略使用bfloat16，参考和奖励模型使用4-bit NormalFloat（通过BitsAndBytes）。

### 5. 实验数量与充分性

- **实验数量**：
  - 每组**3个随机种子**（42,43,44），共4种方法 × 3种子 = 12次运行。
  - 无跨数据集验证（仅hh-rlhf），无更大模型或更大组大小（G>2）实验。
  - 未进行超参数敏感性分析（如OR边际值、优势归一化方式、学习率等）。
- **充分性评价**：
  - **客观性**：共享奖励模型、相同训练流程，确保组内比较公平。作者明确指出**跨家族比较（GAE vs 组相对）不可直接解释**，因为响应长度、采样方式、价值函数使用等不同。
  - **不足**：3个种子统计效力低，无法做显著性检验；仅一个数据集和一个基座模型（1B参数）；组相对只用了G=2，限制了GRPO典型设置；奖励模型准确率仅60-61%，噪声大；未在独立的人类偏好评估上验证，仅报告训练时奖励得分。
  - 实验设计合理但**覆盖窄**，论文自己也承认结果仅刻画该特定设置，而非普遍结论。

### 6. 论文的主要结论与发现

- **GAE家族内**：PPO-OR的平均最终奖励得分（0.500）比PPO-clip（0.195）**高0.305**，但跨种子标准差更大（±0.158 vs ±0.110）。即OR带来更高均值但更分散。
- **组相对家族内**：GRPO-OR的平均得分（0.457）略低于GRPO（0.488），但标准差更小（±0.027 vs ±0.080）。GRPO-OR的OR残差接近零，过冲比例下降，而GRPO的截断目标轨迹大且波动。
- **策略漂移**（rollout到当前策略的对数比率位移）：
  - GAE方法保持在~0.3–0.5。
  - 组相对方法（GRPO和GRPO-OR）均高达~5–13，**OR未减少该位移**——即局部平滑不能控制全局策略偏移。
- **核心矛盾**：OR改变了优化行为，但效果高度依赖于优势估计框架；它**不是信任区域机制**，不能替代价值函数、参考正则化等组件。

### 7. 优点

- **理论贡献**：形式化了OR在策略优化中的单侧平滑饱和性质，证明其一阶连续性，并揭示了样本级更新方向由优势符号决定。
- **诊断视角**：引入**过冲比例**（overshoot fraction）和**OR能量**（目标平方幅度）等可解释指标，帮助理解优化动态（如OR何时激活）。
- **实验严谨性**：在匹配比较中固定所有其他成分（优势估计、优化器、响应长度等），确保为公平比较损失函数本身的效果。
- **开源代码**：提供GitHub仓库，增强可复现性。
- **提出开放问题**：清晰指出策略漂移未解决、大小组差异未验证等，引导后续工作。

### 8. 不足与局限

- **统计力度弱**：仅3个种子，不能保证结果的可重复性或显著性。
- **模型与数据集单一**：仅使用Llama-3.2-1B和hh-rlhf，无法推广到更大模型或其他任务。
- **组大小受限**：G=2是因为内存限制，对应原始GRPO的大组设置，结论外推需谨慎。
- **奖励模型质量低**：60-61%准确率说明偏好信号噪声大，可能干扰方法比较。
- **超参数未调优**：PPO-OR与PPO-clip使用相同的名义α=0.2，但实际边界不对称（log-ratio空间），也未比较不同OR边际。
- **无独立人类评估**：得分来自训练时奖励模型，可能过估计。
- **优势符号仅用符号**：忽略了幅度信息，可能丢失重要梯度权重（论文提出加权变体但未实验）。
- **未解决策略漂移问题**：组相对方法下OR并不能减少位移，论文承认这是主要未解决问题。
- **跨家族比较不可比**：GAE与组相对在响应长度、采样次数、CPU卸载等方面有差异，无法分离优势估计和损失函数的效果。

（完）
