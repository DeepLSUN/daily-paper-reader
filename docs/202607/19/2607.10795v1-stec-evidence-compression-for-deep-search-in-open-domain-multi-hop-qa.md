---
title: "STEC: Evidence Compression for Deep Search in Open-domain Multi-Hop QA"
title_zh: STEC：面向开放域多跳问答的深度搜索证据压缩
authors: "Xinkang Li, Rong Jiang, Xin Song, Ye Wang, Yue Han, Changjian Li"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10795v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于LLM的搜索代理用于多跳问答
tldr: 开放域多跳问答中，基于LLM的搜索代理通过多条搜索轨迹寻找答案，但直接比较原始轨迹受噪声和异构信息干扰，比较答案字符串则忽略证据，导致最终答案选择困难。为此，提出STEC证据压缩框架，将同一答案的轨迹分组压缩为候选级证据表示，并通过证据引导验证进行比较选优。在四个基准上，STEC取得最佳整体性能，消融实验证实答案级证据压缩的关键贡献。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1118, \"height\": 411, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1266, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1271, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 754, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1273, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1274, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10795v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1274, \"height\": 478, \"label\": \"Table\"}]"
motivation: 多跳问答中多条搜索轨迹导致最终答案选择困难，直接比较原始轨迹受噪声和异构信息干扰，比较答案字符串忽略证据支持。
method: 提出STEC框架，包含答案级证据压缩（按答案分组轨迹并转换为候选证据表示）和证据引导答案验证（比较表示选出最终答案）。
result: 在四个开放域多跳问答基准上，STEC整体表现优于所有对比方法，消融实验证明答案级证据压缩的有效性。
conclusion: STEC通过将原始轨迹比较转为候选级证据比较，有效解决了多跳问答中多轨迹答案选择的噪声和异构问题。
---

## 摘要
在开放域多跳问答中，基于大语言模型的搜索代理通过将检索与推理相结合，为知识密集型问答提供了一种有前景的方法。现有方法主要通过推理范式、检索交互和搜索策略优化来改进开放域多跳问答。然而，使用多条搜索轨迹带来了具有挑战性的最终答案选择问题。不同轨迹可能支持不同的候选答案，而检索到的信息可能异构、冗余、不完整或相互冲突。直接比较原始轨迹会使验证器暴露于嘈杂且不对齐的内容中，而比较答案字符串则忽略了支持每个候选答案的证据，使得可靠的最终选择变得困难。为解决这一挑战，我们提出STEC，一种用于多跳问答最终答案选择的证据压缩框架。STEC通过两种机制从现有候选集中选择最终答案：（1）答案级证据压缩，将轨迹按归一化的答案标识分组，并将每个答案组转换为针对候选答案的证据表示；（2）证据引导的答案验证，比较这些表示并从候选集中选择最终答案。该设计将最终选择从原始轨迹比较转变为候选级证据比较。我们在四个开放域多跳问答基准上评估了STEC，并与代表性基线进行了比较。实验结果表明，STEC在比较方法中总体表现最佳，消融实验结果也证明了答案级证据压缩对最终答案选择有贡献。

## Abstract
In open-domain multi-hop question answering (QA), LLM-based search agents offer a promising approach to knowledge-intensive QA by combining retrieval with reasoning. Existing methods mainly improve open-domain multi-hop QA through reasoning paradigms, retrieval interaction, and search strategy optimization. However, using multiple search trajectories introduces a challenging final answer selection problem. Different trajectories may support different candidates, and the retrieved information can be heterogeneous, redundant, incomplete, or conflicting. Directly comparing raw trajectories exposes the verifier to noisy and unaligned content, while comparing answer strings ignores the evidence supporting each candidate, making reliable final selection difficult. To address this challenge, we propose STEC, an evidence compression framework for final answer selection in multi-hop QA. STEC selects the final answer from the existing candidate set through two mechanisms: (1) Answer-Level Evidence Compression, which groups trajectories by normalized answer identity and converts each answer group into a candidate-specific evidence representation; and (2) Evidence-Guided Answer Verification, which compares these representations and selects the final answer from the candidate set. The design shifts final selection from raw trajectory comparison to candidate-level evidence comparison. We evaluate STEC on four open-domain multi-hop QA benchmarks against representative baselines. Experimental results show that STEC performs best overall among the compared methods, and ablation results provide evidence that answer-level evidence compression contributes to final answer selection.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：开放域多跳问答（Multi-Hop QA）要求系统从大规模开放语料中检索信息，并通过多步推理整合信息来回答复杂问题。传统方法如单次检索-生成管道（RAG）无法根据推理状态动态更新检索目标，容易因初始检索缺失或噪声导致推理错误。
- **核心问题**：近期深度搜索（Deep Search）方法（如Search-o1、Search-R1）允许模型在推理过程中交替进行检索和推理，生成包含搜索查询、中间推理和候选答案的**搜索轨迹**。但使用多条轨迹后，最终答案选择面临挑战：不同轨迹可能支持不同候选答案，提供的证据异构、冗余、不完备甚至冲突。直接比较原始轨迹会使验证器受到噪声和不对齐内容的影响；仅比较答案字符串则会忽略证据支持，导致难以可靠选择正确答案。
- **整体含义**：本文提出**STEC（Evidence Compression）** 框架，旨在将最终答案选择从原始轨迹比较转变为**候选级证据比较**，通过先压缩证据再验证的方式，提升开放域多跳QA的最终答案选择准确性。

## 2. 方法论

- **核心思想**：STEC将多路径搜索后的最终答案选择形式化为**候选中心的证据比较问题**。分为三个阶段：
  1. **多轨迹搜索（Multi-Trajectory Search）**：对同一问题生成N条搜索轨迹，每条轨迹包含检索过程、中间推理和原始候选答案。
  2. **答案级证据压缩（Answer-Level Evidence Compression）**：
     - 对每个原始候选答案执行保守归一化（去除大小写、标点、冗余空格等），得到归一化答案标识。
     - 按归一化标识将轨迹分组，每个候选答案对应一个轨迹组。
     - 对每个组独立压缩，生成候选专用证据表示 \( C_a = (V_a, E_a^+, E_a^-, P_a, \Gamma_a) \)，其中：
       - \(V_a\)：原始答案变体集合；
       - \(E_a^+\)：支持候选答案的证据单元；
       - \(E_a^-\)：矛盾或削弱支持的证据单元；
       - \(P_a\)：从支持证据到候选答案的简洁推理路径；
       - \(\Gamma_a\)：组级统计（如轨迹数量）。
  3. **证据引导的答案验证（Evidence-Guided Answer Verification）**：验证器比较各候选证据表示，从现有候选集中选择最终答案 \( \hat{a} = \text{Verifier}(q, C(q)) \)。决策依据是支持证据的覆盖度、推理路径的连贯性、矛盾证据的存在等，组统计仅作辅助信号。最终将归一化答案映射回原始答案字符串。

- **关键技术细节**：
  - 归一化采用保守策略，减少错误合并含义不同的答案。
  - 压缩过程独立于每个答案组，避免跨组证据混入。
  - 验证器输入为结构化字段，使不同候选可在相同粒度比较，降低了对重复、噪声内容的敏感度。

## 3. 实验设计

- **数据集**：四个开放域多跳QA基准：
  - **HotpotQA**（dev, 7,405 例）：多跳问题，含句子级支持事实。
  - **2WikiMultihopQA**（dev, 12,576 例）：结合结构化与非结构化来源。
  - **MuSiQue**（dev, 2,417 例）：由连接的单跳问题复合而成。
  - **Bamboogle**（test, 125 例）：人工构建的合成问题。
- **评估指标**：精确匹配（Exact Match, EM），并报告宏平均EM。
- **对比基线**：
  - 无检索：Direct Inference, Chain-of-Thought (CoT)
  - 带检索：RAG, IRCoT
  - 微调方法：SFT, R1-base, R1-instruct, Rejection Sampling（使用搜索引擎）
  - 搜索增强推理：Search-o1, Search-R1
- **实现细节**：所有方法共用相同检索器、知识语料、检索文档数及预训练骨干模型（Qwen2.5-7B，除非另有说明）。STEC默认使用Search-R1生成的N=8条轨迹。
- **实验场景覆盖**：主要结果表（表3）、消融实验（表4, 去掉压缩模块的Verifier Only）、超参数分析（N=2,4,8，图3及表5）、模型配置分析（表6，不同模型大小3B/7B及指令微调类型）。

## 4. 资源与算力

- **文中未明确说明**：论文未提及具体的GPU型号、数量、训练时长或推理时间等算力资源信息。仅提到使用Qwen2.5-7B及3B/7B模型进行实验，所有方法在同一硬件设置下比较以保证公平性，但未公开算力细节。

## 5. 实验数量与充分性

- **实验数量**：
  - **主实验**：在4个数据集上与9个基线对比，报告EM。
  - **消融实验**：1组（Verifier Only vs STEC），展示压缩模块贡献。
  - **超参数分析**：3种N值（2,4,8），在每个数据集上对比Verifier Only和STEC。
  - **模型分析**：4种模型配置（3B Base/Instruct, 7B Base/Instruct），各在4个数据集上对比，共16组结果。
  - 总计超过30组EM分数报告。
- **充分性与公平性**：
  - **充分**：覆盖了主要的基线类别和方法变体，包括无检索、带检索、微调、搜索增强，以及不同模型大小和指令调优类型。
  - **公平**：宣称保持检索器、知识语料、检索文档数、骨干模型一致；所有方法使用相同评估集和指标。
  - **客观**：消融实验控制变量，超参数分析展示趋势，模型分析验证泛化性。但没有进行统计显著性检验，可能影响结论的严谨性。

## 6. 论文的主要结论与发现

1. **STEC整体表现最佳**：在四个数据集上STEC均取得最高EM，宏平均EM为0.347，超过所有基线（最佳基线Search-R1为0.301，提升4.6个百分点）。
2. **答案级证据压缩有效**：消融实验显示，去掉压缩模块后平均EM从0.347降至0.339，压缩模块带来正向贡献（尤其在HotpotQA、2Wiki、Bamboogle上）。
3. **增加轨迹数通常有益**：N从2增加到8，STEC和Verifier Only的EM均上升，但STEC在N≥4时改善更明显，表明压缩模块能更好利用多轨迹证据。
4. **STEC对不同模型配置鲁棒**：在3B/7B Base/Instruct四种配置下，STEC均优于Verifier Only，且指令微调模型效果更好，证明了方法的通用性。

## 7. 优点

- **创新性**：首次将多轨迹最终的答案选择问题明确定义为候选中心的证据比较，并设计专用压缩机制，思路清晰。
- **结构化和模块化**：三个阶段的框架设计（搜索→压缩→验证）解耦了证据获取与答案选择，便于独立优化。
- **减少噪声影响**：通过归一化分组和结构化表示，避免验证器直接处理异构、冗余和冲突的原始轨迹，提升了鲁棒性。
- **全面的实验验证**：在多个数据集、多种基线、不同模型大小和轨迹数下进行对比，消融实验和超参数分析充分。
- **无需额外训练**：压缩和验证过程均基于现有模型（训练无关），易于部署。

## 8. 不足与局限

- **资源算力未说明**：缺少计算资源和推理效率的详细分析，难以评估实际应用中的成本。
- **未统计显著性检验**：论文报告了EM分数但未提供p值或置信区间，无法判断改善是否统计显著。
- **归一化策略保守**：仅做简单表面归一化，可能无法处理同义词或语义等价但表述不同的答案，导致正确答案被误分组。
- **依赖搜索轨迹质量**：STEC的性能受限于搜索生成器（如Search-R1）的质量；若搜索过程本身很差，压缩也难有明显效果。
- **应用限制**：当前仅在英文开放域多跳QA上评估，未验证在其他领域（如医学、法律）或跨语言场景的泛化能力。
- **压缩开销**：对每个答案组独立压缩和构建推理路径可能引入额外延迟，特别是在候选数较多时。
- **实验覆盖有限**：未对比其他验证框架（如基于投票、一致性检查等）；消融只比较了有无压缩模块，未分析压缩各子成分的贡献。

## 9. 未来工作方向

- **提升归一化策略**：引入语义级别的归一化（如同义词替换、指代消解、实体链接），使证据分组更准确地反映语义等价关系，减少因表面形式差异导致的错误分组。
- **动态轨迹选择**：当前使用固定数量的轨迹（N=8），未来可设计自适应选择机制，根据问题复杂度或推理不确定性动态调整搜索轨迹数，平衡效率与性能。
- **验证器轻量化**：探索更高效的验证架构（如基于交叉注意力或图神经网络的证据聚合），降低压缩和验证的计算开销，适应实时应用场景。
- **跨领域与多语言扩展**：在医学、法律等专业领域以及中文、多语言环境上测试STEC，验证其泛化性和可迁移性。
- **与搜索过程联合优化**：将证据压缩与搜索策略（如Reward-based搜索、Beam Search）结合，形成端到端的搜索-压缩-验证闭环，提升整体效率。

## 10. 个人评价与总结

本文针对多跳QA中多轨迹搜索后答案选择这一被忽视但关键的环节，提出了简洁而有效的STEC框架。其核心贡献在于将复杂的轨迹比较转化为结构化的候选级证据比较，通过归一化分组、独立压缩和基于证据的验证，显著提升了最终答案的准确性。实验设计较为充分，在多个数据集上与多种基线对比，并进行了充分的消融和超参数分析，结论可靠。方法的模块化设计使其易于集成到现有检索-推理流水线中。不足之处在于缺乏效率分析和统计显著性检验，归一化策略较为粗糙，且在泛化性验证上仍有空间。总体而言，STEC为开放域多跳QA的答案集成提供了一种新颖且实用的范式，对后续研究具有启发意义。

（完）
