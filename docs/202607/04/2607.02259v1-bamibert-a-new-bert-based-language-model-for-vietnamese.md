---
title: "BamiBERT: A New BERT-based Language Model for Vietnamese"
title_zh: BamiBERT：一种新的基于BERT的越南语语言模型
authors: "Dat Quoc Nguyen, Thinh Pham, Chi Tran, Linh The Nguyen"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02259v1"
tags: ["query:llm-reports"]
score: 9.0
evidence: 介绍BamiBERT，一个基于BERT的越南语语言模型及其训练细节
tldr: 现有越南语BERT模型PhoBERT存在输入长度限制和依赖分词等局限。BamiBERT直接从原始文本训练，支持最长2048个token，无需外部分词。在8个越南语基准的15个指标中，取得11个最优和3个次优，达到新的SOTA。该模型开源，为越南语NLP提供更强的编码器。
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02259v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1623, \"height\": 792, \"label\": \"Table\"}]"
motivation: 克服PhoBERT的输入长度和分词依赖，提升越南语表示能力。
method: 在129GB越南语文语料上从零训练BERT，支持原生2048 token输入。
result: 在15个指标中11个第一、3个第二，刷新base级越南语编码器SOTA。
conclusion: BamiBERT作为更强越南语编码器，开源促进研究与应用。
---

## 摘要
在本文中，我们介绍了BamiBERT，一种新的基于BERT的越南语预训练语言模型，它解决了当前越南语文本编码器事实标准PhoBERT的主要局限性。BamiBERT在129GB通用越南语文本语料库上从头训练了20个epoch，支持最多2048个标记的扩展上下文长度，并直接对原始输入进行操作，无需外部分词。在8个越南语基准测试中，它在15个指标中的11个上取得了最佳成绩，并在另外3个上取得了第二佳成绩，在"base"规模的越南语编码器中树立了新的最先进水平，并展示了强大的跨领域泛化能力。我们在以下网址发布BamiBERT：https://huggingface.co/Qualcomm-AI-Research/BamiBERT

## Abstract
In this paper, we introduce BamiBERT, a new BERT-based pre-trained language model for Vietnamese that addresses key limitations of PhoBERT -- the current de facto Vietnamese text encoder. Trained from scratch on a 129GB corpus of general-domain Vietnamese text for 20 epochs, BamiBERT supports an extended context length of up to 2048 tokens and operates directly on raw input, eliminating the need for external word segmentation. Across 8 Vietnamese benchmarks, it achieves the best score on 11 of 15 metrics and the second-best on 3 others, setting a new state of the art among "base"-sized Vietnamese encoders and demonstrating strong cross-domain generalization. We release BamiBERT at: https://huggingface.co/Qualcomm-AI-Research/BamiBERT

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **现有局限**：PhoBERT 是当前越南语 NLP 的事实标准编码器，但其存在两个关键缺陷：最大上下文长度仅 256 子词 token，且需要依赖外部分词工具（如 RDRSegmenter）对输入进行词分割后才能使用。这限制了其处理长文本的能力，并增加了集成复杂度。
- **研究动机**：为克服这些局限，开发一种新的越南语单语 BERT 模型，支持更长的上下文（2048 tokens）并直接处理原始文本，无需预分词，从而提升灵活性和下游任务性能。
- **整体意义**：BamiBERT 填补了越南语编码器在长上下文和易用性方面的空白，在 8 个基准测试上取得 11/15 个指标的最优（SOTA），成为 base 规模越南语编码器的新标杆。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：从零开始预训练一个 BERT-base 架构的越南语文本编码器，采用 RoBERTa 的预训练策略（动态掩码、无下一句预测），并使用扩展的越南语专用 BPE 分词器，直接在原始文本上训练，支持 2048 token 的最大序列长度。
- **关键技术细节**：
  - **架构**：12 层 Transformer 块，隐藏层大小 768，12 注意力头（标准 BERT-base）。
  - **分词器**：基于 PhoGPT 的越南语字节级 BPE 分词器，额外添加 `<mask>` 词符，最终词表大小 20481。
  - **预训练目标**：仅使用掩码语言建模（MLM），动态掩码策略。
  - **预训练数据**：129GB 未压缩的通用领域越南语文本，清洗后使用。
  - **优化器**：Adam，峰值学习率 0.00015，批次大小 1024（序列块），预热 2 个 epoch，总训练 20 个 epoch。

## 3. 实验设计

- **使用的数据集/场景**：
  - 共 8 个越南语基准数据集，涵盖多种任务：
    - **ViNLI**：自然语言推理（4 标签分类）
    - **PhoNER_COVID19**：COVID-19 命名实体识别
    - **UIT-VSFC（情感）** 和 **UIT-VSFC（主题）**：学生反馈的情感/主题分类
    - **ViSpamReviews**：电商垃圾评论检测
    - **UIT-ViSFD**：智能手机电商的方面级情感分析（检测和分类）
    - **UIT-ABSA（酒店）** 和 **UIT-ABSA（餐厅）**：酒店和餐厅领域的方面级情感分析（检测和分类）
- **Benchmark 对比方法**：
  - **BamiBERT**（本文，直接处理原始文本）
  - 对比基线：ViDeBERTa-base、ViSoBERT（原始文本输入）、XLM-RoBERTa-base（多语言）、PhoBERT-base（需分词）。其中 ViDeBERTa 和 PhoBERT 使用 RDRSegmenter 进行分词。
- **任务类型**：自然语言推理、命名实体识别、情感分类、主题分类、垃圾检测、方面检测、方面情感分类。共 15 个评估指标（部分数据集有多个指标）。

## 4. 资源与算力

- **明确说明**：预训练使用 8 块 NVIDIA A100 GPU（每块 40GB 显存），批次大小 1024 序列块，训练 20 个 epoch。初始 2 个 epoch 用于学习率预热。
- **微调资源**：所有模型微调使用单个 A100 GPU（或类似）进行网格搜索学习率，训练 30 个 epoch，未说明具体 GPU 数量（通常单卡即可）。

## 5. 实验数量与充分性

- **实验数量**：在 8 个数据集上进行了 15 个指标的对比实验，并报告了完整结果（表 2）。未进行消融实验（例如不同分词方式、不同上下文长度的影响等）。
- **充分性**：
  - 对比了 4 种有代表性的基线（包括多语言、单语、领域特化模型），覆盖了主流越南语编码器。
  - 评估任务多样化（多样分类、序列标注、推理），能够反映模型的多方面能力。
  - 但缺乏对长上下文场景的直接评估（例如将输入长度设为 512 或 2048 的对比），也未测试超过 512 token 任务时的性能表现。
  - 未进行训练策略消融（如分词器、训练数据规模、预训练 epoch 数的影响）。
- **客观公平性**：
  - 所有实验使用相同的微调流程（AdamW、批次 32、网格搜索学习率、30 epoch、早停选验证集最佳）、相同评估脚本，结果可靠。
  - 但基线模型的最佳超参数可能未经细致调优（仅对 BamiBERT 进行了网格搜索），且 PhoBERT 等模型的结果部分取自前人论文（用 † 标注），可能引入微小差异。

## 6. 论文的主要结论与发现

- **性能领先**：BamiBERT 在 15 个指标中取得 11 个第一、3 个第二、1 个第三，明显优于所有基线，尤其在 ViNLI 上超出 PhoBERT 3 个点以上。
- **跨域泛化强**：在通用域（ViNLI、PhoNER_COVID19）和社交域（UIT-VSFC、ViSpamReviews、UIT-ABSA 等）均表现优异，甚至超越社交域特化模型 ViSoBERT。
- **检测优于分类**：在方面级情感分析中，检测任务（span 定位）的提升更显著，分类任务在某些领域（酒店）略逊于 ViSoBERT，说明 BamiBERT 擅长边界敏感特征。
- **易用性提升**：无需外部分词，支持 2048 token 上下文，简化了集成流程。

## 7. 优点

- **方法创新**：直接针对 PhoBERT 的局限设计，训练数据更大（129GB vs 20GB）、上下文更长、无需分词，实际改进了越南语编码器的可用性。
- **全面评测**：在 8 个不同任务和领域的基准上严格对比，包括最新基线 ViSoBERT 和通用多语言模型，结果可信度高。
- **开源释放**：模型（含训练脚本）在 HuggingFace 上公开发布，促进后续研究。
- **工程细节清晰**：预训练超参数、数据规模、训练轮数等均有明确说明，易于复现。

## 8. 不足与局限

- **缺少消融实验**：未分析分词器设计、训练数据规模、上下文长度、预训练 epoch 等对性能的具体贡献，因此 model 的改进来源不明确。
- **长上下文评估缺失**：虽支持 2048 token，但所有下游任务的最大输入均未超过 512（基线模型也都是 512），没有在真正的长文本任务上验证性能。
- **基线微调可能不完全公平**：部分基线结果取自前文（† 标记），可能与原文微调设置略有不同；ViSoBERT 和 XLM-RoBERTa 等模型的超参数可能未针对当前数据集进行充分搜索。
- **计算资源未完整披露**：只提到了预训练使用 8*A100，但训练一个 epoch 的耗时、总训练时间等未说明；微调成本也仅粗略提及。
- **领域覆盖有限**：仅涵盖通用和社交领域，未在法律、医疗等专业领域测试。
- **对分类任务提升不均衡**：在酒店 ABSA 分类任务上 BamiBERT 甚至低于 ViSoBERT，说明在特定细粒度情感强调上仍有不足，可能存在领域偏差。

（完）
