---
title: "Linguistic Firewall: Geometry as Defense in Multi-Agent Systems Routing"
title_zh: 语言防火墙：多智能体系统路由中的几何防御
authors: "Dvir Alsheich, Adar Peleg, Ben Hagag, Rom Himelstein, Amit Levi, Avi Mendelson"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30555v1"
tags: ["query:agent"]
score: 9.0
evidence: 多智能体系统路由安全
tldr: "多智能体系统的路由机制常依赖代理的文本描述或嵌入表示，易被恶意代理伪装或植入后门。本文提出ANTAP架构，通过主动能力测试将代理性能转化为固定行为算子，并在共享语义空间中进行非文本代数投影路由，形成语言防火墙。实验表明，ANTAP对描述注入攻击ASR接近零，对抗自适应嵌入攻击ASR降低20%，有效防御元数据攻击。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30555v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1777, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30555v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 720, \"height\": 541, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30555v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1742, \"height\": 1097, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.30555v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1366, \"height\": 478, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30555v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 1002, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30555v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 862, \"height\": 801, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30555v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1048, \"height\": 816, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30555v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 845, \"height\": 169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.30555v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 981, \"height\": 712, \"label\": \"Table\"}]"
motivation: 现有路由机制依赖非经验代理（如自我描述或静态表示），存在安全漏洞，恶意代理可轻易伪装能力或隐藏后门。
method: 提出ANTAP，通过主动查询代理进行能力测试，将性能转化为固定行为算子，在共享语义空间中进行非文本代数投影路由。
result: "对描述注入攻击ASR接近零，对抗自适应嵌入攻击ASR降低20%，且对描述操纵具有设计弹性。"
conclusion: ANTAP通过非文本路由建立语言防火墙，有效防御元数据攻击，提升多智能体系统安全性。
---

## 摘要
大型语言模型（LLM）的快速集成推动了多智能体系统（MAS）的发展，其中专业化智能体协作执行复杂工作流。这些环境中的有效编排需要强大的路由机制，将任务高效分配给最合适的智能体。然而，现有路由器从根本上依赖未经验证的代理（从文本自描述到静态替代表示）来衡量智能体的能力。这种对非经验数据的依赖造成了智能体预期画像与其实际操作能力之间的关键差距，引入了严重的安全漏洞。恶意智能体可以轻易误报其能力或隐藏后门，这些后门可以逃避标准外部分析和静态表示学习技术。在这项工作中，我们引入了ANTAP（自动非文本智能体选择器），这是一种评估驱动的路由架构，它摒弃间接代理，转而采用主动能力测试。通过动态查询智能体以经验性地确定其真实能力，ANTAP将性能提炼为共享语义空间内的固定行为算子。在推理时，通过纯粹的非文本代数投影执行路由，建立了一个"语言防火墙"，使得基于元数据的攻击无法表达。在我们的实验中，ANTAP对基于描述的注入攻击实现了近乎零的攻击成功率（ASR），而基于描述的路由基线则达到67.3%及以上。针对自适应嵌入攻击，ANTAP的ASR显著低于基于嵌入的基线，降低了20%，同时其设计上对描述操纵保持鲁棒性。

## Abstract
The rapid integration of Large Language Models (LLMs) has driven the evolution of Multi-Agent Systems (MAS), where specialized agents collaborate to execute complex workflows. Effective orchestration in these environments requires robust routing mechanisms to efficiently allocate tasks to the most suitable agent. However, existing routers fundamentally rely on unverified proxies, ranging from textual self-descriptions to static surrogate representations, to gauge an agent's competence. This reliance on non-empirical data creates a critical gap between an agent's projected profile and its actual operational capabilities, introducing severe security vulnerabilities. Malicious agents can easily misrepresent their proficiencies or harbor covert backdoors that evade both standard external analysis and static representation-learning techniques. In this work, we introduce ANTAP (Automatic Non-Textual Agent Picker), an evaluation-driven routing architecture that discards indirect proxies in favor of active capability testing. By dynamically querying agents to ascertain their true competencies empirically, ANTAP distills performance into fixed behavioral operators within a shared semantic space. At inference time, routing is performed via a purely non-textual algebraic projection, establishing a "linguistic firewall" that renders metadata-based attacks inexpressible. In our experiments, ANTAP achieves near-zero ASR against description-based injection attacks, compared to 67.3\% and above for the description-based router baseline. Against adaptive embedding attacks, ANTAP achieves substantially lower ASR than the embedding-based baseline, with a 20\% reduction, while remaining resilient to description manipulation by design.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将基于您提供的论文内容，对其做出结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义

*   **研究动机**：当前的多智能体系统（MAS）依赖于一个关键但脆弱的路由机制。该机制通过解析智能体的**文本自描述**（如功能介绍、提示词摘要）来分配任务。这引入了严重的安全漏洞：
    *   **路由器劫持**：恶意智能体可通过在被分析的文本描述中嵌入指令，欺骗路由器将其选为任务执行者。
    *   **后门智能体**：恶意智能体可被植入“后门”，仅在遇到特定触发词时才执行恶意行为，而平时表现正常，难以被上游路由器检测。
    *   **性能偏差**：文本描述常夸大能力或存在提示词工程优化，无法真实反映智能体的实际操作能力。
*   **整体含义**：论文旨在解决MAS中路由层的安全问题，指出当前依赖非经验性元数据（如文本描述）进行路由决策的设计模式是根本性的攻击面。

### 2. 论文提出的方法论：ANTAP

论文提出 **ANTAP (Automatic Non-Textual Agent Picker，自动非文本智能体选择器)**，一种**评估驱动**的路由架构，其核心思想是**从文本依赖转向行为几何**。

*   **核心思想**：通过主动对智能体进行**离线能力测试**，将智能体的真实性能转化为一个**固定的线性行为算子**（Behavioral Operator）。在**在线推理**阶段，路由器基于用户查询的**语义嵌入**与这些行为算子的**几何对齐情况**来选择智能体，完全不接触任何文本元数据。

*   **关键技术细节**：
    1.  **离线阶段（构建行为算子）**：
        *   **数据构建**：对每个智能体执行 `N` 个基准查询。记录其成功/失败，构建一个**性能矩阵 Y** (`K x N`，`K` 为智能体数量)。成功定义为“回答正确且**未调用未授权工具**”，映射为 `+1`；失败（包括后门触发时调用恶意工具）映射为 `-1`。这个 `-1` 标签内在地编码了对恶意行为的惩罚。
        *   **算子学习**：将基准查询嵌入到语义空间形成 `Q` 矩阵 (`N x d`)。通过求解一个**吉洪诺夫正则化最小二乘问题**，学习一个线性算子 `W*`，使其能近似预测性能矩阵 `Y`。该过程等价于计算一个正则化伪逆矩阵。
            *   数学表达：`W* = Y (QQ^T + λI)^-1 Q`，其中 `λ` 是正则化参数。
        *   **可扩展性**：注册新智能体仅需 `O(N)` 计算，即在新智能体上运行基准测试，获得其性能向量，再通过一次矩阵乘法投影到现有流形上。

    2.  **在线阶段（语言防火墙）**：
        *   **文本隔离**：在推理时，路由器**完全不摄入任何文本元数据**（如智能体描述）。
        *   **几何路由**：
            *   将用户查询 `q_new` 嵌入为向量 `z = E(q_new)`。
            *   通过一次**矩阵-向量乘法**计算所有智能体的预测效用向量：`s = W* z`。
            *   选择得分最高的智能体：`a* = argmax(s)`。
        *   **安全性**：由于不存在文本入口，任何嵌入在文本描述中的攻击指令都无法影响路由决策，形成了结构性的“语言防火墙”。

### 3. 实验设计

*   **数据集/场景**：
    *   **安全性**：MMLU, Big-Bench Hard (BBH), AgentHarm（用于测试对有害请求的拒绝能力）。
    *   **路由准确性**：与安全性评估在同一数据集上进行。
*   **Benchmark/基线方法**：
    *   **AutoGen**：基于文本描述的标准LLM路由器，代表当前主流做法。
    *   **EmbedLLM**：基于嵌入表示的学习型路由器，代表了另一种非文本但易受攻击的路由方法。
*   **攻击方法（对抗设置）**：
    *   **描述注入攻击**：在恶意智能体的描述中嵌入提示注入指令。
    *   **数据投毒攻击（DPA）**：使用预训练的LoRA适配器（源自BackdoorLLM）创建五种不同类型的后门智能体（BadNet, Sleeper, VPI等），这些智能体在特定触发词下会执行恶意工具调用。
    *   **自适应嵌入攻击**：采用贪心令牌替换算法（基于Universal Adversarial Triggers），直接优化用户查询的嵌入，使其在几何空间中偏离安全代理而靠近恶意代理。

### 4. 资源与算力

论文在“Compute and infrastructure”部分明确说明：
*   **GPU**：单张 **NVIDIA A40** (48 GB)。
*   **时长**：未给出具体的训练总时长。
*   **其他硬件**：Intel Xeon Gold 6230R CPU (104逻辑核心, 503 GB RAM)。
*   **软件**：Python 3.13.12, PyTorch 2.5.1, 本地 Ollama 用于LLM推理。
*   **分析**：算力资源属于中等水平，使用单卡即可完成所有实验，表明方法的计算开销不高。

### 5. 实验数量与充分性

*   **实验数量**：相当充分。论文围绕三个研究问题（RQ1, RQ2, RQ3）设计了多组实验。
    *   **RQ1**：比较 ANTAP 与 AutoGen、EmbedLLM 在5种不同攻击类型（1种注入+5种DPA，其中AgentHarm算独立场景）下的攻击成功率（ASR）和路由准确率，共**6个主表** (Table 1)。
    *   **RQ2**：在**5个维度**（场景、模型、提示词长度、描述长度、智能体数量）上进行了详细的消融实验（Ablation Study），每个维度下又有多种子配置（Table 2）。共**3大类、8种子配置**。
    *   **RQ3**：对抗自适应攻击的鲁棒性测试，设置了**16个攻击令牌长度**（1-16 tokens），每个长度进行**7次不同随机种子**的重复实验（Figure 3）。
*   **充分性与公平性**：
    *   **充分**：分析非常全面，不仅对比了SOTA方法，还通过消融实验探究了自身方法的边界和影响因素，并考虑了最坏情况的白盒自适应攻击。
    *   **客观公平**：基线方法设置合理（AutoGen代表了最常用的方法，EmbedLLM代表了最先进的嵌入方法）。实验设置（如校准集和测试集的分割）清晰透明，确保了公平比较。唯一的潜在偏差在于所有实验基于自建的MAS模拟环境，而非统一的、社区公认的MAS基准测试平台。

### 6. 论文的主要结论与发现

*   **ANTAP 实现了近乎零的ASR**：在抵御文本描述注入攻击方面，ANTAP 的攻击成功率（ASR）接近0%（例如0.2%），而 AutoGen 高达75.3%以上。
*   **对后门智能体攻击具有强鲁棒性**：在五种不同的数据投毒攻击下，ANTAP 的ASR均远低于基线方法（0.3% - 2.4% vs AutoGen的70%以上）。其核心原因在于，后门在离线校准阶段被“激活”，导致性能标签为 `-1`，从而被行为算子学习为负信号。
*   **对自适应嵌入攻击的鲁棒性**：即使攻击者知晓防御机制并直接优化查询嵌入，ANTAP 的ASR也显著低于 EmbedLLM，并且增加攻击令牌长度对其影响不大，表现出刚性。
*   **结构性优势**：ANTAP 的安全性优势是**结构性的**，源于其对文本的零依赖，而不是依赖启发式过滤。这种设计从根源上消除了描述注入攻击的表达空间。
*   **实用优势**：在提升安全性的同时，ANTAP 在路由准确率和**延迟**（27ms vs AutoGen的428ms）上也更具优势，因为它用轻量级几何运算取代了昂贵的LLM推理。

### 7. 优点

*   **方法论创新**：**“语言防火墙”** 的概念非常新颖且强大。它不试图修补漏洞，而是通过改变路由的接口设计来彻底消除攻击面。这种“从源头防御”的思路是安全领域的核心思想。
*   **隐式安全训练**：通过将 `-1` 标签赋予“调用未授权工具”的行为，性能矩阵 `Y` 无需显式的安全分类器就直接编码了安全约束，使得路由器能学习到对有害请求的负面信号。
*   **激励对齐**：路由决策基于真实行为而非自吹自擂，这激励智能体专注于提升实际能力，而不是优化提示词以欺骗路由器，从而提升了整体系统的可靠性。
*   **高效率和可扩展性**：在线推理仅为一次矩阵乘法，延迟极低。新增智能体的注册几乎是瞬时的，使得系统易于扩展到大规模智能体池。
*   **实验设计严谨**：全面考虑了多种攻击模式（注入、后门、自适应），并通过系统的消融实验验证了方法的可靠性和边界条件。

### 8. 不足与局限

*   **信任假设**：论文明确假设一个**可信的离线校准环境**和一个**鲁棒的语义嵌入模块**。如果校准过程被投毒（例如，校准数据本身被恶意篡改），或者嵌入模型本身存在后门，则ANTAP的安全性根基将被动摇。
*   **二元性能标签**：ANTAP 目前使用严格的等分（`+1`/`-1`）评分机制。对于需要更精细化能力评估的复杂工作流，这种简化的二值化可能不足以描述智能体的多维度能力。未来可探索连续或多维的评分。
*   **安全与精度的权衡**：在AgentHarm数据集（开放结束的有害请求）上，ANTAP的ASR仍为55%左右。虽然已显著优于基线，但这表明它在区分“有能力执行有害任务的良善智能体”和“仅能拒绝的智能体”方面仍有改进空间。论文将此归因于校准集（仅32个样本）过小，暗示在数据充足时性能可进一步提升。
*   **与应用场景的绑定**：方法性能（尤其是AgentHarm）高度依赖于校准数据的覆盖范围。论文未明确说明在分布外（OOD）场景下的鲁棒性。

（完）
