---
title: "The Remarkable Effectiveness of Providing AI Agents with Natural Language Tools: A Replication Study Validating NLT Performance Across 14 Models"
title_zh: 为AI智能体提供自然语言工具的显著效果：一项验证NLT在14个模型上性能的复制研究
authors: "Alexander Somma, Isabelle Plante, Fred Premji"
date: 2026-07-04
pdf: "https://arxiv.org/pdf/2607.03953v1"
tags: ["query:agent"]
score: 9.0
evidence: AI智能体自然语言工具的复制研究
tldr: "传统结构化工具调用在LLM agent系统中存在脆弱性。本研究独立复现并扩展了自然语言工具(NLT)框架，在14个模型、8560次试验中验证其效果。NLT将工具调用准确率从47.4%提升至62.3%，关键错误减少93%，token用量降低25.2%。模型能力越弱增益越大，而强优化前沿模型优势不显著，证明了NLT在可靠性方面的部署价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.03953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1515, \"height\": 2403, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03953v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1523, \"height\": 1142, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03953v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1526, \"height\": 1030, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03953v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1536, \"height\": 851, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03953v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1515, \"height\": 1077, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.03953v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1536, \"height\": 872, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.03953v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1716, \"height\": 884, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.03953v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 306, \"label\": \"Table\"}]"
motivation: 传统结构化工具调用复杂易错，NLT被提出但缺乏独立验证和模型能力影响分析。
method: 在14个模型上（含GPT-5、Gemini 2.5 Pro等）进行8560次对照试验，复现并扩展NLT评估。
result: "NLT整体准确率提升14.9pp，关键错误减少93%（51 vs 755），token用量降低25.2%；能力较弱模型增益+24.0~43.1pp。"
conclusion: NLT是可靠高效的替代方案，尤其适合生产系统，其优势在递归工作流中进一步放大。
---

## 摘要
本研究独立复制并扩展了Johnson等人（2025）的自然语言工具（NLT）框架，该框架质疑在大语言模型（LLM）智能体系统中使用结构化工具调用的必要性。我们在14个模型和8,560次试验中评估了NLT，在原始模型基础上增加了更新的前沿模型、推理模型和开放权重模型。结果证实了核心发现并增加了细节。NLT将工具调用准确率整体提高了14.9个百分点（62.3%对比结构化方法的47.4%），并将关键错误减少了93%（51次错误对比755次）。收益取决于模型能力：没有原生工具调用的模型、推理模型和较小模型收益显著（+24.0至+43.1个百分点），而经过高度优化的前沿模型（GPT-5、Gemini 2.5 Pro）显示出较小或反转的优势。这与最近关于强化学习优化工具使用的分析（Martinez, 2025）一致。NLT还减少了25.2%的token使用量。在递归智能体工作流中，可靠性和效率优势叠加，智能体在子智能体之间链式调用多个工具：结构化失败会触发重试、回退路由和协调开销，而NLT从源头避免了大部分成本。本文有三项贡献：（1）首次使用开源工具对NLT进行独立验证，（2）证明模型能力调节NLT优势的证据（Chen等人，2025；Zhang等人，2025），以及（3）测量了NLT的可靠性收益（错误减少93%），鉴于结构化工具调用的已知脆弱性，这是其最与部署相关的特性。NLT是结构化工具调用的实用替代方案，尤其适用于重视可靠性而非可解析性的生产系统。

## Abstract
This study independently replicates and extends the Natural Language Tools (NLT) framework of Johnson et al.~(2025), which questions the use of structured tool calling in large language model (LLM) agentic systems. We evaluated NLT across 14 models and 8,560 trials, adding newer frontier, reasoning, and open-weight models to the original set. The results confirm the core findings and add detail. NLT improves tool-calling accuracy by 14.9 percentage points overall (62.3\% versus 47.4\% structured) and reduces critical errors by 93\% (51 versus 755 errors). The gains depend on model capability: models without native tool calling, reasoning models, and smaller models gain substantially (+24.0pp to +43.1pp), while heavily optimized frontier models (GPT-5, Gemini 2.5 Pro) show smaller or reversed advantages. This matches recent analyses of reinforcement-learning-optimized tool use (Martinez, 2025). NLT also cuts token usage by 25.2\%. The reliability and efficiency advantages compound in recursive agentic workflows, where agents chain many tool calls across sub-agents: a structured failure triggers retries, fallback routing, and coordination overhead, while NLT avoids most of that cost at the source. This work makes three contributions: (1) the first independent validation of NLT using open-source tooling, (2) evidence that model capability moderates NLT's advantages (Chen et al., 2025; Zhang et al., 2025), and (3) a measurement of NLT's reliability benefit (93\% fewer errors), its most deployment-relevant property given the known fragility of structured tool calling. NLT is a practical alternative to structured tool calling, especially for production systems that value reliability over parseability.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：主流LLM智能体系统采用结构化工具调用（如JSON Schema），但该方法存在脆弱性——格式约束可能分散模型的认知资源，导致准确率下降和频繁的API错误。Johnson等人（2025）提出自然语言工具（NLT）框架，用自然语言YES/NO格式代替结构化调用，初步显示显著优势。然而该发现需要独立复制验证，且随着新模型快速迭代，需评估NLT的泛化能力。
- **整体含义**：挑战结构化工具调用作为默认范式的假设，揭示格式约束是智能体系统性能的潜在瓶颈。NLT提供更可靠、更高效的工具调用替代方案，尤其适合生产环境。

## 2. 方法论

- **核心思想**：用自然语言指令替代JSON等结构化格式进行工具选择——模型只需回复“YES/NO”判断是否需要调用每个工具，无需生成符合预定义Schema的JSON对象。
- **关键技术细节**：
  - **场景定义**：两个场景（客服Alex、心理健康Sage），每个场景含8个工具（如Alex: 回顾对话、网站信息、社交媒体、折扣、活动列表、历史购买、联系人类、评估完成；Sage: 最近对话、心理测验、网站信息、技术信息、公司信息、社交媒体、结束对话、安全呼叫、评估完成）。
  - **方法对比**：
    - NLT：在系统提示中以自然语言列出工具，要求模型按“Tool名称 – YES/NO”格式输出；通过正则表达式解析。
    - Structured：使用OpenAI兼容的function calling schema传递工具定义，模型以JSON形式输出工具调用。
  - **评价指标**：
    - 准确率（Exact Match）：预测工具集与期望工具集完全匹配。
    - 纠正准确率：若某条件下错误数≥70/80（即绝大多数试验失败），将其有效准确率设为0%（避免生存偏差）。
    - 错误率：API错误或解析失败的试验比例。
    - Token用量：每轮试验的输入+输出token总数。
    - 方差：多次重复间的精度变异。
  - **API客户端**：纯Python实现（urllib），无外部依赖；对不可用API自动重试。

## 3. 实验设计

- **场景与数据集**：
  - 场景1：Alex（客服音乐场地“Yes! Music”的预订对话）。
  - 场景2：Sage（心理健康场景的对话）。
  - 每个场景包含16个模拟用户输入，均来自原论文。
- **实验变量**：
  - 方法：NLT vs Structured
  - 场景：Alex vs Sage
  - 扰动：非扰动 vs 扰动（语义等价但措辞不同的系统提示）
  - 重复次数：每个条件5次独立重复。
- **Benchmark与对比方法**：
  - 基准：Johnson et al. (2025) 原始结果（10个模型，6400次试验，+18.4pp增益）。
  - 对比方法：结构化工具调用（行业标准function calling）。
- **模型选择**：
  - 14个模型，包括前沿闭源模型（GPT-5, Claude Sonnet 4, Gemini 2.5 Pro）、推理模型（DeepSeek-R1）、中档模型（DeepSeek-V3, Gemini Flash, GPT-5-nano）、小型开放模型（Llama 3.1 8B, Mistral 7B）、开放权重模型（GPT-OSS, Qwen3-VL）。
  - 12个模型具有完整数据（640个试验/模型）；2个模型因API限制数据不完整（Gemini 2.5 Pro缺失2个条件，Qwen3-VL缺失3个条件）。
- **总试验次数**：8560次（107个聚合条目）。

## 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量或训练时长。所有实验均为API调用，不涉及模型训练或本地推理资源。
- 使用多家API提供商（OpenRouter、原生API等），参数统一设为temperature=1.0, top_p=1.0。
- 开源评估工具基于纯Python（urllib），无需外部依赖，可重复低成本运行。
- 因此，无法评估算力成本，但属于推理阶段评估，算力消耗较低。

## 5. 实验数量与充分性

- **数量**：8,560次试验，覆盖14个模型、2个场景、2种方法、2种扰动、每输入5次重复。实验规模较大（原始研究为6,400次试验，10个模型）。
- **充分性**：
  - **积极方面**：独立实现，开源代码，包含生存偏差纠正；进行了扰动鲁棒性测试和域对比；多数模型数据完整（12/14）。
  - **不足**：
    - 每个条件只有5次重复，统计功效有限（小效应量难以检测）。
    - 仅测试单轮、无参数工具选择，未覆盖多轮、参数化工具。
    - 仅两个场景（客服和心理健康），泛化性有限。
    - 部分模型数据不完整，可能影响结论鲁棒性。
    - 方差分析因结构化高失败率而混杂，难以直接比较。
  - **公平性**：使用相同的输入、参数、评估指标，避免偏袒任一方法；纠正生存偏差是合理且必要的。

## 6. 主要结论与发现

- **核心复制结果**：
  - NLT整体准确率62.3%，结构化47.4%（纠正后），增益+14.9pp（原研究+18.4pp，部分差异来自前沿模型）。
  - NLT关键错误减少93%（51 vs 755），token用量降低25.2%（3,384,196 vs 4,522,651 tokens）。
- **能力依赖模式（新发现）**：
  - 无原生工具调用模型：收益最大（Mistral 7B: +39.4pp，Qwen3-VL: +33.8pp）——结构化完全失败。
  - 推理模型（DeepSeek-R1）：+24.0pp，NLT自然适应链式推理。
  - 中档模型（DeepSeek-V3, GPT-5-nano等）：+10~20pp。
  - 前沿模型（GPT-5）：+1.6pp，近趋同；Gemini 2.5 Pro反转（-33.7pp，结构化更强）。
  - 小型开放模型（Llama 3.1 8B等）收益高于大型模型。
- **可靠性压倒可解析性**：即使准确率趋同，NLT的错误率始终更低，这对生产系统至关重要。
- **递归智能体工作流中优势放大**：每次失败触发重试、回退路由和协调开销，NLT从源头避免大部分成本。
- **扰动鲁棒性**：NLT准确率在扰动前后几乎不变（62.4% vs 62.2%）。

## 7. 优点

- **独立复制与验证**：使用不同代码库、新模型集和更严格的纠正方法，增强了原始发现的科学可信度。
- **开源工具与可重现性**：提供完整代码、提示、输入和原始结果，便于其他研究者验证和扩展。
- **生存偏差纠正**：对灾难性失败条件（≥70/80错误）将有效准确率设为0%，避免误导性高精度（如Qwen3-VL原始看似60.7%，纠正后0%）。
- **能力依赖模式的识别**：首次系统揭示NLT收益随模型能力变化的规律，为分层部署策略提供依据。
- **实用部署建议**：针对不同模型类型提出具体推荐（如无原生支持模型用NLT、前沿模型用混合方法）。
- **经济成本分析**：量化token节省（25.2%）和预期月度成本降低（$4,000-$6,000/百万次调用）。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅单轮、无参数工具选择，未涉及多轮交互、带参数工具或嵌套工具。
  - 仅两个领域（客服、心理健康），可能不适用于编码、数据分析等其他场景。
  - 14个模型仍占LLM总量一小部分；API提供商异质性和参数实现差异可能影响结果。
- **部分数据不完整**：Gemini 2.5 Pro和Qwen3-VL缺失部分条件，结论需谨慎。
- **统计功效不足**：每条件仅5次重复，可能无法检测小于5-10pp的效应；零膨胀分布使方差分析复杂化。
- **解析器差异**：基于正则的YES/NO解析可能漏掉边缘情况，略微高估NLT错误。
- **时间有效性**：原研究（2025年10月）到本研究（2026年1-2月）模型能力可能已变化，结论滞后。
- **应用限制**：未考察NLT在需要严格可解析性的场景（如自动数据处理管道）中的适配性；对于结构化优化极佳的模型（如Gemini 2.5 Pro），NLT可能不利。
- **未分析多智能体框架内的具体故障传播**：仅定性讨论，缺乏定量测量。

（完）
