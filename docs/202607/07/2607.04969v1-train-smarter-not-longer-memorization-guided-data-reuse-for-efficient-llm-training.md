---
title: "Train Smarter, Not Longer: Memorization-Guided Data Reuse for Efficient LLM Training"
title_zh: 更聪明地训练，而非更长时间：基于记忆化引导的数据重用于高效LLM训练
authors: "Jingwei Zuo, Cong Zeng, Ilyas Chahed, Maksim Velikanov, Dhia Eddine Rhaiem, Pasquale Balsebre, Abhay Kumar, Younes Belkada, Hakim Hacid"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.04969v1"
tags: ["query:llm"]
score: 9.0
evidence: 记忆引导的数据重用提升LLM训练效率
tldr: 大语言模型训练中重复使用有限高质量数据可提升性能但易过拟合。本文提出基于“记忆窗口”信号的自适应数据重用范式，通过损失保留动态和下游评估分数决定何时及如何重用数据。初步实验发现性能随重复持续提升，远超常见的四轮上限。该工作为记忆感知训练调度奠定基础，实现更智能而非更长的训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.04969v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1422, \"height\": 529, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04969v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1430, \"height\": 420, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.04969v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1440, \"height\": 459, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.04969v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 834, \"height\": 249, \"label\": \"Table\"}]"
motivation: 现有数据重用缺乏原则性决策，过度重复导致过拟合和收益递减。
method: 定义“记忆窗口”信号（损失保留动态+下游评估分数），自适应决定重用轮次和调度。
result: 性能随数据重复持续提升，远超当前四轮限制，存在记忆驱动改进区间。
conclusion: 记忆指导数据重用为高效训练提供新方向，未来可开发完整调度器。
---

## 摘要
大语言模型的训练范式已从传统的一次性训练转向多周期训练，因为合理复用有限的高质量数据可以提升模型性能和样本效率。然而，过度重复会带来过拟合和收益递减的风险。因此，确定何时以及如何有效复用数据成为一个自然但研究不足的问题。通过对模型从损失保留动态和下游评估分数中得出的“记忆化窗口”信号的新颖观察，我们提出了“记忆化引导的数据重用”训练范式，该范式自适应地确定数据何时以及如何被重用，从而实现对训练周期数和数据重放调度的原则性决策。我们的初步实验揭示了一个一致的记忆化驱动机制：在远超当前实践（例如，常被引用的四周期限制）的重复次数下，性能仍持续提升。虽然完整的调度器仍是未来工作，但这些见解为基于记忆化的训练调度奠定了基础，有助于确定重用预算，并在有限高质量数据下推动更聪明而非更长时间地训练LLM。

## Abstract
The training paradigm of large language models has shifted from traditional one-pass training to multi-epoch training, as reasonable reuse of limited high-quality data can improve both model performance and sample efficiency. Meanwhile, excessive repetition introduces the risk of overfitting and diminishing returns. Determining when and how to reuse data effectively thus emerges as a natural but under-explored question. Through a novel observation of model's "Memorization Window" signals derived from loss retention dynamics and downstream evaluation scores, we propose "Memorization-guided Data Reuse", a training paradigm that adaptively determines when and how data should be reused, enabling principled decisions on the number of training epochs and the scheduling of data replays. Our preliminary experiments reveal a consistent memorization-driven regime: performance continues to improve with repetition far beyond current practice (e.g., the commonly cited four-epoch limit). While a full scheduler remains future work, these insights provide a foundation for memorization-aware training schedules, helping to determine reuse budgets and move toward training LLMs smarter rather than longer with limited high-quality data.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：大语言模型（LLM）训练中，高质量（HQ）数据稀缺，多周期训练成为主流。然而，过度重复会导致过拟合和收益递减，现有方法（如全局混洗）缺乏原则性决策，通常使用保守的固定轮次（如四轮上限）。
- **动机**：需要确定“何时”以及“如何”有效重用数据，实现 “更聪明地训练，而非更长时间训练”。
- **整体含义**：提出基于模型“记忆化窗口”信号的自适应数据重用范式，利用损失保留动态和下游评估分数来指导数据重用的轮次和调度，从而在有限高质量数据下安全地超越传统四轮限制。

## 2. 方法论

### 核心思想
- **记忆化窗口（Memorization Window）**：定义两个互补窗口——保留窗口（τret，模型开始遗忘的时间点）和泛化窗口（τgen，过拟合开始的时间点），有效窗口τ* = min(τret, τgen)。当数据重用的间隔（epoch size N）满足 N ≥ τ* 且总轮数 E ≤ Emax 时，重复能带来与新鲜数据相同的边际收益。
- **间隔重复调度**：提出基于记忆窗口的有序重复（spaced repetition），避免传统全局混洗导致的无控制间隔。

### 关键技术细节
- **定义损失保留间隙**：Δi(τ) = ℓi(θti) − ℓi(θti+τ)，衡量样本xi在经过τ tokens训练后的遗忘程度。Δ大表示仍记忆，小表示遗忘。
- **Rollback协议**：通过计算固定检查点在历史数据上的损失与基准损失的差值，近似估计τret（避免逐样本追踪开销）。
- **安全重用条件**：N ≥ τ* 且 E ≤ Emax，同时建议引入低质量（LQ）数据提供多样性（ρLQ > 0）。

### 算法流程（文字说明）
1. 训练过程中监控损失保留动态（通过rollback）获取τret。
2. 通过下游评估获取τgen（性能峰值点）。
3. 根据τ* = min(τret, τgen)确定安全间隔。
4. 以固定顺序重复数据，保证间隔N ≈ τ*。
5. 当累积轮数超过Emax（泛化下降点）时停止重复。

## 3. 实验设计

- **数据集**：高质量数据为OpenMathInstruct2（采样0.8B唯一token），低质量数据为FineWeb。
- **基准/下游任务**：MATH500（数学推理准确率）。
- **对比方法**：论文未直接对比其他调度方法（如全局混洗），而是通过改变epoch size N（0.8B, 0.85B, 0.95B, 1.6B, 3.2B tokens）内省式验证框架预测。
- **实验场景**：
  - 保留窗口测量：在100M模型上训练500GT无重复数据，在不同位置（20-500GT）进行rollback。
  - 泛化窗口测量：固定HQ 0.8B，混合不同量LQ以获得不同N，在MATH500上评估准确率随累积HQ tokens的变化。
  - LQ多样性效应：纯HQ vs 含6% FineWeb的混合配置。

## 4. 资源与算力

- **文中明确信息**：使用100M参数decoder-only Transformer，训练500 Gigatokens（GT），常数学习率。
- **未明确信息**：未提及GPU型号、数量、训练时长。仅可推断为单次实验规模较小（100M模型）。

## 5. 实验数量与充分性

- **实验数量**：两组主要实验——rollback实验（1个图，多个检查点）和泛化窗口实验（3个子图，5种N配置）。附加LQ效应对比（2种）。
- **充分性**：实验设计初步但聚焦，验证了记忆窗口存在性及epoch size影响。然而，仅覆盖一个模型尺度（100M）、一个领域（数学推理）、一个HQ预算（0.8B）。缺少消融实验（如不同学习率、不同阈值ϵ）、缺少与其他调度策略（全局混洗）的直接对比，也未探索模型大小、架构的泛化性。作者承认这些局限。
- **公平性**：对比组间数据混合比例不同（HQ占比随N增大而减小），可能混淆epoch size与数据多样性效应，作者指出这一点。

## 6. 主要结论与发现

- **记忆窗口的存在验证**：保留窗口τret约在1B–5B tokens之间，且在不同训练阶段（20-500GT）保持一致；泛化窗口τgen随epoch size增大而延迟（N从0.85B到1.6B，峰值从约30轮移到约175轮）。
- **饱和现象**：当N ≥ τ*后，进一步增大N（如从1.6B到3.2B）不再带来额外收益，性能曲线几乎重叠。
- **LQ多样性是关键**：少量LQ（6%）即可显著提升纯HQ训练，效果远超仅增加间隔，说明多样性对防止过拟合至关重要。
- **重复与新鲜数据等价**：在间隔足够（N ≥ τ*）时，重复数据的边际收益与新鲜数据相同，性能可远超四轮限制（例如1.6B epoch size下可安全训练125-190轮）。

## 7. 优点

- **概念创新**：提出“记忆窗口”框架，将遗忘动力学与泛化性能结合，为数据重用提供理论指导。
- **方法简洁实用**：定义损失保留间隙和rollback协议，无需复杂追踪，易于实现。
- **实证揭示**：发现间隔重复可等价于新鲜数据，突破了传统四轮上限，具有实际指导意义。
- **讨论全面**：连接了课程学习、持续学习、grokking等多个方向，为后续研究提供丰富视角。

## 8. 不足与局限

- **实验覆盖狭窄**：仅一个模型尺度（100M）、一个领域（数学推理）、一个HQ预算（0.8B），记忆窗口对模型大小、架构、数据复杂度、超参数的依赖性未探索。
- **评估方法的稳健性**：rollback估计有噪声，阈值ϵ未明确；最优N*的确定依赖峰值，但受数据混合比例干扰。
- **缺乏对比基准**：未与全局混洗、固定轮次等常见调度方法直接比较。
- **未实现完整调度器**：论文仅给出初步验证和框架，实际自适应调度算法（如在线估计τ*）未开发。
- **计算开销**：rollback需要存储历史损失或重计算，大规模训练中成本可能高。
- **潜在偏差**：仅使用MATH500一个下游任务，可能存在任务特定的记忆窗口行为。

（完）
