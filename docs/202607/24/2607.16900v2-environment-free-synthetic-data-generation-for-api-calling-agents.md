---
title: Environment-free Synthetic Data Generation for API-Calling Agents
title_zh: 无环境合成数据生成用于API调用智能体
authors: "Seanie Lee, Sanjoy Chowdhury, Chao Jiang, Cheng-Yu Hsieh, Ting-Yao Hu, Alexander T Toshev, Oncel Tuzel, Raviteja Vemulapalli"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16900v2"
tags: ["query:llm-synth"]
score: 9.0
evidence: 面向API调用智能体的合成数据生成
tldr: 训练API-calling代理需要大量高质量轨迹数据，但依赖完整执行环境难以扩展。本文提出无环境合成数据生成方法，利用LLM作为数字世界模型，仅需API规范即可生成模拟代理与环境交互的轨迹。方法先由LLM生成多样任务，再通过教师代理逐步求解，并由LLM模拟器产生一致的API响应，最后经LLM裁判筛选确保质量。在AppWorld和OfficeBench上微调模型获得显著性能提升，证明了无需可执行环境即可有效训练代理，为构建多API生态下的可扩展训练方案提供了新途径。
source: arxiv
selection_source: fresh_fetch
motivation: 现有训练API-calling代理的方法依赖完整执行环境，难以规模化扩展数据收集。
method: 利用LLM作为世界模型，根据API规范生成任务、模拟代理与环境交互并过滤高质量轨迹。
result: 在AppWorld和OfficeBench上微调模型取得显著性能提升，超越基线。
conclusion: 无环境合成数据生成可有效训练API-calling代理，为构建可扩展训练方案提供可行途径。
---

## 摘要
训练API调用的大型语言模型智能体需要大量高质量轨迹。然而，大规模收集此类数据通常需要具有可执行API和真实、预填充后端数据库的完整实现环境，这为可扩展性造成了主要瓶颈。为克服这一难题，我们提出了一种无环境合成数据生成方法，该方法利用大型语言模型作为即时数字世界模型。仅需提供API规范，我们的方法即可生成模拟智能体与有状态环境之间交互的轨迹。具体而言，首先由大型语言模型生成可通过所提供API解决的多样化任务。然后，教师智能体迭代解决每个任务，同时大型语言模型模拟器根据任务上下文和模拟历史生成连贯的合成API响应。最后，大型语言模型评审员筛选轨迹以确保生成数据集的质量。我们在具有挑战性的AppWorld和OfficeBench基准上评估了该方法，这些基准包含信息检索和状态改变任务。在我们的合成数据上微调模型取得了显著的性能提升，表明无需任何可执行环境即可为API调用智能体生成有效监督。我们的结果确立了基于大型语言模型的API模拟作为跨多样化API生态系统训练智能体的实用、可扩展解决方案。

## Abstract
Training API-calling large language model (LLM) agents demands massive amounts of high-quality trajectories. However, collecting such data at scale typically requires fully implemented environments with executable APIs and realistic, pre-populated backend databases, creating a major bottleneck for scalability. To overcome this, we propose an environment-free synthetic data generation approach that leverages LLMs as on-the-fly digital world models. Given only API specifications, our method generates trajectories mimicking interactions between an agent and a stateful environment. Specifically, an LLM first generates diverse tasks solvable with the provided APIs. A teacher agent then iteratively solves each task while an LLM simulator generates coherent synthetic API responses conditioned on the task context and simulation history. Finally, an LLM judge filters the trajectories to ensure the quality of the resulting dataset. We evaluate our approach on the challenging AppWorld and OfficeBench benchmarks, which include both information-retrieval and state-changing tasks. Fine-tuning models on our synthetic data yields significant performance gains, demonstrating that effective supervision for API-calling agents can be generated without any executable environment. Our results establish LLM-based API simulation as a practical, scalable solution for training agents across diverse API ecosystems.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将基于您提供的论文内容，对其核心思想、方法、实验及结论进行结构化和深入的分析总结。

### 论文核心问题与研究动机

*   **核心问题**：训练大型语言模型作为API调用智能体需要海量的、高质量的、多步骤的交互轨迹数据。然而，传统方法依赖完整的、可执行的软件环境（包括真实API和后端数据库/状态），这成为了规模化扩展的主要瓶颈。为每个新的API集合构建和填充这样的环境成本高昂且耗时。

### 论文提出的方法论：ESAT

*   **核心思想**：提出一种“无环境”（Environment-Free）的合成数据生成范式，利用现代LLM自身具备的广泛世界知识，作为“动态数字世界模型”（Dynamic Digital World Models）。仅需API的规范文档作为输入，即可生成完整的、状态一致的智能体交互轨迹，完全免去了构建真实后端环境的必要。

*   **关键技术细节（ESAT管道）**：
    1.  **任务合成阶段**：
        *   **桶化生成**：通过定义难度、动作类型、任务焦点、应用数量等属性的组合网格，引导一个“任务生成器LLM”生成多样化且覆盖均匀的任务。
        *   **逆频率采样**：为平衡API覆盖度，优先选择使用次数少的应用和API来生成任务。
        *   **任务评审与重写**：使用LLM评审员过滤无效任务，并由另一LLM将生成的任务从过程性描述重写为自然、聚焦意图的用户请求。
    2.  **轨迹合成阶段**：
        *   **教师-模拟器交互**：一个“教师代理LLM”迭代地执行任务，发出API调用。一个“API模拟器LLM”动态生成符合规范的、上下文一致的API响应（模拟环境反馈），从而产生完整的、多步交互轨迹。
        *   **有状态模拟**：模拟器通过输入任务上下文、历史调用记录、虚拟用户信息等，来确保生成响应的数据一致性、多样性和真实性，使其成为一个持续的数字世界模型。
        *   **质量控制**：模拟器在生成响应前会进行程序化参数验证，生成后还会进行模式验证和语义正确性评估（通过LLM评审员），不符合要求则重新生成。
    3.  **轨迹过滤阶段**：
        *   使用一个“轨迹评审LLM”评估生成的完整轨迹，筛选出正确、完整、无冗余的高质量轨迹，用于最终的模型训练。

### 实验设计

*   **数据集与基准**：
    *   **AppWorld**：一个挑战性极高的多应用基准，包含九个应用的457个API，任务需要复杂的读写迭代。提供了Test-Normal和Test-Challenge两个测试集。
    *   **OfficeBench**：聚焦办公自动化，包含八个应用的20个API，任务需要跨应用的长周期工作流。

*   **对比方法**：
    *   **零样本基线**：直接在基准上评估未经微调的模型。
    *   **真实环境轨迹**：使用从真实AppWorld环境中收集的轨迹（AWT）进行微调。
    *   **现有合成数据集**：与ToolAlpaca、ToolACE、Nemotron等仅面向简单工具调用场景的现有合成数据集进行对比。
    *   **前沿模型**：与GPT-4o，Gemini-3.1-Pro等商用大模型进行交叉比较。

### 资源与算力

*   论文明确给出了训练阶段的算力配置，但**未明确提及**训练的总时长。
    *   **数据合成**：使用GLM-4.7-FP8和GLM-5.1-FP8模型。
    *   **模型微调**：
        *   参数小于140亿的模型（如Qwen3-8B）在8张H100 GPU上进行训练。
        *   参数为140亿及以上的模型（如Qwen3-14B, Qwen3.5-27B）在8张B200 GPU上进行训练。

### 实验数量与充分性

*   **实验数量**：论文在不同模型系列（Qwen3和Qwen3.5）的8个不同参数规模（1.7B到27B）上进行了大量实验，涵盖了两种数据集、多种对比方法以及多种数据来源的组合（如仅合成数据、合成数据+真实数据）。此外，还包含了关于LLM评审员质量、模拟器质量、与现有数据集对比等多项消融实验，总计超过几十组实验。
*   **充分性与公平性**：
    *   实验**非常充分**，系统性地验证了方法在不同模型规模和不同数据配置下的有效性。
    *   **结果客观**，报告了8次运行的平均值和标准差，增加了结果的可靠性和可复现性。
    *   对比条件**公平**，例如在同时使用合成数据和真实数据时，保证了模型性能的提升确实来源于ESAT数据。

### 主要结论与发现

1.  **效果显著**：在ESAT合成数据上微调的模型，性能相比基座模型有巨大提升（AppWorld最高提升50.5%，OfficeBench最高提升60.5%）。
2.  **超越真实环境数据**：在AppWorld上，使用ESAT合成数据训练的模型，甚至**超越了**使用从真实AppWorld环境中收集的数据（AWT）训练的模型，证明了无环境合成数据的强大能力。
3.  **强泛化能力**：即使使用与目标领域完全无关的合成API（如52个虚构应用）生成数据，模型也能学到良好的API调用技能，成功泛化到未见过的真实API。
4.  **高效知识蒸馏**：使用ESAT数据微调的小模型（如8B参数）能够达到或接近比其大得多的模型（如GPT-4o, GLM-5.1-FP8）的性能，证明了该方法在知识蒸馏方面的有效性。
5.  **可扩展性**：该方法将训练数据生成简化为一个“轻量级规范问题”，仅需API规范即可，大大降低了在新API生态上扩展的壁垒。

### 优点

1.  **方法论创新**：提出“无环境”数据生成范式，核心是利用LLM作为有状态的数字世界模型，从根本上解决了依赖真实执行环境的扩展性问题。
2.  **高质量模拟器**：通过历史信息、上下文和多项规则约束，模拟器能生成状态一致、真实感强的API响应，保证了合成轨迹的质量。
3.  **完整且健壮的管道**：从任务生成、轨迹模拟到最终过滤，每一步都有LLM进行质量控制，形成了一个自动化、高质量的合成数据生产流水线。
4.  **全面的实验验证**：在两个极具挑战性的基准上，对多种模型大小和数据来源进行了一致且充分的验证，实验结果的说服力很强。

### 不足与局限

1.  **模拟器在长输出下表现不佳**：分析表明，当模拟器需要生成长文本输出（超过500 token）时，失败率会显著上升，这揭示了当前模型在维持长程一致性时的局限性。
2.  **任务的多样性偏倚**：合成任务在涉及的应用数量上存在一定偏倚，大部分任务集中在单个应用，跨越多应用的任务相对较少。虽然论文认为这符合现实分布，但可能限制了模型处理高度复杂跨应用任务的能力。
3.  **语言上的偏倚**：所有实验均使用英文进行，其方法在非英语环境下的泛化能力和有效性尚未得到验证。
4.  **对LLM评审员的依赖**：虽然论文验证了其LLM评审员的高准确率，但整个流程仍然依赖于另一个（通常是更强大的）LLM来保证数据质量，这引入了一定的成本和潜在的偏倚风险。
5.  **未探索的安全性**：论文未讨论合成数据或训练出的模型在真实世界中可能产生的安全隐患（如恶意指令遵循、数据泄露等），这是一个未来研究的重要方向。

（完）
