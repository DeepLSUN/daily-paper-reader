---
title: "Demonstrating GenDB: Instance-Optimized and Customized Query Processing Code Generation via LLM Agents"
title_zh: 展示GenDB：通过LLM代理实现实例优化和自定义的查询处理代码生成
authors: "Jiale Lao, Immanuel Trummer"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.20630v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于实例优化查询处理代码生成的LLM智能体
tldr: 传统查询处理引擎扩展困难且构建新系统成本高。GenDB提出基于LLM代理的查询处理代码生成方法，自动为特定数据、工作负载和硬件生成实例优化的执行代码。在TPC-H和新基准测试中，生成代码性能显著优于现有引擎。该工作开创了LLM驱动的数据库代码生成新范式，降低了定制化查询处理的工程成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.20630v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 753, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20630v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 882, \"height\": 606, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.20630v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1663, \"height\": 1205, \"label\": \"Figure\"}]"
motivation: 传统查询引擎难以扩展，且构建新系统需要大量工程投入，亟需更灵活的查询处理方法。
method: 利用LLM代理分析工作负载、硬件和数据结构，自动生成查询计划并迭代优化代码，确保正确性和高效性。
result: 在TPC-H和新基准上，GenDB生成代码的性能显著优于State-of-the-art查询引擎。
conclusion: GenDB展示了LLM在数据库代码生成中的潜力，为实例优化查询处理提供了可行且高效的解决方案。
---

## 摘要
传统的查询处理引擎需要持续开发和扩展以支持新技术和用户需求，在某些情况下，必须从头构建全新的系统。然而，由于其内部复杂性，这些引擎难以扩展，而构建新系统需要大量的工程工作和成本。为了解决这一问题，我们展示了GenDB，一种生成式查询引擎，它将查询处理从手动构建的系统转向由大型语言模型（LLM）驱动的查询处理代码生成。GenDB的早期原型使用LLM代理生成针对特定数据、工作负载和硬件资源量身定制的实例优化查询执行代码。该原型适用于重复性模板查询的离线代码生成，因为前期生成成本可通过多次执行分摊，并且可以通过广泛的模糊测试和人工检查来确保正确性。对于即席查询，GenDB可以与传统数据库管理系统（DBMS）以混合架构协作：DBMS处理一次性查询，而GenDB加速频繁的SQL模板。我们的演示允许用户：（1）通过可视化交互方式探索GenDB如何分析工作负载、分析硬件资源和底层数据、生成查询计划、基于这些生成代码，并最终使用优化器迭代实现正确且高效的实现；（2）通过视觉检查和定性分析，了解为什么GenDB生成的代码在两个基准测试（TPC-H和一个新构建的旨在减少LLM训练数据潜在泄露的基准测试）上比最先进的查询引擎性能显著提升；（3）上传自己的数据和查询，探索GenDB与不同LLM和查询模式的结合。

## Abstract
Traditional query processing engines require continuous development and extensions to support new techniques and user requirements, and in some cases, entirely new systems must be built from scratch. However, these engines are difficult to extend due to their internal complexity, and building new systems demands significant engineering effort and cost. To address this, we demonstrate GenDB, a generative query engine that shifts query processing from manually engineered systems to query processing code generation driven by Large Language Models (LLMs). An early prototype of GenDB uses LLM agents to generate instance-optimized query execution code tailored to specific data, workloads, and hardware resources. This prototype suits offline code generation for repetitive, templated queries, since the upfront generation cost amortizes over many executions and correctness can be ensured through extensive fuzz testing and manual inspection. For ad-hoc queries, GenDB can work with a traditional DBMS in a hybrid architecture: the DBMS handles one-off queries, while GenDB speeds up frequent SQL templates. Our demonstration allows users to (1) visually and interactively explore how GenDB analyzes workloads, profiles hardware resources and underlying data, produces query plans, generates code based on them, and finally uses an optimizer to iteratively achieve a correct and efficient implementation; (2) use visual inspection and analysis to gain qualitative insights into why GenDB produces code that achieves significantly better performance than state-of-the-art query engines on two benchmarks: TPC-H and a newly constructed benchmark designed to reduce potential data leakage from LLM training data; and (3) upload their own data and queries to explore GenDB with different LLMs and query patterns.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：GenDB：通过LLM代理实现实例优化和自定义查询处理代码生成

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：传统查询处理引擎（如关系型数据库）需要持续开发和扩展以支持新技术和用户需求，但引擎内部复杂，扩展困难；构建全新系统则需要巨大的工程成本和人力投入。数据库领域技术演进迅速，当前范式难以跟上节奏。
- **整体含义**：探索一种替代方案——利用大型语言模型（LLM）自动生成查询处理代码，取代手动构建和扩展查询引擎。GenDB 将查询处理从“手工工程系统”转向“LLM驱动的代码生成”，实现针对特定数据、工作负载和硬件资源的实例优化。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 使用 **LLM代理（LLM Agents）** 组成多代理工作流，自动分析工作负载、硬件资源、数据特征，生成并迭代优化查询执行代码（C++）。
- 目标场景：离线代码生成，适用于重复执行的模板化SQL查询（常见于工业负载），前期生成成本可通过多次执行分摊。
- 混合架构：对于即席查询，GenDB与传统DBMS协作——DBMS处理一次性查询，GenDB加速频繁的SQL模板。

### 关键技术细节
- **多代理工作流**（共5个代理）：
  1. **Workload Analyzer**：分析硬件资源（如SIMD支持、缓存大小）、模式、SQL查询和底层数据，生成结构化工作负载特征（存储类型、表统计、连接模式、过滤选择性等）。
  2. **Storage/Index Designer**：设计并实现优化的存储结构（如列式存储、编码、压缩）和索引。例如，对于低基数列使用字典编码，对`l_orderkey`建立分组索引实现O(1)查找。
  3. **Query Planner**：基于工作负载分析和存储设计，生成资源感知的物理执行计划。例如，聚合操作根据GROUP BY列基数（如国家名25个键）选择直接平面数组（完全放入L1缓存）或线程本地哈希表。
  4. **Code Generator**：将执行计划实现为高效的C++代码，应用编译器优化等系统级优化。
  5. **Query Optimizer**：通过运行时反馈迭代优化执行计划和代码实现。使用实际执行时间作为反馈，直至达到预算或目标。

- **工具使用**：每个代理配备内置工具（文件操作、终端执行、网络搜索等），可自动生成采样程序评估候选连接顺序等复杂任务。

- **正确性验证**：
  - 与传统DBMS（如DuckDB）输出对比。
  - 使用模糊测试：在不同SQL模板谓词和测试数据库上验证结果一致性。
- **迭代优化**：初始代码可能性能差，通过识别瓶颈（如大数组零初始化、I/O策略不当）逐步改进，最终达到高性能。

## 3. 实验设计

### 数据集/场景
- **TPC-H**：规模因子10（SF=10），选取5个代表性查询（Q1, Q3, Q6, Q9, Q18）。
- **SEC-EDGAR**：新构建的基准测试，使用SEC EDGAR数据集（很少用于数据库基准测试），查询由SQLSmith随机生成，旨在减少LLM训练数据泄露风险。

### 对比方法（baselines）
- DuckDB、ClickHouse、Umbra、MonetDB、PostgreSQL。
- 对比时确保所有系统使用可比硬件资源（如全并行执行），同时报告各系统的最佳性能：仅用自动创建的索引 vs. 补充GenDB推荐的索引。

### 实验数量与充分性
- 论文中仅展示部分实验结果（两个基准测试共约11条查询），声称完整结果在项目仓库中。
- 额外进行了成本分析（生成时间和费用）、正确性分析（模糊测试）。
- 进行了迭代优化过程的消融分析，展示了Q3从593ms到27ms的改进（22×提升）。
- 总体实验规模较小，但聚焦于证明概念和性能潜力。

## 4. 资源与算力

- **硬件**：Ubuntu服务器，双路Intel Xeon Gold 5218 CPU（64核），384 GB RAM，数据库完全缓存到内存中。
- **LLM**：Claude Sonnet 4.6（Anthropic）作为底层LLM。
- **生成成本（时间与费用）**：
  - TPC-H：$14美元，91分钟。
  - SEC-EDGAR：$23美元，140分钟。
- **说明**：未明确提及GPU型号或训练过程，因为GenDB使用现成的商业LLM API，不涉及模型训练。

## 5. 实验数量与充分性分析

- **实验数量**：仅展示了两个基准测试共11条查询的结果，数量偏少。但论文声称完整结果在GitHub仓库中（包括所有TPC-H查询）。
- **充分性**：实验设计较为客观：
  - 对比了多个主流引擎，确保硬件事先配置可比。
  - 为baseline提供了两种索引配置，尽量公平。
  - 使用新基准SEC-EDGAR避免数据泄露。
- **局限性**：
  - 只测试了离线模板查询场景，未验证即席查询的混合架构效果。
  - 实验未展示在不同硬件（如单核、不同缓存大小）下的泛化能力。
  - 未进行跨LLM模型的消融实验（仅使用Claude Sonnet 4.6）。
  - 正确性验证仅依靠与DBMS结果一致和模糊测试，未进行形式化验证。

## 6. 主要结论与发现

- GenDB生成的代码在**TPC-H**上总执行时间 **249 ms**，比DuckDB快2.4倍，比Umbra快2.7倍，比ClickHouse快9.2倍。
- 在**SEC-EDGAR**上总执行时间 **403 ms**，比DuckDB快3.8倍，比Umbra快3.3倍。
- 迭代优化显著提升性能：Q3从593ms降至27ms（22×优化），主要通过数据结构压缩、I/O策略切换和并行化实现。
- 生成成本（$14~$23，91~140分钟）对于离线、重复执行的模板查询是可接受的，多次执行可分摊。
- 正确性通过模糊测试验证，但无法保证对所有可能数据库的语义等价。

## 7. 优点：方法与实验设计亮点

- **创新性**：首次将LLM代理用于端到端查询处理代码生成，实现高度实例优化（数据、工作负载、硬件三方面）。
- **透明性**：每个代理的观察、推理、决策均可视化，用户可交互式探索，避免“黑箱”。
- **可扩展性**：支持用户上传自定义数据和SQL，选择不同LLM，具有灵活性和实际应用潜力。
- **性能突出**：在标准基准测试上显著超越现有引擎，且优化过程可解释。
- **系统性设计**：多代理分解复杂任务，工具使用能力使代理能自动完成采样、基准测试等子任务。

## 8. 不足与局限

- **正确性保证不充分**：仅通过少量测试数据和模糊测试验证，不能保证在所有数据库上的语义等价。作者承认需要更强保证（如形式化验证）。
- **适用场景有限**：目前仅针对离线、重复模板查询；对于即席查询，虽提出混合架构但未实验验证。
- **生成成本较高**：即使对TPC-H也需要91分钟和$14美元；在需要快速响应的场景不可行。
- **实验覆盖不足**：仅测试了约11条查询，未展示所有TPC-H查询结果；未进行跨硬件、跨LLM、跨数据规模的多维实验。
- **依赖商业LLM API**：可能带来延迟、成本波动、隐私风险和数据依赖性。
- **未考虑安全性**：LLM生成的C++代码可能包含未检测到的漏洞（如内存问题），尤其是迭代优化中可能引入不安全操作。
- **可重复性问题**：LLM生成具有随机性，未报告多次运行的平均值和方差。
- **缺乏与手动优化代码的对比**：未对比手写C++的极限性能。

（完）
