---
title: "Adaptive Depth Sparse Framework: Similarity-Driven Resource Allocation for Pre-Trained LLMs"
title_zh: 自适应深度稀疏框架：面向预训练大语言模型的相似度驱动资源分配
authors: "Yidu Wu, Xiang Wang, Kejie Zhao, Zhangchi Wang, Qinghai Guo, Xiaoying Tang"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21291v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向预训练LLM的推理优化
tldr: 大语言模型推理成本高，现有加速方法依赖任务特定微调或重新训练。本文提出自适应深度稀疏框架AdaDSF，在无需全重训练下将预训练LLM转为深度稀疏模型。其核心是利用层间余弦相似度统计分配token保留比例，通过轻量路由器选择重要token，并引入特征保留对齐目标匹配稀疏与稠密模型的中间和最终表示。在GPT-NeoX和Qwen2.5的语言建模与常识推理任务上，AdaDSF大幅降低推理FLOPs，性能接近稠密模型，在可比稀疏度下准确率下降小于MoD、D-LLM、DLO等强基线。该方法避免了任务特定微调，提升了跨任务泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21291v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1168, \"height\": 678, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.21291v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 950, \"height\": 565, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21291v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1148, \"height\": 429, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1236, \"height\": 509, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 853, \"height\": 663, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 721, \"height\": 223, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21291v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 468, \"height\": 140, \"label\": \"Table\"}]"
motivation: 现有LLM加速方法依赖任务特定微调或重新训练，适应成本高且跨任务可用性有限。
method: 基于层输入输出余弦相似度统计分配token保留比例，用轻量路由器选择信息token，并引入特征保留对齐目标。
result: 在GPT-NeoX和Qwen2.5上显著减少推理FLOPs，性能接近稠密模型，优于MoD、D-LLM、DLO等基线。
conclusion: AdaDSF将预训练LLM转为深度稀疏模型无需全重训练，有效降低推理成本且保持性能，具有良好泛化性。
---

## 摘要
大型语言模型（LLMs）在生成和推理方面表现出色，但Transformer架构推理成本高昂。现有加速方法通常依赖于特定任务的微调或从头训练，增加了适配成本并限制了跨任务可用性。我们提出了一种自适应深度稀疏框架（AdaDSF），该框架无需完全重新训练即可将现成的预训练大语言模型转换为深度稀疏模型。我们的关键洞察是，各层对表示变换的贡献不同，这一特性可通过层输入与输出隐藏状态之间的余弦相似度来表征。基于此，AdaDSF根据相似度统计为每层分配令牌保留比例，使用轻量级路由器在每层选择信息丰富的令牌，并引入特征保持的对齐目标以匹配稀疏模型与稠密模型之间的中间和最终表示。在GPT-NeoX和Qwen2.5上进行的语言建模和常识推理任务中，AdaDSF在保持接近稠密模型性能的同时大幅减少了推理FLOPs。在可比稀疏度下，AdaDSF相对于强基线方法（包括MoD、D-LLM和DLO）始终表现出更小的准确率下降。

## Abstract
Large language models (LLMs) achieve strong generation and reasoning performance, but the Transformer architecture incurs high inference cost. Existing acceleration methods often rely on task-specific fine-tuning or training from scratch, increasing adaptation cost and limiting cross-task usability. We present an Adaptive Depth Sparse Framework (AdaDSF) that converts off-the-shelf pre-trained LLMs into depth-sparse models without full retraining. Our key insight is that layers contribute unequally to representation transformation, characterized by the cosine similarity between layer input and output hidden states. Based on this, AdaDSF assigns layer-wise token retention ratios from similarity statistics, uses a lightweight router to select informative tokens at each layer, and introduces a feature-preserving alignment objective to match intermediate and final representations between sparse and dense models. On GPT-NeoX and Qwen2.5 over language modeling and commonsense reasoning, AdaDSF substantially reduces inference FLOPs while preserving performance close to dense counterparts. Under comparable sparsity, AdaDSF consistently yields smaller accuracy degradation than strong baselines including MoD, D-LLM, and DLO.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

大语言模型（LLMs）基于Transformer架构，在生成和推理任务上表现优异，但推理成本高昂——复杂度随层数线性增长、随序列长度二次增长。现有加速方法（如量化、知识蒸馏、轻量注意力）往往需要针对特定任务进行微调或从头训练，导致适配成本高、跨任务可用性受限。本文的核心动机是：**能否在不全重训练的情况下，将现成的预训练稠密LLM转换为深度稀疏模型，从而大幅降低推理计算量，同时保持逼近稠密模型的性能？** 论文提出自适应深度稀疏框架（AdaDSF），利用层间表示变换的不均匀性（以余弦相似度衡量）来动态分配每层的计算资源，实现了高效的推理加速。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

#### 核心思想
- 观察：Transformer各层对输入表示的变换贡献不均衡，通过层输入与输出隐藏状态的余弦相似度可定量刻画。
- 思路：相似度高的层（变换较小）分配更少的计算资源（保留较少token），相似度低的层（变换较大）分配更多资源。从而在不显著影响性能的前提下减少总FLOPs。

#### 关键技术细节
1. **相似度驱动的层间token保留比例分配**：
   - 对第i层，计算输入`x_in^{(i)}`与输出`x_out^{(i)}`的余弦相似度`s_i`。
   - 通过温度归一化加权（公式5）、偏差缩放（公式6）、Sigmoid映射到[0.05, 0.95]区间（公式7），再经全局预算校正（公式8）得到每层保留比例`r_i`，使得所有层平均保留率为目标值`t`。
2. **轻量级token路由器**：
   - 每层配备一个MLP路由器，预测每个token的重要性分数，并选择Top-K_i个token（K_i = r_i * 序列长度）进入Transformer层计算；其余token通过残差路径跳过。
3. **特征保持对齐训练**：
   - 引入两个对齐损失：
     - 隐藏状态对齐：每层稀疏模型与稠密模型输出softmax后做MSE（公式9）。
     - 输出分布对齐：KL散度对齐最终logits（公式10）。
   - 总损失：`L = L_align + (1/L) * Σ L_hid^{(l)}`（公式11）。

#### 算法流程（文字描述）
1. 使用小规模校准数据计算每个稠密层的余弦相似度。
2. 根据相似度生成每层的token保留比例。
3. 将预训练模型每层替换为“路由器 + Transformer层”的稀疏模块（路由器与原始层参数可训练，其余冻结）。
4. 用对齐损失（隐藏状态+输出分布）训练路由器，并微调Transformer层以恢复因跳过计算导致的表示偏移。
5. 推理时保留路由器及训练后的参数，按每层保留比例动态跳过token。

### 3. 实验设计：数据集、基准与对比方法

#### 模型与数据集
- **语言建模**：GPT-NeoX-130M在Wikitext103上训练和测试。
- **指令微调场景**：Qwen2.5-0.5B和Qwen2.5-1.5B使用GenQA、InfinityInstruct、OpenHermes2.5进行指令微调训练。
- **评估任务**：Wikitext103困惑度（PPL）+ 6个常识推理基准：ARC-Challenge、ARC-Easy、HellaSwag、PIQA、WinoGrande、OpenBookQA。

#### 基准方法
- **MoD**（Mixture-of-Depths）：固定预算下选择性执行全块。
- **D-LLM**：动态层执行，减少KV缓存。
- **DLO**（Dynamic Layer Operation）：结合跳过与扩展。

#### 实验设置
- 保留比例：90%、80%、70%三档。
- 性能指标：准确率（Avg）、与稠密基线的差异（Diff）、归一化FLOPs。

### 4. 资源与算力

论文中**未明确说明**训练所用的GPU型号、数量、总训练时长等具体算力信息。仅提到实验在GPT-NeoX-130M、Qwen2.5-0.5B和1.5B上进行，训练采用对齐损失微调路由器及部分层，所需计算量远低于全重训练。

### 5. 实验数量与充分性

- **主实验**：在两个模型家族（GPT-NeoX、Qwen2.5）上，对3种保留比例分别与MoD、D-LLM、DLO做了比较（表1、表2）。
- **消融实验**：
  - 相似度保留比例分配的有效性（表3：不同温度τ的影响，最优τ=0.05）。
  - 对齐损失的必要性（表4：无对齐损失PPL为20.14，有对齐损失为18.91）。
- **数量**：总计约10+组对比实验，覆盖2个语言模型、3个保留比例、6个推理任务，消融实验2组。
- **充分性评价**：实验设计较全面，涵盖了不同规模模型、不同稀疏度、强基线对比，并验证了核心组件（相似度分配、对齐损失）的必要性。但缺少与更稀疏方法（如结构化剪枝、量化）的对比，且未在更大模型（7B+）上验证，泛化性有一定限制。总体公平客观，消融清晰。

### 6. 论文的主要结论与发现

1. AdaDSF在所有保留比例下均优于MoD、D-LLM、DLO，平均准确率下降更小（如Qwen2.5-0.5B在80%保留率下平均准确率49.1%，Diff=-2.6，而MoD为-7.3，DLO为-3.4）。
2. 在GPT-NeoX-130M上，AdaDSF在80%保留率下PPL=18.9，远优于D-LLM（2019）和MoD（21.6），且FLOPs仅0.787×稠密模型。
3. 相似度驱动分配可有效将计算导向变换更显著的层；对齐训练显著弥补稀疏化带来的表示偏移。
4. AdaDSF**无需修改内部注意力机制或层结构**，可直接应用于现有预训练检查点，实现低成本的推理加速。

### 7. 优点：方法或实验设计上的亮点

- **轻量且可迁移**：只需在每层加一个MLP路由器，无需修改Transformer主干；可基于预训练检查点直接转换，无需全重训练。
- **数据驱动的分配策略**：利用余弦相似度作为代理指标，避免了手工设计固定保留比例，具有自适应性。
- **特征保持对齐**：同时对齐中间隐藏状态和最终输出，显著提升了稀疏模型的鲁棒性，且训练目标简单有效。
- **公平比较**：所有基线在同一硬件环境下重新实现，控制保留比例和FLOPs相当，确保对比公正。

### 8. 不足与局限

- **模型规模覆盖有限**：实验只验证了130M、0.5B和1.5B大小的模型，未在更大规模（7B/13B/70B）上测试，无法确认方法在更大模型上的扩展性。
- **任务多样性不足**：仅评估了语言建模和常识推理，未涉及代码生成、数学推理、多轮对话等复杂任务。
- **依赖校准数据**：相似度统计需要一点校准数据（小批量），可能在实际部署时需要额外准备。
- **未与更广泛的加速方法对比**：未比较量化、剪枝、蒸馏等正交方法，也未讨论组合使用的可能性。
- **训练成本未量化**：虽然声称轻量，但未给出具体微调所需GPU小时数或数据量，不利于实际部署的算力评估。
- **潜在偏差**：WikiText103和6个常识推理数据集多为英文，其他语言或领域未验证。

（完）
