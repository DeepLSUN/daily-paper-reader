---
title: Just A Rather Very Intelligent Spoken Agent
title_zh: 仅仅是一个相当非常智能的语音代理
authors: "Chen Chen, Zhehuai Chen"
date: 2026-07-18
pdf: "https://arxiv.org/pdf/2607.16610v1"
tags: ["query:agent"]
score: 9.0
evidence: 语音代理中介者和长期代理基准
tldr: 当前长周期AI代理与用户交互单薄，缺乏实时协商的中介。提出JarvisBench基准，含agent-collaboration和user-interaction两轨，并构建模块化Jarvis原型。在WildClaw任务上初步评估显示，基于GPT-5.5等模型的中介能提供轨迹可溯回答，在适当时刻注入稀疏指导可提升任务性能。该工作强调了中介层的重要性和对更强LLM的需求。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.16610v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 761}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.16610v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 476}, {\"url\": \"assets/tables/arxiv/2607.16610v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1434, \"height\": 273}]"
motivation: 现有AI代理与用户交互过于单薄，缺少持续可协商的Jarvis式中介层。
method: 提出JarvisBench基准，含双轨衡量中介的协作与交互价值，并构建模块化Jarvis原型。
result: 在WildClaw任务上，中介能基于轨迹回应用户问题，适当时刻注入指导可提升任务性能。
conclusion: Jarvis式中介改善代理透明度与任务完成，但效果高度依赖底层LLM能力。
---

## 摘要
长期规划的AI代理正变得越来越强大，但它们与用户的交互仍然出奇地薄弱。在大多数工作流程中，用户给出初始指令，仅收到选择性的文本更新，并且对代理正在做什么或何时介入缺乏清晰的认识。这导致了当前代理生态系统中的一个缺失部分：一个始终在线的Jarvis式中介，使代理持续可被用户触及。这样的中介应支持与用户的实时语音交互，在不中断工作者的情况下回答问题，主动报告进展或困惑，并在有用时将用户指导注入回代理的执行中。在这项工作中，我们引入了JarvisBench，一个用于衡量长期代理工作流中中介双重价值的基准测试。JarvisBench包含两个互补的轨道：一个代理协作轨道，衡量中介是否改善下游任务完成；一个用户交互轨道，衡量中介是否使正在进行的执行更加可理解、响应迅速和用户可访问。我们通过一个模块化的参考Jarvis原型实例化该基准，并在OpenClaw中执行的34个纯文本WildClaw任务上进行了评估。使用GPT-5.5、Claude Opus 4.7、基于Gemini和基于GPT的工作代理的初步结果表明，Jarvis式中介能够为用户问题提供基于轨迹的答案，并在适当时刻注入稀疏用户指导时提高任务性能。结果还表明，有效性强烈依赖于中介的LLM大脑，凸显了这一缺失中间层的潜力以及需要更广泛的社区努力。演示页面 https://cchen1436.github.io/jarvis

## Abstract
Long-horizon AI agents are becoming increasingly capable, yet their interaction with users remains surprisingly thin. In most workflows, users give an initial instruction, receive only selective textual updates, and lose a clear sense of what the agent is doing or when to step in. This leaves a missing part in the current agent ecosystem: an always-on Jarvis-style mediator that keeps the agent continuously reachable to the user. Such a mediator should support real-time spoken interaction with the user, answer questions without interrupting the worker, proactively report progress or confusion, and inject user guidance back into the agent's execution when useful. In this work, we introduce JarvisBench, a benchmark for measuring the dual value of mediation in long-horizon agent workflows. JarvisBench contains two complementary tracks: an agent-collaboration track that measures whether mediation improves downstream task completion, and a user-interaction track that measures whether mediation makes ongoing execution more understandable, responsive, and accessible to users. We instantiate the benchmark with a modular reference Jarvis prototype and evaluate it on 34 text-only WildClaw tasks executed in OpenClaw. Preliminary results with GPT-5.5, Claude Opus 4.7, Gemini-based, and GPT-based worker agents suggest that Jarvis-style mediation can provide trace-grounded responses to user questions and improve task performance when sparse user guidance is injected at appropriate moments. The results also show that effectiveness depends strongly on the mediator's LLM brain, highlighting both the promise of this missing middle layer and the need for broader community effort. Demo page https://cchen1436.github.io/jarvis

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前长期规划（long-horizon）AI 代理的能力不断增强，但与用户的交互模式仍然单薄。用户通常只能给出初始指令、接收零星的文本更新，无法实时了解代理正在做什么、是否遇到困难、是否偏离意图，也无法在关键时刻进行干预。
- **整体含义**：论文指出现有代理生态系统缺少一个**持续在线的语音交互中介层**（类似钢铁侠中的 J.A.R.V.I.S.），该中介应能实时监控工作代理的执行轨迹、主动向用户报告进度或困惑、回答用户问题，并在适当时刻将用户指导注入回代理执行中。为此，论文提出了 **JarvisBench** 基准来系统评估这种中介的价值，并构建了模块化的参考原型。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将 Jarvis 式的中介定义为一个**即插即用的可评测接口**，而非固定架构。中介位于用户和长期工作代理之间，可以观察工作代理的 ReAct 轨迹，通过语音与用户通信，并可选地将用户反馈注入工作代理的上下文。
- **关键技术细节**：
  - **双轨评估设计**：
    - **Agent‑Collaboration Track（代理协作轨道）**：衡量中介能否通过识别失败敏感检查点（如重复工具故障、进展停滞、模糊观察、风险操作）并请求专家用户提供简洁指导，从而提高工作代理的任务完成度。中介不独立解决问题，仅作为通信桥梁。
    - **User‑Interaction Track（用户交互轨道）**：衡量中介能否在代理执行期间为非专家用户提供实时、基于轨迹的解释和回答。用户会交替提出进度导向和主题导向的问题，由中介给出答案并评价其质量。
  - **Jarvis 参考原型**：
    - **级联全双工语音接口**：使用流式 ASR 和语义 VAD（SoulX‑Duplug）判断用户是否说完，用 Chatterbox TTS 生成语音回复。支持始终在线监听。
    - **轨迹监控与上下文管理**：分离对话上下文与执行上下文，分别存储用户‑中介交互历史和工作代理的最近动作、失败、当前计划等信息。
    - **交互检查点机制**：在特定时刻（如失败状态、按计划触发）暂停代理执行，让用户提问或提供指导，之后再恢复代理并注入反馈。该机制用于两个轨道中，但触发策略不同（用户交互轨道为按计划触发，代理协作轨道为失败敏感触发）。

### 3. 实验设计：数据集、基准、对比方法

- **任务套件**：使用 **WildClawBench**（Ding et al., 2026a）中的 34 个纯文本任务，在 OpenClaw 环境中执行。这些任务涵盖 Productivity Flow、Code Intelligence、Social Interaction、Search and Retrieval、Creative Synthesis、Safety Alignment 六大类别，要求多步规划、工具协调、错误恢复和安全感知决策。
- **基准**：JarvisBench 本身即为提出的基准，包含两个评估轨道。
- **对比方法**：
  - **代理协作轨道**：将同一工作代理在两种设置下对比：原始 OpenClaw 基线 vs. 添加 Jarvis 中介。工作代理包括 GPT‑5.4‑mini、GPT‑5.5、Gemini‑3.1‑Pro、Claude Opus 4.7。Jarvis 大脑使用 GPT‑5.4，专家用户模拟器也使用 GPT‑5.4。
  - **用户交互轨道**：固定工作代理为 GPT‑5.4‑mini，变化 Jarvis 大脑（Qwen3‑30B‑A3B‑Instruct、Qwen3‑32B‑Thinking、GPT‑OSS‑120B、GPT‑5.4‑mini、GPT‑5.4），评估用户交互质量。
- **评价指标**：
  - 代理协作：Overall Score（任务完成百分数）、相对增益 Δ、满分任务数、干预次数。
  - 用户交互：Progress 评分（1‑5）、Topic 评分（1‑5）、Latency（语音回复延迟）。

### 4. 资源与算力

论文中**未明确说明使用的 GPU 型号、数量或训练时长**。仅提到使用了商业模型（GPT‑5.5、Claude Opus 4.7、Gemini‑3.1‑Pro 等）的 API，以及一些开源模型（Qwen3 系列、GPT‑OSS‑120B）。参考原型中的 ASR/TTS 组件（SoulX‑Duplug、Chatterbox）也未提供具体推理硬件信息。因此无法总结算力需求。

### 5. 实验数量与充分性

- **实验数量**：
  - 静态评估 34 个 WildClaw 纯文本任务。
  - 代理协作轨道：测试了 4 种工作代理，每种有 / 无 Jarvis 两种设置，共 8 组对比。每组报告指标，表中还有干预次数。
  - 用户交互轨道：5 种 Jarvis 大脑，固定工作代理，共 5 组。每组报告 Progress、Topic 评分及 Latency。
  - 附录还提供了多个定性案例分析（如恢复失败、防止覆盖、强制执行约束等）。
- **充分性与公平性**：
  - **优点**：任务来自成熟的 WildClaw 基准，具有一定真实性和难度；利用 LLM 模拟专家/非专家用户，保证可复现性；使用标准化判断协议（LLM‑judge 提示词）评估答案质量。
  - **局限**：
    - 仅使用 34 个纯文本任务，任务数量偏少，且排除了多模态任务，覆盖范围有限。
    - 用户模拟器可能无法完全反映真实用户的行为（例如专家用户的指导总是简洁且正确）。
    - 未进行鲁棒性测试（如不同噪音水平、打断、多轮复杂对话）。
    - 仅进行了初步结果，论文明确表示是“preliminary results”，需要更多基准和社区验证。

### 6. 论文的主要结论与发现

- **代理协作轨道**：Jarvis 中介对所有测试的工作代理均带来提升。最大增益出现在 Gemini‑3.1‑Pro（+11.64%）和 Claude Opus 4.7（+11.78%）；GPT‑5.5 和 GPT‑5.4‑mini 也有提升。干预次数较少（18~44 次之间），说明改进不是靠接管工作，而是靠识别高质量干预点并注入简洁反馈。
- **用户交互轨道**：Jarvis 大脑的能力显著影响用户体验。GPT‑5.4 在 Topic 答案质量上最优；GPT‑5.4‑mini 在 Progress 答案上与 GPT‑5.4 相当；GPT‑OSS‑120B（开源模型）在 Progress 上表现突出但 Topic 较差；Qwen 系列延迟低但答案质量偏低。整体表明中介层不只是一个语音接口问题，还需要强大 LLM 处理不断变化的执行上下文。
- **中介层的价值**：中介可以增强代理透明度（让用户了解进度、回答主题问题）并提升任务完成度（通过稀疏指导帮助代理摆脱局部失败模式）。但效果高度依赖中介大脑的能力。

### 7. 优点：方法或实验设计上的亮点

- **问题新颖且实用**：识别出现有代理工作流中缺失的“用户‑代理持续通信层”，并系统化设计和评估。
- **双轨设计**：既衡量任务完成（实用）又衡量用户体验（可用），提供全面的评估视角。
- **即插即用协议**：支持组件级替换（LLM 大脑、语音模块、监控策略、工作代理），便于社区贡献和对比。
- **模拟用户+标准化判断**：使用 LLM 模拟用户，通过 LLM‑judge 打分，增强可复现性和公平性。
- **任务选择合理**：基于 WildClaw 的文本任务，确保任务的长程性和真实工具使用，同时避免多模态干扰。
- **定性案例**：在附录中提供了多个具体实例（如恢复失败源、防止文件覆盖、强制执行约束），增加了结果的可解释性和说服力。

### 8. 不足与局限

- **任务数量与类型**：仅 34 个文本任务，排除多模态任务，任务覆盖不够全面，可能限制泛化结论。
- **用户模拟偏差**：模拟用户行为（专家用户的指导总是精确且有益）在现实中很少成立；真实用户可能提供模糊、冲突甚至有害的反馈。
- **缺乏真实人机交互实验**：所有用户交互均基于 LLM 模拟，未与真实人类用户进行测试，无法验证中介在实际使用中的效果和延迟可接受度。
- **实验充分性有限**：仅进行了一组对照实验（有/无中介），未进行不同监控策略、不同检查点频率、不同提示设计的消融实验。未测试中介自身失败或引入错误的情况。
- **资源与复现**：未公开代码或模型，仅提供演示页面（暂无实际内容？只给了 demo page 链接但未验证），影响可复现性。
- **缺少统计显著性检验**：未报告多次重复实验的标准差或置信区间，结果可能受随机性影响较大。
- **未探索混合架构**：论文虽提到强 omni 模型是未来方向，但实验仅使用级联语音管道，未直接与 omni 模型对比。

（完）
