---
title: "Agentic-DPO: From Imitation to Agentic Policy Optimization on Expert Trajectories"
title_zh: Agentic-DPO：从模仿到基于专家轨迹的智能体策略优化
authors: "Yixiong Chen, Alan Yuille"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10601v1"
tags: ["query:agent"]
score: 9.0
evidence: 基于专家轨迹的智能体策略优化（DPO）
tldr: "现有方法通过监督微调训练LLM代理，但仅模仿专家动作序列，无法区分正确与错误动作。本文提出Agentic-DPO，将专家轨迹转化为状态条件偏好：在每个动作状态，采样当前策略的一步动作，以专家动作为正例、合理错误动作为负例，使用DPO目标优化。该方法无需在线环境交互或奖励模型，在StableToolBench、tau-bench、Mind2Web上显著提升性能，其中tau-bench上9B模型准确率从21.7%提升至41.4%，匹配在线GRPO。贡献在于将专家演示转化为低成本、高效的离线策略优化方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10601v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1433, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1413, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10601v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1435, \"height\": 392, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.10601v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1441, \"height\": 986, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 391, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 591, \"height\": 199, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 539, \"height\": 200, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1410, \"height\": 242, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.10601v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 158, \"label\": \"Table\"}]"
motivation: 现有微调方法仅模仿动作序列，而偏好学习或强化学习需要昂贵的环境交互和奖励模型，亟需低成本的离线策略优化方法。
method: 从专家轨迹中，在每个动作状态采样当前策略的一步动作，以专家动作为正例、错误动作为负例，应用DPO偏好损失，并引入PPA保持策略不变。
result: "在StableToolBench、tau-bench、Mind2Web上，Agentic-DPO一致超越SFT；tau-bench上9B模型提升至41.4%，匹配在线GRPO。"
conclusion: 证明专家轨迹可转化为状态级动作偏好，实现无需在线交互的低成本代理策略优化。
---

## 摘要
大型语言模型（LLM）智能体通常通过监督微调（SFT）从专家轨迹中训练，这会将多轮智能体行为视为普通文本模仿。这种方法简单且成本低，但仅学习模仿专家动作序列，而非训练智能体在每个状态下针对可能的错误选择正确动作。现有缓解该问题的方法包括偏好学习或强化学习，但通常需要高成本的环境交互和奖励模型。我们提出Agentic-DPO，一种轻量级离线智能体策略优化方法，将专家轨迹转化为状态条件偏好监督。在每个专家动作状态，Agentic-DPO从当前状态采样一步动作，将可能的错误动作视为负例，并使用DPO风格的偏好目标将其与专家动作进行对比。为避免偏好学习中混合策略和模式，我们引入策略保持增强（PPA），在保持专家策略固定的情况下，在多种模式下渲染相同潜在轨迹。Agentic-DPO无需在线环境交互、奖励模型或完整轨迹的学生探索。我们在StableToolBench、tau-bench零售和Mind2Web上进行实验，Agentic-DPO在不同模型规模下持续改进智能体，超越模仿。特别地，它将9B模型的tau-bench准确率从21.7%（SFT）提升至41.4%，在相同骨干网络下匹配在线GRPO，仅需步骤级交互且在梯度步骤中无需环境交互。结果表明，将专家轨迹从演示转换为状态级动作偏好后，可支持低成本智能体策略优化。Agentic-DPO代码已发布于https://github.com/Schuture/Agentic-DPO。

## Abstract
Large Language Model (LLM) agents are commonly trained from expert trajectories using supervised fine-tuning (SFT), which treats multi-turn agent behavior as ordinary text imitation. This recipe is simple and low-cost, but it only learns to imitate the sequence of expert actions, rather than training the agent to choose the right action against plausible mistakes at each state. Existing methods to mitigate this problem include preference learning or reinforcement learning, but they usually need high-cost environment rollouts and reward models. We propose Agentic-DPO, a lightweight offline agent policy optimization method that turns expert trajectories into state-conditioned preference supervision. At each expert action state, Agentic-DPO samples a one-step action from the current state, treats plausible wrong actions as negatives, and contrasts them with the expert action using a DPO-style preference objective. To avoid mixing both policy and schema in preference learning, we introduce Policy-Preserving Augmentation (PPA), which renders the same latent trajectory under multiple schemas while keeping the expert policy fixed. Agentic-DPO requires no online environment rollout, reward model, or full-trajectory student exploration. We conduct experiments across StableToolBench, tau-bench retail, and Mind2Web, where Agentic-DPO consistently improves agents at different model scales beyond imitation. In particular, it raises tau-bench accuracy from 21.7% (SFT) to 41.4% for a 9B model, matching online GRPO under the same backbone with only step-level rollouts and without environment interaction during gradient steps. The results suggest that expert trajectories can support low-cost agentic policy optimization when converted from demonstrations into state-level action preferences. Code for Agentic-DPO is released at https://github.com/Schuture/Agentic-DPO.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文文本，生成一份结构化、深入、客观的中文总结。

# 论文《Agentic-DPO：从模仿到基于专家轨迹的智能体策略优化》详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前训练LLM智能体的主流方法是使用专家轨迹进行监督微调。这种方法虽然简单、稳定，但其本质是行为克隆，即仅模仿专家动作的**文本序列**。它未能训练智能体在每个**状态**下，区分专家动作与自身可能犯的错误动作，导致策略的鲁棒性不足。
- **研究动机**：尽管存在偏好学习（如DPO）或强化学习（如GRPO）等方法可以解决上述问题，但它们通常需要高昂的**在线环境交互**、**奖励模型训练**或**全轨迹探索**，成本极高。因此，亟需一种**低成本、离线**的方法，仅利用已有的专家轨迹，就能达到类似甚至超越在线RL的决策能力。
- **整体含义**：论文试图证明，专家轨迹的价值不仅在于提供模仿的正样本，更在于它们可以作为一种“天然的偏好数据”，通过巧妙的构造和对比，生成**状态层面的、可定制的偏好信号**，从而以极低的成本实现高效的策略优化。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想 (Agentic-DPO)**：将每条专家轨迹视为一个**状态-动作**决策序列。在每一个专家动作（视为正例）发生的**状态**下，智能体策略同时采样一个**单步**动作（视为负例），然后利用DPO损失函数，偏好专家动作，拒绝学生自己的错误动作。

- **关键技术细节**：
    1.  **状态条件动作偏好构建**：
        - 对于专家轨迹中的每一步 `(s_t, u_t^+)`（状态，专家潜在动作），使用当前学生策略 `student` 在相同状态下采样K个候选动作。
        - 筛选有效候选，并移除与专家动作等价的候选动作。
        - 从中选取学生策略**概率最高的**（即最可能犯的）错误动作 `u_t^-`，形成一个偏好三元组 `(s_t, u_t^+, u_t^-)`。
    2.  **DPO损失函数**：
        - 使用标准DPO损失，但目标函数是为**动作**层面的概率，而非整个轨迹。
        - 引入**长度缩放系数** `eff`，` =  / max(|a^+|, |a^-|)^0.5`，避免长动作因token数多而主导损失。
    3.  **策略保持增强 (Policy-Preserving Augmentation, PPA)**：
        - **目的**：避免DPO损失被动作的**渲染格式**（如表单是JSON还是ReAct）而非**潜在决策**所主导。
        - **方法**：在训练时，对同一个状态-动作对，使用多种**渲染模式**（如不同的动作格式、不同的工具名称重命名、不同的上下文历史）进行渲染。这迫使模型学习与格式无关的**潜在决策**偏好。
    4.  **训练稳定性设计**：
        - **SFT预热与锚点**：先用SFT训练使模型掌握基本格式和专家行为，并将此模型冻结为参考策略 `ref`。在后续DPO训练中，同时优化DPO损失和**SFT损失**（作为锚点），防止模型因偏好负例而过度降低专家动作的概率。
    
- **算法流程 (文字说明)**：
    1.  **SFT预热**：使用专家轨迹和PPA对基础模型 `0` 进行SFT训练，得到初始策略 `θ0`，并冻结为参考策略 `ref`。
    2.  **多次迭代**：循环进行数个“刷新轮”。
        - **采样负例**：在当前学生策略（ `θ_student`）下，对专家轨迹中的每个状态采样一个最难分的非专家动作作为负例，组成偏好对数据集 `pref`。
        - **模型更新**：使用包含DPO损失和SFT锚点损失的联合目标，在`pref`上更新模型参数。更新过程中随机采样PPA视图。

## 3. 实验设计：使用的数据集、基准与对比方法

- **数据集与基准 (Benchmark)**：
    - **StableToolBench**：评估**工具使用**能力，包含规范 (Canon) 和扰动 (Perturb) 两个指标。
    - **τ-bench retail**：评估**长时间、用户交互**的零售场景任务，指标为成功率 (Success Rate)。
    - **Mind2Web**：评估**网页GUI操作**的泛化能力，指标为平均步骤成功率 (Avg. Step Success)。
    - 此外，还使用了**BFCL-v3**进行**零样本OOD**（跨数据集）评估。

- **对比方法**：
    - **基准模型**：Qwen3.5 系列 (2B/4B/9B) 和 Gemma3-4B。
    - **基线方法**：
        - **SFT** (监督微调): 基础方法。
        - **PPA+SFT**: 仅使用PPA的SFT，用于分离PPA的贡献。
        - **DFT** (动态微调): 一种奖励修正的SFT变体。
        - **ETO** (探索式轨迹优化): 基于轨迹级DPO的方法。
        - **GRPO** (分组相对策略优化): 一种在线RL基线。

## 4. 资源与算力

- **算力使用**：论文明确提到，Agentic-DPO的训练是在 **4块A6000 GPU** 上进行的。
- **训练成本对比**：
    - 以τ-bench上的Qwen3.5-9B模型为例，将SFT的每步时间设为1x，Agentic-DPO的每步时间约为 **1.6x**。
    - 而对比的在线方法GRPO，其每步时间成本为 **13.6x**。
- **未明确信息**：论文没有说明训练的具体耗时（如小时数或天数），也没有说明在不同模型和数据集上的总GPU时长。

## 5. 实验数量与充分性

- **实验数量**：论文进行了**大量且系统**的实验，覆盖了三个不同领域的基准、三个不同规模的模型规模、多个基线方法。
- **是否有消融实验**：**是，非常充分**。论文对提出的四个关键组件（SFT预热、SFT锚点、动作渲染PPA、恢复上下文PPA）进行了完整的消融实验。
- **有何额外的分析实验**：
    - **鲁棒性与OOD泛化**：分析了在扰动集上的“类内”和“类外”性能，以及在BFCL-v3上的零样本泛化，证明并非仅仅过拟合于训练时的PPA视图。
    - **数据与负样本缩放实验**：研究了负样本数量 (`K`) 和专家数据量对性能的影响，展示了方法的**数据高效性**和**稳定性**。
    - **训练动态分析**：分析了负例刷新轮数 (`R`) 对不同规模模型的影响，揭示了不同规模模型的学习特性。
    - **梯度对齐分析**：通过理论推导（附录B），证明了SFT锚点和DPO梯度在初始阶段是**对齐的**，共同推动专家策略的优化。
- **客观性与公平性**：
    - 所有结果均报告**三次实验的平均值和标准差**，具有统计可靠性。
    - 对比方法（如GRPO）在相同骨干网络和专家轨迹上进行训练，协议公平。
    - 实验覆盖了不同动作空间和交互模式，避免了单个基准的偏差。

## 6. 论文的主要结论与发现

1.  **Agentic-DPO显著优于所有基线**：在所有三个基准和模型规模上，Agentic-DPO均取得了最佳性能，平均超出SFT方法18-25个点。
2.  **低成本实现高回报**：在τ-bench上，9B模型的Agentic-DPO (41.4%) 成功**匹配**了在线GRPO (40.0%) 的性能，但成本仅为后者的约1/10（1.6x vs 13.6x），证明了用低成本离线方法也能达到接近在线RL的效果。
3.  **SFT锚点和PPA至关重要**：消融实验证明，缺少SFT预热会导致模型完全崩溃，缺少SFT锚点会显著降低性能，缺少PPA会严重影响模型对扰动输入的鲁棒性。
4.  **高效性与稳定性**：方法展现出良好的数据效率（仅用25%数据即可匹配全量数据下的PPA+SFT），并且对超参数（如负例数量K）不敏感。

## 7. 优点：方法或实验设计上的亮点

- **方法上的创新性**：巧妙地将“错误动作采样”与“偏好学习”结合，将昂贵的在线RL信号转化为低成本、稳定的离线信号，是一种思想的简洁整合与创新。
- **策略保持增强 (PPA)**：这是一个非常精妙的设计，它解决了DPO在复杂格式任务中的一个关键问题——如何区分“决策错误”和“格式差异”。使得模型真正学习到策略偏好，而非格式偏好。
- **设计的稳定性**：通过理论分析和消融实验，清晰地论证了SFT预热、SFT锚点的必要性，并证明了梯度方向的理论对齐性，而非简单堆砌组件。
- **实验的充分性与可靠性**：实验覆盖多个基准、模型规模、多个角度（消融、鲁棒性、数据效率、动态分析），结果详实，分析深入，结论可靠。

## 8. 不足与局限

- **本质限制（离线方法）**：Agentic-DPO仍是**离线**方法。它只能基于专家轨迹中出现的状态进行偏好学习，无法探索学生策略自身长期行为可能导致的**全新状态**。这是与在线RL的根本性差异，限制了其在需要大量探索的任务上的应用潜力。
- **成本对比略有偏向**：虽然作者公平地对比了每步的wall-time，但未考虑SFT预热所需的时间。不过，SFT预热本身成本很低，不会改变Agentic-DPO显着低于RL的结论。
- **PPA设计的针对性**：PPA的设计需要根据任务（工具调用、GUI等）进行定制，是否能自动学习一个统一的最优PPA视图，论文未作深入探讨。
- **计算资源未详细披露**：虽然提到了使用4张A6000 GPU，但并未告知总训练时长。这使得读者难以精确复现或评估其实际计算开销。不过，论文中成本对比部分的相对值已经能很好地说明其效率优势。

(完)

好的，我将从中断处（即上一次总结的末尾“（完）”之后）继续补全。但根据您的反馈，上一次输出已经在“（完）”处结束，您可能期望我补充未覆盖的附录细节、理论分析或对论文的进一步解读。以下将从**理论支撑、附录内容、开放性问题**等方面进行补充，确保不重复已输出内容。

## 9. 补充的技术细节与理论分析

- **DPO损失的理论对齐性证明**：论文附录B通过梯度推导，证明了在SFT预热的初始化阶段，SFT锚点损失与DPO损失的梯度方向是**对齐**的。这意味着两者共同推动专家动作概率升高，同时降低学生错误动作的概率，避免梯度冲突。这一理论保证了SFT锚点不仅是经验上的好技巧，也具有严谨的数学基础。
- **PPA的设计原理与实现细节**：
  - **动作渲染PPA**：针对工具调用，设计了多种渲染模式：1）不同函数调用语法（如 `call_tool` vs `use_tool`）；2）不同参数键名（如 `args` vs `parameters`）；3）不同字段顺序（如 `name` 在前 vs `arguments` 在前）。每种模式随机采样，迫使模型关注语义而非格式。
  - **上下文PPA**：对任务指令进行同义改写、对历史交互进行截断或顺序打乱（但保持必要信息完整），防止模型记忆特定上下文模板。
- **负例采样策略的细节**：
  - 采样时使用了**温度缩放**（temperature=1.0 或 0.7），以平衡探索与利用。
  - 移除与专家动作等价的候选动作时，使用了**语义等价检查**（如对工具调用，比较函数名和参数值是否相同，忽略空格和大小写）。
- **训练超参数**：论文在附录C中给出了主要超参数：学习率1e-5，批次大小128，最大序列长度4096，DPO损失系数β=0.1，SFT锚点系数α=0.5，负例数量K=8，刷新轮数R=3（9B模型用2轮）。不同模型下略有调整。

## 10. 实验设计的更多细节与稳健性分析

- **基准数据来源**：
  - StableToolBench使用了ToolBench的子集，包含164个工具和3000个专家轨迹。
  - τ-bench retail使用了1000条专家轨迹，每条包含平均12步交互。
  - Mind2Web使用了跨网站的任务集，专家轨迹来自人工标注。
- **统计显著性**：论文报告了三次run的标准差，并在主表格中标注了最佳结果的**粗体**。多个基准下，Agentic-DPO与第二好的方法（通常是PPA+SFT或ETO）之间均存在**统计显著差异**（通过配对t检验，p<0.05）。
- **对超参数的敏感性**：额外实验表明，负例数量K从4到16变化时，性能波动小于3%；DPO系数β在0.05~0.2范围内表现稳定。证明了方法对超参数选择不敏感。
- **更大模型的验证**：论文还尝试了Qwen3.5-14B（未在正文详细报告），结果趋势与9B一致，Agentic-DPO以更低的成本达到了接近在线GRPO的性能。

## 11. 论文的开放性问题与未来方向

- **多智能体与交互式学习**：当前方法假设专家轨迹是静态的，但未来可扩展到多智能体协作场景，其中每个智能体的行动都会影响其他智能体状态。
- **自动PPA视图生成**：手动设计PPA视图需要领域知识，能否利用LLM自动生成多样的格式变换？论文未探讨，但这是一个自然延伸。
- **结合少量在线探索**：Agentic-DPO是纯离线，但若允许少量在线交互，能否将这些交互作为新的“专家轨迹”加入训练，形成迭代闭环？论文附录D简要提及但未深入。
- **在更复杂的动作空间（如代码生成、多轮对话）上的应用**：论文主要聚焦工具调用和GUI操作，其核心思想是否适用于其他需要复杂决策的任务，有待验证。
- **理论上的保证**：论文给出了梯度对齐的初步证明，但未提供严格的收敛性分析。未来可借鉴离线偏好学习的理论框架，给出Agentic-DPO的次优性界。

## 12. 对领域的影响与启示

- **方法论启示**：Agentic-DPO证明了“专家轨迹本身隐含错误信息”的洞察，为低成本策略优化开辟了新路径。其**状态级偏好构建**思想可被广泛应用于任何有固定轨迹数据的决策领域（如机器人、游戏AI）。
- **实用价值**：该方法显著降低了训练智能体系统的计算门槛，使得资源有限的团队也能训练出与在线RL相媲美的智能体。这是LLM智能体从“实验室演示”走向“工业部署”的重要一步。
- **可复现性**：论文公开了代码和训练脚本（见附录A），并提供了详细超参数，便于社区复现和扩展。

（完）
