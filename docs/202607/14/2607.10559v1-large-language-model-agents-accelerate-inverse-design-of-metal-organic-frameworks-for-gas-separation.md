---
title: Large language model agents accelerate inverse design of metal-organic frameworks for gas separation
title_zh: 大语言模型智能体加速用于气体分离的金属有机框架的反向设计
authors: "Zhaolin Hu, Hehe Fan, Wangyihan Guo, Meng Xu, Chenhao Rao, Qiwei Yang, Yi Yang"
date: 2026-07-12
pdf: "https://arxiv.org/pdf/2607.10559v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于MOF逆向设计的LLM代理框架
tldr: 金属有机框架（MOF）的可逆设计面临化学有效性、分离性能和结构多样性的协同约束。本文提出LEMO Agent，一种基于大语言模型的智能体框架，在MOFid空间中进行闭环逆设计，通过语言生成候选、有效性检查、Transformer预测、设计记忆与多岛探索的迭代循环。在CH4/N2和CO2/N2分离任务中，相比基线方法，该代理富集了更多高性能候选，提升了预测分离性能，并保持了广泛的化学与拓扑多样性。经GCMC模拟和实验初筛验证，该工作展示了LLM智能体作为可解释、可扩展的设计引擎加速MOF发现的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.10559v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1503, \"height\": 1113, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10559v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1585, \"height\": 904, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10559v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1566, \"height\": 1332, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10559v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1612, \"height\": 1449, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.10559v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1597, \"height\": 1709, \"label\": \"Figure\"}]"
motivation: 克服传统固定库筛选的局限，实现兼顾化学有效性、分离性能和结构多样性的MOF逆向设计。
method: 基于大语言模型智能体，通过MOFid标准化、有效性检查、Transformer预测、设计记忆与多岛探索的迭代生成-验证-评估-记忆循环。
result: 在CH4/N2和CO2/N2分离中，LEMO Agent生成更多高性能候选，预测分离性能优于基线，结构多样性高，并经GCMC和实验验证。
conclusion: 大语言模型智能体可作为加速MOF发现的可解释且可扩展的设计引擎。
---

## 摘要
金属有机框架（MOFs）为吸附式气体分离提供了一个高度模块化的平台，然而其庞大的网状设计空间使得在化学有效性、分离性能和结构多样性的多重约束下进行反向设计变得困难。在此，我们提出LEMO Agent，一个用于在MOFid空间中进行气体分离MOF闭环反向设计的大语言模型智能体框架。LEMO Agent将基于语言的候选生成与MOFid标准化、显式有效性检查、基于Transformer的性能预测、结构化设计记忆以及多岛屿探索相结合。通过迭代的生成-验证-评估-记忆循环，该智能体利用成功和失败候选的反馈来指导跨连接体、金属和拓扑选择的化学约束搜索。我们在CH₄/N₂和CO₂/N₂分离任务上评估了LEMO Agent。与代表性的生成、优化和智能体基线相比，LEMO Agent丰富了高性能候选，提高了预测分离性能，并保持了广泛的化学和拓扑多样性。选定的候选结构进一步重建，通过GCMC模拟评估，并经过基于化学可行性和配体可购买性的实验筛选流程，最终进行初始湿法合成和SEM表征。这些结果表明，大语言模型智能体可以作为可解释且可扩展的设计引擎，加速超越传统固定库筛选的MOF发现。

## Abstract
Metal-organic frameworks (MOFs) offer a highly modular platform for adsorptive gas separation, yet their vast reticular design space makes inverse design difficult under simultaneous constraints of chemical validity, separation performance, and structural diversity. Here, we present LEMO Agent, a large-language-model agent framework for closed-loop inverse design of gas-separation MOFs in MOFid space. LEMO Agent couples language-based candidate generation with MOFid standardization, explicit validity checking, Transformer-based property prediction, structured design memory, and multi-island exploration. Through iterative generate--validate--evaluate--remember cycles, the agent uses feedback from both successful and failed candidates to guide chemically constrained search across linker, metal, and topology choices. We evaluate LEMO Agent on CH$_4$/N$_2$ and CO$_2$/N$_2$ separation tasks. Compared with representative generative, optimization, and agentic baselines, LEMO Agent enriches high-performing candidates, improves predicted separation performance, and maintains broad chemical and topological diversity. Selected candidates are further reconstructed, evaluated by GCMC simulations, and passed through an experimental down-selection workflow based on chemical feasibility and ligand purchasability, leading to initial wet-lab synthesis and SEM characterization. These results demonstrate that large language model agents can serve as interpretable and scalable design engines for accelerating MOF discovery beyond conventional fixed-library screening.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，以下是根据您提供的论文内容生成的结构化、深入且客观的中文总结。

## 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**：金属有机框架（MOF）是一类极具潜力的气体分离材料，但其设计空间极其庞大（包含金属节点、有机连接体、拓扑结构等的无数组合）。传统的**高通量筛选**受限于预设的数据库，无法探索空间之外的新结构；而现有的**深度生成模型**在有效性、多样性和目标性能优化之间难以取得平衡。因此，如何解决在化学有效性、分离性能和结构多样性的多重约束下进行**高效、自适应的反向设计**，是一个核心难题。
*   **研究动机**：最近，大语言模型（LLM）展现了结合化学知识、文献规则和过往尝试进行科学设计的能力。论文的动机是利用LLM的这种能力，将其从一次性序列生成器转变为一个能够迭代、学习和优化的**闭环材料设计智能体**，从而加速MOF的发现过程，突破传统方法的局限性。

## 2. 论文提出的方法论：LEMO Agent

### 核心思想
提出一个名为**LEMO Agent（LLM-Guided Evolutionary MOF Optimization Agent）** 的框架。它将MOF的反向设计建模为一个在**MOFid符号空间**中的闭环迭代优化过程。核心思想是将LLM作为可自适应进化的核心引擎，通过**生成-验证-评估-记忆**的循环，结合结构化记忆和多岛屿探索策略，引导搜索向高性能区域收敛，同时保持结构多样性。

### 关键技术细节
1.  **设计表示**：使用**MOFid**字符串作为MOF的紧凑文本表示，编码了有机连接体、金属单元和拓扑信息。这为LLM提供了天然的文本接口。
2.  **闭环迭代流程**：
    *   **规划 (Planning)**：根据目标、设计记忆（包括成功/失败案例）和领域知识，制定下一步搜索策略。
    *   **生成 (Generation)**：LLM根据规划策略和当前记忆，提出一批新的MOFid候选。
    *   **验证 (Validation)**：对生成的MOFid进行严格的语法检查和化学有效性验证（如使用RDKit）。
    *   **评估 (Evaluation)**：使用预训练的、基于Transformer的**快速代理预测器**（Surrogate Predictor）对有效候选进行性能评估（如选择性、工作容量）。
    *   **记忆更新 (Memory Update)**：将评估结果（成功/失败案例）结构化地存入**成功记忆**、**失败记忆**和**知识记忆**中，用于指导后续迭代。
3.  **结构化记忆**：将反馈信息分为三类：
    *   **知识记忆**：存储目标、MOFid语法、领域知识。
    *   **成功记忆**：存储高性能和有用的结构模式。
    *   **失败记忆**：存储无效或低性能的候选，用于避免重复错误。
4.  **多岛屿探索 (Multi-Island Exploration)**：同时运行多个独立的“岛屿”，每个岛屿拥有**独立的记忆**和搜索策略。通过一个基于性能的**概率选择机制**，在岛屿间进行切换，既能利用高绩效岛屿的模式，又能保留低绩效岛屿的多样性探索，防止过早收敛。
5.  **评分函数**：采用**阈值优先策略**，候选只有其预测选择性超过任务特定阈值才算成功，最终得分是选择性和工作容量的加权和。

### 公式与算法流程（文字说明）
1.  在每个迭代 `k`，规划器 `Planner` 根据当前记忆 `Mk` 生成计划。
2.  生成器 `Generator` 根据计划生成一批 `B` 个候选MOFid。
3.  验证器检查MOFid的有效性，**无效的直接跳过**。
4.  对于有效候选 `x`，预训练的预测器 `f` 评估其性能 `ŷ(x)`。
5.  根据阈值 `τ` 和评分函数 `S(x)`（公式8）计算最终得分。
6.  记忆模块根据结果更新成功/失败记忆。
7.  多岛屿模块根据各岛屿的累积得分（公式12）和概率选择公式（公式13-14）决定下一个迭代由哪个岛屿进行。

## 3. 实验设计

### 数据集与场景
*   **任务**：两个代表性的气体分离任务：**CH₄/N₂分离**（约110,000个MOF）和**CO₂/N₂分离**（约220,000个MOF，来自ARC-MOF数据集）。
*   **参考数据**：约200个来自文献的实验合成MOF，用于领域知识引导和后续分析。
*   **验证数据**：通过GCMC模拟对生成的代表性候选进行高保真度验证。
*   **应用场景**：最终的实验合成流程，对候选进行湿法合成和SEM表征。

### 评估指标
*   **性能指标**：预测的**选择性**和**工作容量**。
*   **多样性指标**：生成的MOFid字符串之间的平均成对距离。
*   **物理合理性**：预测的孔限制直径（PLD）和框架密度。
*   **合成可行性**：配体的合成可及性（SA Score）和合成复杂度（SCScore）。

### 对比方法（Baselines）
*   **LLM-only**：一次性生成，无迭代和反馈。
*   **Vanilla Agent**：有迭代生成和性能反馈，但**无结构化记忆**和**多岛屿探索**。
*   **LEMO w/o MI**：有迭代和结构化记忆，但**去除了多岛屿机制**。
*   **MOFGPT**：基于强化学习的MOF专用生成模型，基线方法通过与LEMO Agent相同的预测器进行优化。

## 4. 资源与算力

*   **明确提到的信息**：
    *   **硬件**：实验运行在配备**8块NVIDIA GeForce RTX 4090 GPU**的Ubuntu Linux系统上。
    *   **软件框架**：使用Python实现，预测器模型基于PyTorch，LLM通过OpenAI API（GPT-5.1）访问。
*   **未明确说明**：论文没有提供**具体的训练时长**（如多少GPU小时）或**总的推理成本**（如API调用次数），也未说明Transformer预测器的具体训练时间。

## 5. 实验数量与充分性

*   **实验数量**：实验设计相对系统化。
    *   **主实验**：在两个分离任务（CH₄/N₂, CO₂/N₂）上对4种基线方法（LLM-only, Vanilla, w/o MI, MOFGPT）进行了定量比较。
    *   **消融实验**：通过对比 `LEMO w/o MI`，明确验证了“多岛屿机制”的贡献。
    *   **轨迹分析**：可视化展示了优化过程中性能、有效性和Pareto前沿的演变。
    *   **多维评估**：除了性能，还从物理合理性、多样性、合成可行性等多个角度进行了分析。
    *   **高级验证**：通过GCMC模拟和初步湿法实验对结果进行验证。
*   **充分性与公平性**：
    *   **优点是**：实验设计较为全面，考虑了**性能、多样性、物理合理性**等多个维度，并通过GCMC和湿法实验进行了初步验证，增加了可信度。与多个有代表性的基线方法（包括生成式、优化式和智能体式）进行了公平对比，且所有方法都共享相同的预测器评估环境。
    *   **不足是**：实验主要在**两个**分离任务上进行，泛化性有待在更多任务（如烷烃/烯烃分离）上验证。与现有的更先进的专用化学生成模型（如扩散模型）未做对比。GCMC和实验验证的样本数量较少，更偏向于**展示可行性**而非严格的大规模验证。

## 6. 论文的主要结论与发现

1.  **LEMO Agent有效地优化了MOF设计**：在两个分离任务上，LEMO Agent显著优于所有基线方法，产生了预测性能更高、多样性更好的候选MOF。
2.  **结构化记忆和多岛屿探索是关键**：消融实验表明，删除多岛屿机制会同时损害性能和多样性，证明了其在维持搜索效率和广度之间的平衡中扮演关键角色。
3.  **生成的MOF兼具高潜力和实用价值**：这些候选不仅在性能上优秀，而且其预测的物理性质（如孔径、密度）与已知MOF相符，配体合成难度较低，化学多样性广，表明它们并非是不切实际的“幻觉”结构。
4.  **从符号设计到实体材料的可行路径**：通过GCMC模拟和（有限的）湿法合成实验，证明了LEMO Agent生成的MOF候选可以被成功构建、模拟并初步合成，验证了该框架的实际可行性。

## 7. 优点

1.  **创新的方法论**：巧妙地**利用MOFid文本表示作为桥梁**，将强大的LLM代理能力与结构化、物理约束的MOF设计流程相结合，形成了一个高度模块化和可解释的闭环系统。
2.  **有效平衡冲突目标**：通过多岛屿探索和阈值优先评分，该方法在**追求高性能的同时，成功保持了化学多样性**，避免了过早收敛到单一模式，这是许多优化方法的痛点。
3.  **高度可解释和可扩展**：框架的每个模块（生成、验证、评估、记忆）都是独立的，易于理解和修改。此设计范式可以轻松扩展到其他多孔材料或分子系统。
4.  **实验验证链条完整**：工作不仅停留在计算模拟，而是从**预测到GCMC，再到实验合成**，构建了一个完整的验证链条，增强了结论的可靠性。

## 8. 不足与局限

1.  **对预测模型的强依赖**：整个优化过程依赖于预训练的Transformer预测器。预测器的准确性和泛化能力直接决定了生成结果的质量。如果预测器在某些区域出现系统性偏差，LEMO Agent可能也会在相同区域产生错误的高性能候选项。
2.  **MOFid表示的限制**：虽然MOFid是文本表示，但它仍是基于模板的，可能无法表示一些具有特殊结构或缺陷的MOF。生成的候选空间本质上受限于MOFid能编码的信息。
3.  **实验验证规模有限**：目前湿法合成和SEM表征只是初步验证，仅展示了少数几个候选的合成可能性。缺少**吸附等温线、分离性能（IAST选择性）** 等关键实验数据，其实际性能仍未知。
4.  **计算与API成本**：使用GPT-5.1等大型商业模型，其API调用成本和推理延迟较高，对于需要大量迭代的任务可能是一个瓶颈。论文未提供这方面的具体成本分析。
5.  **潜在的LLM幻觉风险**：尽管有验证步骤，但LLM在生成过程中可能组合出在化学上缺乏合理解释的“假”结构，或是依赖记忆中的常见模式，限制了真正的“发现”。

（完）
