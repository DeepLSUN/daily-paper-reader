---
title: "Toward cryptographically verifiable authorization for autonomous AI agents: A security hypothesis, preliminary formal model, and proof-of-concept implementation"
title_zh: 迈向自主AI代理的密码学可验证授权：安全假设、初步形式化模型及概念验证实现
authors: "M. Llambí-Morillas, D. Fernández-Fernández"
date: 2026-07-23
pdf: "https://arxiv.org/pdf/2607.21325v1"
tags: ["query:agent"]
score: 9.0
evidence: 自治AI智能体的可验证授权
tldr: 现有自主AI代理的认证授权机制缺乏密码学证据证明具体请求满足策略。本文提出密码学可验证代理授权（CVA）形式化模型，将授权关系绑定代理主体、请求、上下文和策略满足，并使用Groth16 zk-SNARK实现零知识证明概念验证。该原型满足授权可靠性、主体绑定、请求绑定等安全属性。论文进一步揭示身份绑定、请求绑定与运行时执行绑定的结构分离这一开放问题，并提出可证伪的研究方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.21325v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1715, \"height\": 139, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.21325v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1785, \"height\": 622, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.21325v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1793, \"height\": 622, \"label\": \"Table\"}]"
motivation: 当前认证授权机制无法为自主AI代理的请求提供策略满足的密码学证据，限制无人工监督场景的可审计性和安全性。
method: 提出CVA形式化模型，定义关系R_CVA绑定代理主体、请求、上下文与策略满足，并用Groth16 zk-SNARK实现零知识证明概念验证。
result: 概念验证原型实现了授权可靠性、主体绑定、请求绑定、策略绑定和防重放攻击等安全属性，并展示了密码学验证可行性。
conclusion: 身份绑定、请求绑定与执行绑定的结构分离是核心设计难题，本工作提供了可证伪的研究议程推动安全代理系统发展。
---

## 摘要
自主AI代理越来越多地在有限人类监督下执行操作、调用工具并操作受保护资源。现有认证和授权机制建立身份并委派权限，但本质上并未提供密码学证据，证明特定代理发出的具体请求在特定执行上下文中满足适用策略。本文假设代理授权可以形式化为一种密码学可验证关系，记为$R_{CVA}$，该关系联合绑定代理主体、具体授权请求、执行上下文以及适用策略的满足，同时有选择地保护私有授权属性的机密性。我们引入了密码学可验证代理授权（CVA）的初步形式化抽象，定义了一组紧凑的候选安全属性，包括授权可靠性、主体绑定、请求绑定、策略绑定和重放抵抗，并提供了一个可执行的零知识概念验证，该验证基于Groth16 zk-SNARK构造实例化了模型的选择性元素。我们进一步识别并形式化了身份绑定、授权请求绑定和运行时执行绑定之间的结构性分离，将其作为安全代理系统设计中的一个核心开放问题（当前代理安全框架未明确解决的区别），并提出一个可证伪的研究议程以解决该问题。

## Abstract
Autonomous AI agents increasingly execute actions, invoke tools, and operate on protected resources with limited human oversight. Existing authentication and authorization mechanisms establish identity and delegate authority, but do not inherently provide cryptographic evidence that a concrete request issued by a specific agent satisfies the applicable policy in a specific execution context. This paper hypothesizes that agent authorization can be formalized as a cryptographically verifiable relation, denoted $R_{CVA}$, that jointly binds an agent principal, a concrete authorization request, an execution context, and the satisfaction of an applicable policy, while selectively preserving the confidentiality of private authorization attributes. We introduce a preliminary formal abstraction for Cryptographically Verifiable Agent Authorization (CVA), define a compact set of candidate security properties including authorization soundness, principal binding, request binding, policy binding, and replay resistance, and provide an executable zero-knowledge proof of concept that instantiates selected elements of the model over a Groth16 zk-SNARK construction. We further identify and formalize the structural separation among identity binding, authorization-request binding, and runtime execution binding as a central open problem in the design of secure agentic systems (a distinction {not explicitly addressed by} current agentic security frameworks) and present a falsifiable research agenda for its resolution.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有自主AI代理的认证（Authentication）和授权（Authorization）机制（如OAuth 2.0、OpenID Connect、SAML）仅能验证“谁或什么是一个代理”或“凭证是否被委托”，但无法提供密码学证据，证明某个具体代理在特定执行上下文中发出的具体授权请求（包括操作、资源、上下文等）满足适用策略。
- **深层症结**：论文指出，传统IAM框架建立在确定性行为假设和单一认证主体假设之上，而自主AI代理具备动态上下文、多步动作序列和自主工具调用能力，导致“认证不等于授权，委托也不等于授权”。作者用形式化符号表达为：AuthN(Ai) ⇏ AuthZ(qi) 以及 Delegate(U, Ai, κ) ⇏ AuthZ(qi)。这一缺口被称为“当前代理安全框架未明确解决的结构性分离”。
- **研究背景**：ISACA、OpenID Foundation等机构已识别出“授权危机”，但缺乏形式化表征。本文旨在将授权本身建模为一个请求绑定的密码学可验证关系，区别于身份认证、能力委托和事后审计。

## 2. 论文提出的方法论

### 核心思想
- 提出**密码学可验证代理授权（CVA）** 假设H1：授权决策可以表示为密码学可验证的关系$R_{CVA}$，该关系联合绑定代理主体、具体授权请求、执行上下文以及策略满足，同时选择性隐藏私有授权属性。
- 核心创新：将授权证据（authorization evidence）独立于身份证据、委托证据、执行证据，形成明确分离：身份证据 ≠ 授权证据 ≠ 执行证据。

### 关键技术细节

1. **系统模型**：定义环境元组 S = (A, G, T, R, P) 分别表示代理、授权网关、外部工具、受保护资源、策略集。
2. **授权请求形式化**：$q_i = (id_i, α, res, c, pid_j, n, t)$，其中id_i为代理身份承诺，α为动作，res为资源，c为执行上下文，pid_j为策略标识符，n为随机数（防重放），t为时间戳。
3. **公共陈述与私密证人**：
   - 公共陈述 $x = (id_i, h_q, h_c, pid_j, n, t)$
   - 私密证人 $w = (sk_i, ρ_i, attrs_i, q_{priv}, c_{priv})$
4. **核心关系$R_{CVA}$** 由四个合取组成：
   - BindPrincipal: CommitID(sk_i, ρ_i) = id_i
   - BindRequest: Hq(Encode(q_priv)) = h_q
   - BindContext: Hc(Encode(c_priv)) = h_c
   - SatisfyPolicy: Pj(attrs_i, q_priv, c_priv) = 1
5. **证明系统**：使用zk-SNARK（Groth16），证明者生成证明π，验证者通过VerifyAuth(pp, x, π)并附加状态检查Fresh(n,t,N)决定是否接受。
6. **威胁模型**：概率多项式时间对手可观察、拦截、重放、替换身份/请求/策略标识，但不能攻破密码学原语或直接获取诚实方秘密。

## 3. 实验设计

- **实验/验证类型**：论文并未进行传统意义上的大规模实验或数据集基准测试。它提供了一个**概念验证（proof-of-concept）原型**，旨在展示所选CVA元素的构造可行性。
- **实现细节**：
  - 底层零知识证明采用**Groth16 zk-SNARK**（bn128曲线），电路用Circom 2.x编写，证明生成使用snarkjs。
  - 网关实现为FastAPI服务，进行无状态证明验证和有状态重放控制。
  - 身份绑定使用Poseidon哈希（简化的承诺方案），请求绑定使用SHA-256。
- **测试场景**：原型仅覆盖单代理-单网关交互，没有多代理或多委托链。验证的测试案例包括：有效请求、无效代理密钥、身份替换、请求哈希篡改、策略不满足、重放攻击、公共信号篡改等。
- **对比方法**：论文未进行与其他方法的对比实验，而是通过文献综述（表1）将CVA与DIAP、BAID、Aegis、zk-MCP、AIP等方法在多项安全属性上进行了定性对比。

## 4. 资源与算力

- **未明确说明**：论文中未提及实验使用的GPU型号、数量、训练时长等算力信息。仅提到Groth16的证明生成成本与约束数量成正比，但未给出具体测量数据。作者承认“未进行跨证明系统的比较基准测试”。

## 5. 实验数量与充分性

- **实验规模**：仅包含一个单一的概念验证实现，未进行多轮次、多场景的广泛实验。验证内容主要是属性导向的测试案例（共7类），覆盖了主体绑定、请求绑定、策略满足、重放抵抗等但未涵盖上下文绑定和执行绑定。
- **充分性评估**：实验作为“构造可行性证据”是充分的，但距离完整的实证安全评估差距较大。
  - 未进行形式化安全规约的归约证明。
  - 未进行独立密码学电路审计。
  - 未进行性能基准测试（如与其他证明系统比较延迟、证明大小）。
  - 未评估在真实代理工作负载下的端到端延迟（高频率授权 vs 低频率授权）。
- **公平性**：由于没有对比实验，公平性问题主要在于原型覆盖范围被保守声明，避免过度宣称，这是诚实的。

## 6. 论文的主要结论与发现

1. **分离性发现**：身份绑定 ≠ 授权请求绑定 ≠ 运行时执行绑定。这是论文最核心的结构性结论，明确了零知识证明授权的边界——可证明请求满足策略，但不能保证同一请求被执行。
2. **构造可行性**：基于Groth16 zk-SNARK的原型证明了在有限但关键的维度上（主体绑定、计划级请求绑定、私有策略满足）可以实现密码学可验证代理授权。
3. **开放问题**：正式提出了以下研究方向：
   - RQ1：哪些维度必须密码学绑定以防止证据转移？
   - RQ2：哪些策略类别可以在实际电路复杂度约束下高效表示为算术ZK关系？
   - RQ3：在不同证明系统和代理工作负载下，请求绑定ZKP授权的开销如何？
   - 此外，多代理委托链中的组合授权绑定是未解决的挑战。
4. **预执行授权与运行时执行的分离**：确认了TOCTOU问题在代理系统中的存在——授权验证时刻(tv)与执行时刻(te)之间的上下文可能变化，因此密码学授权证据不能独立保证运行时的安全。

## 7. 优点

1. **问题识别精准**：清晰辨识了当前认证/委托机制与代理具体请求授权之间的逻辑缺口，并用形式化符号表达，具有理论深度。
2. **形式化模型严谨**：定义了完整的系统模型、公共-私密分离、核心关系$R_{CVA}$及其四个合取条件，每个条件都具有明确的攻击预防语义。
3. **安全属性定义系统**：提出了五个候选属性（授权可靠性、主体绑定、请求绑定、策略绑定、重放抵抗），每个都有游戏定义，为后续形式化分析奠定了基础。
4. **概念验证诚实**：原型对照表2明确列出哪些属性已实现、部分实现、未实现，不夸大覆盖范围。
5. **研究议程清晰可证伪**：提出的RQ1-3是可操作的开放问题，有助于社区推进。
6. **文献定位准确**：表1对比了相关工作的属性覆盖，清晰地展示了CVA的独特定位（请求绑定预执行授权 + ZKP）。

## 8. 不足与局限

1. **实验覆盖极窄**：仅有一个概念验证原型，没有跨证明系统的性能基准、没有真实环境延迟测量、没有大规模或实际代理场景测试。
2. **形式化安全归约缺失**：候选属性虽然定义了游戏，但完整归约到标准假设（如知识-可靠性、承诺绑定）仍未完成。
3. **原型安全性未审计**：Groth16要求可信设置，且未进行独立密码学电路审计，原型安全性依赖于假设。
4. **未实现上下文绑定**：正式模型中包含上下文承诺(hc)，但原型中未实现。
5. **不支持动态策略**：策略被硬编码为算术电路，修改策略需重新编译电路和可信设置。
6. **运行时执行绑定未解决**：这是开放问题，原型完全未涉及。
7. **多代理链未评估**：实际Agent会委托子任务，但模型仅限单一代理-网关交互。
8. **部分信任假设**：授权网关被部分信任，若网关被攻破则安全模型失效。
9. **未进行跨证明系统比较**：如STARKs（无可信设置、后量子安全但证明体积大）、Bulletproofs等未被评估。

（完）
