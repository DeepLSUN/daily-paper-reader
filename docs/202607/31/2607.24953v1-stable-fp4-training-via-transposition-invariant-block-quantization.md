---
title: Stable FP4 Training via Transposition-Invariant Block Quantization
title_zh: 通过转置不变块量化实现稳定的FP4训练
authors: "Mehdi Rahimifar, Amin Darabi, Mehran Taghian Jazi, Xing Huang, Yao Wang, Zhijun Tu, Yufei Cui, Yunke Peng, Hongliang Li"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24953v1"
tags: ["query:llm"]
score: 9.0
evidence: 基于转置不变分块量化的低精度FP4 LLM训练方法
tldr: "低精度训练是提升LLM效率的关键，但FP4训练因张量转置导致前后向缩放不一致而不稳定。提出2D块FP4量化，强制转置不变缩放，配合无截断缩放和随机舍入，并对注意力Q/K用MXFP8混合精度。在7B密集与30B MoE上训练至100B tokens，稳定匹配BF16，性能下降<1.3%。证明了前后向一致性是实用FP4训练的关键。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1508, \"height\": 1012, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1460, \"height\": 914, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1458, \"height\": 714, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1690, \"height\": 1217, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24953v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 936, \"height\": 603, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24953v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1532, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1020, \"height\": 504, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1124, \"height\": 504, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1147, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24953v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1676, \"height\": 1403, \"label\": \"Table\"}]"
motivation: 现有1D块量化中，张量转置使前后向尺度因子不一致，导致梯度更新有偏且不稳定，阻碍FP4训练。
method: 提出2D块FP4量化，强制转置不变缩放，结合无截断缩放与随机舍入，并对注意力Q/K投影采用MXFP8混合精度。
result: "在7B密集和30B MoE模型上训练至100B tokens，稳定FP4训练，性能与BF16差距小于1.3%。"
conclusion: 前后向缩放一致性足以实现实用FP4训练，为高效LLM训练提供简单有效路径。
---

## 摘要
降低训练精度是提升大型语言模型（LLM）训练效率的关键杠杆，但超越FP8推进至4位浮点（FP4）仍因优化过程中的不稳定性而具有挑战性。我们在现有微缩方法中发现了这一不稳定性的根本来源：由张量转置引起的尺度不一致性。在传统的1D块量化中，前向和反向传播在转置后对相同值分配不同的缩放因子，导致梯度更新有偏且不稳定。为解决此问题，我们提出一种基于2D块FP4量化的低精度训练框架，该框架强制执行转置不变缩放，并保持前向与反向计算之间的一致性。我们进一步将其与无截断缩放和随机舍入相结合，以控制量化误差并保持无偏梯度。为处理注意力机制的敏感性，我们对查询和键投影采用MXFP8量化，形成实用的混合精度设计。我们在多达7B参数的稠密LLM和30B参数的混合专家模型上评估了我们的方法，训练数据规模达100B tokens。在所有设置下，我们的方法实现了稳定的端到端FP4训练，并与BF16性能紧密匹配，困惑度和下游准确率下降不到1.3%。这些结果表明，强制前向-反向缩放一致性足以实现大规模实用的FP4训练，为更高效的LLM训练提供了一条简单有效的途径。

## Abstract
Reducing training precision is a key lever for improving the e ciency of large language model (LLM) training, but pushing beyond FP8 to 4-bit oating point (FP4) remains challenging due to instability during optimization. We identify a fundamental source of this instability in existing microscaling approaches: scale inconsistency induced by tensor transposition. In conventional 1D block quantization, forward and backward passes assign di erent scaling factors to the same values after transposition, leading to biased and unstable gradient updates. To address this issue, we propose a low-precision training framework based on 2D block FP4 quantization, which enforces transposition-invariant scaling and preserves consistency between forward and backward computations. We further combine this with truncation-free scaling and stochastic rounding to control quantization error and maintain unbiased gradients. To handle the sensitivity of attention mechanisms, we adopt MXFP8 quantization for query and key projections, yielding a practical mixed-precision design. We evaluate our method on dense LLMs up to 7B parameters and a 30B Mixture-of-Experts model, trained on up to 100B tokens. Across all settings, our approach achieves stable end-to-end FP4 training and closely matches BF16 performance, with less than 1.3% degradation in perplexity and downstream accuracy. These results demonstrate that enforcing forwardbackward scaling consistency is su cient to enable practical FP4 training at scale, providing a simple and e ective pathway toward more e cient LLM training.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- 大语言模型（LLM）训练的计算与能耗开销急剧增长，降低训练数值精度是提升训练效率的关键路径。
- FP8 训练已在现代加速器上进入实用阶段，但进一步推进至 4 位浮点（FP4）格式仍面临严峻挑战，主要障碍包括：
  - FP4 动态范围极窄，量化误差大；
  - 反向传播与注意力计算对数值扰动高度敏感，易导致训练不稳定。
- 现有微缩（microscaling）方法（如 MXFP4、NVFP4）采用块状量化（block-wise quantization）来缓解误差，但均依赖 **一维（1D）块结构**，在张量转置时破坏了前后向计算的缩放一致性。
- **核心问题**：论文指出，1D 块量化在反向传播中因张量转置导致同一数值在前向与反向使用不同缩放因子，产生有偏梯度，这是 FP4 训练不稳定的**根本性、结构性原因**，而非单纯的精度不足。

### 2. 论文提出的方法论

**核心思想**：通过设计**转置不变**的量化方案，保证前向与反向传播对同一数值使用一致的缩放因子，从根源上消除梯度偏差。

**关键技术细节**：

- **2D 块 FP4 量化**：
  - 将张量划分为 \( b \times b \) 的正方形块（论文默认 \( 32 \times 32 \)），每个块共享一个缩放因子。
  - 转置操作只将块 \( B_{i,j} \) 映射为块 \( B_{j,i} \)，块内数值集合不变，因此块最大值与缩放因子保持不变，满足：
    \[
    S(B_{i,j}) = S(B^\top_{i,j})
    \]
  - 该性质保证前向与反向使用的量化表示一致，消除因缩放不一致引入的梯度偏差。

- **无截断缩放（Truncation-Free Scaling）**：
  - 缩放因子按 \( S = 2^{\lceil \log_2(2M/(Q_p - Q_n)) \rceil} \) 计算，确保块内所有值均落在 FP4 可表示范围内，避免截断（clipping）导致的系统性失真。

- **随机舍入（Stochastic Rounding）**：
  - 在反向传播中对梯度张量使用随机舍入，满足 \( \mathbb{E}[\text{round}(x)] = x \)，从而在强量化下维持梯度的无偏估计，降低舍入偏差。

- **MXFP8 混合精度注意力**：
  - 注意力机制中 \( QK^\top \) 点积会放大量化噪声，softmax 进一步放大扰动。
  - 因此，对查询（Q）和键（K）投影采用 MXFP8 量化以提升数值稳定性；
  - 其余所有 Transformer 线性层（包括 MLP、注意力 V/O 投影）保持 2D 块 FP4。

- **直通估计器（STE）**：用于使量化函数的梯度可传播，在反向近似视量化函数为单位映射。

### 3. 实验设计

**模型与训练规模**：

| 模型 | 参数量 | 架构类型 | 训练数据量 |
|------|--------|----------|------------|
| OLMo-1B | 1B | 稠密 | 100B tokens |
| OLMo-7B | 7B | 稠密 | 100B tokens |
| Qwen 30B MoE | 30B | 混合专家 | 100B tokens |

**对比方法**：

- **BF16**：全精度训练基线。
- **2D-FP4**：本文提出的 2D 块 FP4 量化（无 MXFP8 注意力）。
- **2D-FP4 + MXFP8**：2D-FP4 基础之上对注意力 Q/K 采用 MXFP8 的混合精度配置。
- 文中还涉及与现有 1D 微缩方法的对比（图 4 中的 "alternative microscaling schemes"）。

**评估基准**：

- **语言建模**：Wikitext、Pile、C4 上的困惑度（PPL）。
- **下游推理**：SciQ、COPA、ARC-Easy、HellaSwag 上的准确率。
- **训练动态**：各模型规模下的训练 loss 曲线。

### 4. 资源与算力

- 论文中**未明确说明**所使用的 GPU 型号、数量及具体训练时长（小时数）。
- 明确指出实验是在**不支持原生 FP4 张量核心**的硬件平台上通过**软件模拟**微缩行为完成的。
- 作者称理论效率分析基于 OLMo-1B：模型权重内存减少约 65.6%，线性层理想吞吐量提升约 \( 3.56\times \)（表 1），但该数值为理想化估计，并非实测性能。

### 5. 实验数量与充分性

**实验组数概览**：

- 3 个模型规模（1B / 7B / 30B MoE）× 3 种配置（BF16 / 2D-FP4 / 2D-FP4+MXFP8），共 9 组完整训练实验；
- 3 个语言建模基准 + 4 个下游推理基准上的评估；
- 消融实验：
  - 无截断缩放 vs 有截断缩放；
  - 随机舍入 vs 确定性舍入；
  - 4 种 2D 块大小（\( 8\times8 \)、\( 16\times16 \)、\( 32\times32 \)、\( 64\times64 \)）的权衡分析。

**充分性评价**：

- **优点**：覆盖了稠密和 MoE 两类架构，模型规模跨度从 1B 到 30B，训练 token 量达 100B，消融较为完整，能较好地支撑论文核心论点。
- **潜在不足**：
  - 未与同类最新 FP4 训练方法（如 NVFP4 系列、Quartet 等）在同一硬件条件下做直接、细致的横向对比；
  - 所有实验基于软件模拟，缺乏真实硬件加速效果验证；
  - 下游任务仅 4 个且规模较小，覆盖面有限。

### 6. 论文的主要结论与发现

- **转置引起的缩放不一致是 FP4 训练不稳定的关键失败模式**。1D 微缩方法中前后向量化尺度错配导致有偏梯度，是比纯精度损失更本质的问题。
- **2D 块 FP4 量化可有效消除该不稳定性**，使端到端 FP4 训练在各模型规模下保持稳定收敛。
- **性能与 BF16 高度接近**：所有设置下困惑度与下游准确率退化 < 1.3%；1B 模型损失差距约 1.1%，7B 约 0.9%，30B MoE 约 0.8%。
- **规模越大，FP4 与 BF16 的差距越小**，说明大规模模型对量化噪声的容忍度更高（在消除系统性偏置后）。
- 无截断缩放与随机舍入均为稳定训练的必要组件：前者防止溢出，后者保持梯度无偏。
- MXFP8 注意力提供额外的稳定性冗余，但并非核心因素；2D-FP4 单独使用已接近 BF16。

### 7. 优点

- **视角独特**：将低精度训练的核心障碍从"表示保真度"重新定义为"计算的全局一致性"，提供了新的理论框架。
- **方法简洁且通用**：2D 块量化改动极小，易于集成到现有训练管线，无需复杂的自适应机制。
- **实验规模较大**：覆盖 1B–30B 参数、稠密与 MoE 架构、100B token 训练，验证了方法的可扩展性。
- **消融实验设计清晰**：逐项验证了块结构、无截断缩放、随机舍入和混合精度注意力的贡献，论证具有说服力。

### 8. 不足与局限

- **缺少真实硬件验证**：实验基于软件模拟，未在支持原生 MXFP4 的加速器上验证实际加速比、内存节省与能耗收益，结论对硬件部署的指导性有限。
- **资源信息不透明**：未报告 GPU 型号、数量、训练时长等关键算力细节，影响实验可复现性评估。
- **对比基线不够全面**：未与最新的 FP4 训练方法（如 Quartet、FP4-all-the-way 等）进行同条件详细对比，量化"优势"的说服力有限。
- **下游评估有限**：只使用 4 个小规模推理基准，缺少更广泛的任务覆盖（如代码、数学、多语言）与更大规模指令微调评估。
- **模型规模仍有上限**：最大模型为 30B MoE，前沿模型（如百亿级稠密、万亿级 MoE）下的表现尚未验证。
- **边界块处理简化**：维度无法整除块大小时，边界块仅以有效值计算缩放，但对 padding 对梯度统计的影响分析不足。

（完）
