---
title: "SkillSmith: Learning to Compose Parametric Skills and Textual Knowledge"
title_zh: SkillSmith：学习组合参数化技能与文本知识
authors: "Lucio M. Dery, Benedict Aaron Tjandra, Siavash Samiei, Adhiguna Kuncoro, Zohar Yahav, Jiajun Shen, Arthur Szlam"
date: 2026-07-29
pdf: "https://arxiv.org/pdf/2607.27497v1"
tags: ["query:agent"]
score: 9.0
evidence: 智能体系统、参数化技能库、文本与权重的组合
tldr: 现有LLM智能体常分别利用文本知识合成与参数技能库，但两者被视为正交，缺乏统一整合。本文提出SkillSmith，将模型权重视为可推理的额外模态，通过前缀微调让LLM同时读取权重与文本并直接输出新前缀参数。实验表明该方法显著优于纯文本或纯权重基线。该工作弥合了文本与权重的模态鸿沟，为参数级技能生成提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.27497v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1710, \"height\": 963, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1559, \"height\": 464, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1639, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1652, \"height\": 927, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1660, \"height\": 929, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 703, \"height\": 873, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1715, \"height\": 442, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1093, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1130, \"height\": 605, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.27497v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1089, \"height\": 699, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.27497v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1420, \"height\": 521, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 789, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1712, \"height\": 732, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1723, \"height\": 598, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1719, \"height\": 550, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1699, \"height\": 517, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1704, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1721, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.27497v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1720, \"height\": 316, \"label\": \"Table\"}]"
motivation: 文本知识与参数技能库被割裂研究，缺少将两者结合进行参数合成的统一框架。
method: 用前缀微调实例化参数学习，增强LLM同时摄入权重与文本，输出新前缀权重。
result: 在基准上显著超过纯文本与纯权重的单模态基线，取得增益。
conclusion: 展示跨模态参数合成的有效性，为技能库构建开辟新方向。
---

## 摘要
由大型语言模型（LLM）驱动的智能体系统通常具备两个关键机制来自主解决复杂问题：从过往经验中综合基于文本的知识与流程，以及为重复出现的子目标构建参数化（权重空间）技能库。迄今为止，研究大多将这两者视为正交方向：要么通过组合与反思来组织文本知识，要么通过权重空间合并来整合参数化技能。因此，针对特定性能提升的文本与模型权重的无缝集成仍未得到充分探索。本工作通过将模型权重视为一种LLM能够原生推理的额外模态，弥合了这一模态鸿沟。我们通过前缀微调实例化参数化学习，并增强LLM以同时摄取前缀权重和捕获与目标能力关系的丰富文本数据。我们增强后的LLM（称为SkillSmith）综合这些输入以执行指令引导的参数化合成，直接输出体现目标技能的新前缀权重。我们证明，我们的方法显著优于仅文本和仅权重空间的基线，实现了单模态（仅文本或仅权重）适配无法企及的性能提升。

## Abstract
Agentic systems driven by large language models (LLMs) regularly feature two key mechanisms to autonomously solve complex problems: synthesizing text-based knowledge and procedures from past experiences and building parametric (weight-space) skill libraries for recurring sub-goals. To date, research has largely treated these as orthogonal pursuits: either organizing textual knowledge through composition and reflection, or consolidating parametric skills via weight-space merging. Consequently, the seamless integration of text and model weights for targeted performance improvements remains largely unexplored. This work bridges this modality gap by treating model weights as an additional modality that an LLM can natively reason over. We instantiate parametric learning via prefix-tuning and augment an LLM to ingest both prefix weights and rich textual data which capture relationships to a target capability. Our augmented LLM, which we call SkillSmith, synthesizes these inputs to perform instruction-steered parametric synthesis, directly outputting new prefix weights that manifest the target skill. We demonstrate that our approach significantly outperforms both text-only and weight-space-only baselines, unlocking performance gains that are out of reach for uni-modal (text-only or weight-only) adaptations.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：由大语言模型（LLM）驱动的智能体系统在解决复杂多步推理任务时，通常依赖两种关键机制：(i) **基于文本的知识合成**——通过自我反思、结构化记忆、提示词生成等方式组织和复用经验；(ii) **参数化技能库构建**——通过参数高效微调（PEFT，如前缀微调、LoRA）将学到的行为固化为模块化权重，供检索、复用或合并。
- **核心问题**：现有研究将文本推理和参数技能获取视为**正交的两条路线**——要么只做文本层面的组织与组合，要么只做权重空间的算术合并（平均、拼接等）。**不存在一个统一框架**，能够让智能体同时利用两种模态来针对性地合成新的任务参数。
- **整体含义**：本文提出**SkillSmith**，将模型权重视为一种 LLM 可原生推理的“模态”（modality），让增强后的 LLM 同时摄入文本提示与前缀权重，并直接合成目标任务的新前缀参数，从而弥合了文本与权重之间的模态鸿沟，实现真正的跨模态参数组合与泛化。

## 2. 论文提出的方法论：核心思想、关键技术细节、流程（文字说明）

- **核心思想**：训练一个增强型 LLM（SkillSmith），使其能够推理文本元数据与权重空间输入（前缀 KV-cache），并以端到端方式直接输出目标任务的参数化技能（前缀权重）。
- **关键技术细节**：
  1. **参数化技能实例化**：用**前缀微调（Prefix-Tuning）** 作为参数技能的具体形式。每个源任务对应一个 K-V 前缀缓存（prefix cache），同时关联文本元数据（任务描述、少样本示例、反思文本等），构成“任务包” $b_i = (m_i, w_i)$。
  2. **输入序列构造**：SkillSmith 的输入序列依次包含：
     - **Preamble Text**：整体组合目标描述，激活 LLM 的指令跟随能力；
     - **Interleaved Bundles**：每个源任务包的文本元数据与经 K-V Adapter（多层感知机 MLP）投影到 LLM 隐空间的前缀权重交错排列，用控制 token（如 `<src_start>`、`<kv_start>`、`<kv_end>`）界定结构；
     - **Combination Text**：描述源任务与目标任务关系的文本（如提示词生成的 rationale、目标任务示例、目标任务描述）；
     - **Generation Block**：以 `<gen_start>` 开始的占位 token 序列，LLM 前向传播后取这些位置的 KV-cache 作为原始输出。
  3. **输出后处理**：对生成的 KV-cache 进行逆 RoPE 去旋转以去除位置信息，再经 Out K-V Adapter（MLP）投影，得到可直接部署到冻结的基座模型 $M_\phi$ 上的目标任务前缀权重 $m_{new}$。
  4. **端到端元训练**：训练目标为最小化冻结基座模型在目标任务上的交叉熵损失，即 $\theta^* = \arg\min_\theta \sum \mathcal{L}(M_\phi(x;\, m_T),\, y)$，其中 $m_T = \text{SkillSmith}_\theta(\{b_k\}, w)$。训练过程中动态采样输出前缀长度（16/32/64/128），并加入 KV-cache 丢弃（dropout）正则化。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **数据集**：
  - **Composite-SNI（CSNI）**：用 Gemini 2.5 Pro 从 SNI（Super-Natural Instructions）任务对合成的新组合任务（约 21K 个，17K 训练），有**已知的 ground-truth 源任务映射**，用于隔离验证 SkillSmith 的合成能力。进一步按源任务是否在 meta-train 中出现分为 Both-Seen / One-Seen / Neither-Seen 三个子集。
  - **SNI**：1,616 个 NLP 任务中取 875 个作为源任务池，无 ground-truth 映射，需要**启发式检索**，用于评估“真实场景”泛化。
  - **MMLU-ProX**：多语言、多学科基准（29 语言、14 类别），抽取 6 个语言-学科对（如 wolof_math、spanish_law）作为 held-out 评估空间。
- **评估协议**：每个目标任务构建 `(目标任务数据, 源任务集合, 组合文本)` 元组；所有方法均可访问该元组。在零样本（zero-shot）和目标任务上继续微调（fine-tuning）两种设置下比较。
- **对比方法**：
  - **纯权重空间**：LERP（线性插值/任务算术）、Concat（拼接 64 token）、Source-Task Transfer（SPoT 式初始化）、SVD（沿序列/头/嵌入轴做奇异值分解合并）。
  - **纯文本空间**：ICL（16/32/64-shot 上下文学习）。
  - **免迁移基线**：Direct Prefix Tuning（随机初始化或 ICL 初始化），以及文本增强版 Direct Baseline（将 SkillSmith 的全部文本元数据拼接到输入前）。
- **评估指标**：逐任务负对数似然（NLL），并映射为全局 Elo 评分（Bradley-Terry 框架、L-BFGS-B 优化），附录中附全部原始 NLL。

## 4. 资源与算力

- **论文未明确披露具体计算资源信息**，如 GPU 型号、数量、总训练时长等均未在正文中说明。
- 仅能从方法细节中间接推断：基座模型与 SkillSmith 的共处理器均为 **Gemma 3 4B**；元训练步数分别为 CSNI 约 10,000 步、SNI 约 2,000 步；batch size 从 {32, 64, 128} 中选择；前缀模块训练在 {200, 500, 1000} 步和 1e-2 至 1e-4 学习率的网格中搜索。
- 这意味着读者无法从论文中直接评估其训练成本的可复现性或扩展性。

## 5. 实验数量与充分性

- **实验总量**：较丰富，包括：
  1. CSNI 上 15 个 meta-test 任务的 Elo 对比（零样本 + 微调）。
  2. SNI 上 10 个 held-out 任务的对比（零样本 + 微调）。
  3. MMLU-ProX 上 6 个语言-学科对的对比（零样本 + 微调）。
  4. **消融实验**：(i) 输入内容消融（无输入 vs 仅 KV-cache vs 仅文本 vs 全输入）；(ii) 文本增益来源分析（对比文本增强的 Direct Prefix Tuning）；(iii) 泛化性验证（按 Both/One/Neither-Seen 拆分）；(iv) 检索敏感性分析（True Sources vs Retrieval+LLM  vs Random+LLM）。
- **充分性与客观性**：
  - **优点**：消融设计较为全面，能回答“是否真的用了 KV-cache”“增益来自架构还是文本”“是否真正泛化”等关键问题；附录提供了全部原始 NLL，透明可验证；Elo 评分和原始分数双重呈现。
  - **不足**：SNI 上微调后各方法趋同，区分度较低；检索+LLM 相比随机+LLM 仅略优（Elo），说明检索机制的增益有限；在 MMLU-ProX 上只选了 6 个语言-类别对，覆盖面有限；所有实验只使用 Gemma 3 4B 一个基座模型，未在多模型规模上验证。

## 6. 论文的主要结论与发现

1. **CSNI（已知 ground-truth 源任务）下**：零样本时 SkillSmith 显著优于所有权重合并方法（LERP、Concat、SVD 等）；微调后 SkillSmith 作为初始化，大幅优于 ICL 初始化和直接训练，是全面最优的方法。
2. **SNI（检索式、任务简单、数据充分）**：零样本时，经过 CSNI 预训练再在 SNI 上微调的 SkillSmith 最优；但下游微调后各方法收敛到相似水平，SkillSmith 的优势不明显。
3. **MMLU-ProX（数据稀疏、任务困难）**：由 CSNI 初始化并在 MMLU 源任务上训练的 SkillSmith 零样本时就优于所有基线（包括可以微调的基线）；微调后更以显著优势胜出，体现优越的参数初始化价值。
4. **消融结果证明**：同时使用文本和 KV-cache 输入效果最佳，证明 SkillSmith 确实在用权重模态，并非单纯受益于额外文本；且增益不只是来自文本上下文，而是来自对两种模态的合成。
5. **泛化能力**：在 Neither-Seen（两个源任务都未在 meta-train 中出现）的 CSNI 子集上仍为最优，说明具备分布外组合泛化能力。
6. **总体结论**：将参数空间作为可读、可合成的模态，可提供显著更优的适配初始化，为更整体的智能体架构提供了蓝图。

## 7. 优点：方法或实验设计上的亮点

- **概念创新**：首次系统性地将模型权重视为 LLM 可直接推理的输入模态，在方法论上打破了文本与权重各自为政的范式，提出统一的“指令引导参数合成”框架。
- **架构设计**：选择前缀 KV-cache 作为权重模态，理由充分——文本可自然通过前向传播转为 KV-cache，为文本↔权重的桥接提供了先验可行性；输入 K-V Adapter 和 Out K-V Adapter 的设计简洁有效。
- **合成数据集贡献**：构建了带 ground-truth 源任务映射的 Composite-SNI 数据集（含详细的数据生成与多轮质量过滤流程），为后续研究混合模态组合提供了可复用的资源。
- **实验严谨性**：在理想条件与真实场景（检索式源任务）两套设置下均做了验证；对多种权重合并基线（含 SVD 变体）做了系统对比；提供完整原始 NLL 数据，保证可验证性。
- **公正对比**：区分了零样本与微调两种评价协议，区分了“结构优势”与“文本优势”（文本增强直接训练对比），消融逻辑清晰。

## 8. 不足与局限

- **计算资源信息披露不足**：未报告 GPU 类型、数量、训练时间、能耗等具体指标，影响可复现性和扩展性评估。
- **模型规模的普适性有限**：仅在 Gemma 3 4B 上验证，未测试更大规模 LLM 或多架构（如不同注意力机制、不同基座）下的迁移效果。
- **微调优势随任务简单性和数据量增加而消失**：SNI 上各方法收敛，说明 SkillSmith 的价值可能集中在任务难、数据少的场景，普适性需进一步验证。
- **检索机制的贡献不明显**：Random + LLM Selection 与 Retrieval + LLM Selection 的 Elo 差距很小，且随机选择时 3/15 任务显著变差，说明源任务检索模块是性能瓶颈。
- **覆盖范围**：MMLU-ProX 仅用 6 个语言-类别对评估，CSNI 实验也只报告 15 个测试任务，样本量偏小，统计显著性未系统检验。
- **仅限前缀微调**：论文只验证了 prefix-tuning 一种 PEFT 方法，对 LoRA 等其他主流参数化技能形式尚无直接证据。
- **单一损失函数**：只使用交叉熵损失，未探索强化学习等更复杂的元训练目标。
- **前缀长度固定为 32**：评估时统一使用固定长度，未充分研究输出长度对性能的边际影响。

（完）
