---
title: "Demonstrating TOFFEE: A Learned System for Synthesizing Data Agent Trajectories at Scale"
title_zh: 展示TOFFEE：一种大规模合成数据智能体轨迹的学习系统
authors: "Ziting Wang, Yin Li, Zuhao Yang, Xiuchang Li, Jiale Bai, Gao Cong"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06233v1"
tags: ["query:agent"]
score: 9.0
evidence: 用于合成数据智能体轨迹的系统，支持微调和上下文学习
tldr: 针对LLM数据代理在异构企业环境中泛化不足的问题，提出TOFFEE系统，通过蒙特卡洛树搜索（MCTS）结合自适应模型选择与跨任务前缀重用，自动合成高质量的数据代理轨迹。实验表明，该方法能有效生成复杂分析任务的规模化轨迹数据，既可用于微调数据代理模型，也可作为上下文学习示例，提升模型在未知环境中的推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06233v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 795, \"height\": 661, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 877, \"height\": 377, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 791, \"height\": 784, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06233v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 795, \"height\": 793, \"label\": \"Figure\"}]"
motivation: 现有数据代理难以泛化到异构企业环境，亟需合成高质量轨迹数据以适配目标领域。
method: 采用MCTS、自适应模型选择和跨任务前缀重用，系统化生成覆盖复杂分析流程的轨迹。
result: 在异构环境下成功生成了规模化、高质量的复杂任务轨迹数据。
conclusion: TOFFEE通过轨迹合成有效增强了数据代理的微调与示范推理能力。
---

## 摘要
基于大语言模型的数据智能体在数据驱动决策中扮演着越来越重要的角色。然而，现有的数据智能体难以泛化到未见过的数据环境和分析工作流，尤其是在异构企业环境中。这导致对合成高质量数据智能体轨迹的需求日益增长，这些轨迹能够捕获给定数据环境下的复杂分析工作流。此类轨迹支持两个关键的下游用途：它们可以作为监督微调数据，使数据智能体模型适应目标领域；也可以作为上下文学习演示，指导通用大语言模型在陌生数据环境中工作。为此，我们引入了TOFFEE，一个通过蒙特卡洛树搜索（MCTS）结合自适应模型选择和跨任务前缀重用来从给定数据环境合成高质量数据智能体轨迹的系统。我们展示了TOFFEE能够有效地为跨异构环境的复杂分析任务生成可扩展的轨迹数据。在本次演示中，我们将介绍TOFFEE的系统框架，包括其任务池构建、轨迹探索器和学习成本模型。我们还将介绍TOFFEE的Web界面及其工作流，并展示两个端到端场景：用于数据智能体微调的轨迹合成，以及演示增强的数据智能体推理。

## Abstract
LLM-powered data agents are playing an increasingly important role in data-driven decision making. However, existing data agents struggle to generalize to unseen data environments and analytical workflows, especially in heterogeneous enterprise settings. This creates a growing need for synthesizing high-quality data agent trajectories that capture complex analytical workflows for given data environments. Such trajectories support two key downstream uses: they can serve as supervised finetuning (SFT) data that adapts data agent models to the target domain, and as in-context learning (ICL) demonstrations to guide general-purpose LLMs in unfamiliar data environments. Thus, we introduce TOFFEE, a system for synthesizing high-quality data agent trajectories from given data environments via Monte Carlo Tree Search (MCTS) with adaptive model selection and cross-task prefix reuse. We show that TOFFEE can effectively generate scalable trajectory data for complex analytical tasks across heterogeneous environments. In this demonstration, we present the system framework of TOFFEE, including its task pool construction, trajectory explorer, and learned cost model. We also introduce the web interface of TOFFEE and its workflow, and demonstrate two end-to-end scenarios: trajectory synthesis for data agent finetuning, and demonstration-augmented data agent reasoning.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：基于大语言模型（LLM）的数据智能体（Data Agent）在异构企业环境中泛化能力差，面对未见过的数据环境和分析工作流时容易出现SQL错误、输出模糊结论等问题。
- **整体含义**：高质量的数据智能体轨迹（即推理、工具调用和结果的多步序列）可以通过监督微调（SFT）或上下文学习（ICL）显著提升智能体性能。然而，大规模、高质量地合成这些轨迹面临挑战：自动构建可解任务困难、搜索空间指数级增长、每一步需要选择最优推理配置（模型、上下文长度等）。为此，作者提出了**TOFFEE**系统，旨在**自动、高效、大规模地合成数据智能体轨迹**，以赋能下游微调和推理。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：通过**蒙特卡洛树搜索（MCTS）** 结合**跨任务前缀重用**与**学习成本模型（LCM）**，在给定数据环境中自动探索并合成高质量轨迹，同时实时调整推理配置以控制成本。
- **三个阶段**：
  1. **任务池构建（Task Pool Construction）**：
     - 自动分析数据环境（数据库、CSV、JSON等），构建数据依赖图（如外键关联、异常模式）。
     - 反向推导可行的多步分析任务，并验证每项任务的可解性和有意义性（通过预执行确认答案）。
     - 只保留具有确定性答案和执行证据的任务，避免污染下游评测。
  2. **轨迹探索器（Trajectory Explorer）**：
     - 采用MCTS进行树搜索：节点为分析状态，边为工具调用（SQL、Python等），由LLM生成。
     - 跨任务共享前缀（如模式发现、数据采样），避免重复计算。
     - 遇到错误时分支为修复路径，保留错误-诊断-修复序列作为训练数据。
     - 仅当最终答案与任务构建时记录的答案匹配时才存储完整轨迹。
  3. **学习成本模型（Learned Cost Model, LCM）**：
     - 预算感知的状态控制器：从当前状态和剩余预算中选择LLM模型、推理配置（上下文长度、推理努力等）。
     - 提取状态特征（进度、模式覆盖、错误历史），通过在线岭回归学习预测奖励和探索奖励。
     - 根据前几名配置的分歧度动态调整分支宽度，预算越窄分支越少。
     - 与探索器形成闭环：每步后更新LCM参数。

## 3. 实验设计：数据集、基准与方法对比

- **数据环境来源**：使用了Spider、LiveSQLBench、SpreadsheetBench等现有基准作为初始数据环境来源，同时支持用户连接自定义数据环境。
- **下游评估基准**：
  - **KramaBench**（参考[2]）
  - **DSBench**（参考[1]）
- **对比方法**：
  - 轨迹质量对比：Single-Pass（单次生成）、Best-of-N（N=5）、TOFFEE without LCM、TOFFEE（完整系统）。
  - 下游性能对比：OpenAI o3（前沿推理模型）、Qwen3.5-27B（基础模型）、Qwen3.5-27B + ICL（使用TOFFEE轨迹作为演示）、TOFFEE-27B（基于Qwen3.5-27B微调）。
- **评价指标**：
  - 轨迹质量：综合执行信号、步骤成功率、分析进度、最终答案正确性，得分范围0-1。
  - 下游得分：KramaBench和DSBench的整体成绩。

## 4. 资源与算力

- **硬件**：Ubuntu服务器，双AMD EPYC 9555 CPU，8块NVIDIA H100 GPU。
- **合成工作**：通过OpenRouter API调用外部LLM（未明确说明训练时长或具体API成本）。
- **微调模型**：TOFFEE-9B和TOFFEE-27B分别从Qwen3.5-9B和Qwen3.5-27B微调得到，但训练时长、GPU数量等细节未提及。

## 5. 实验数量与充分性

- **实验数量**：论文仅展示了两个主要结果图（Figure 3a和3b），包含轨迹质量随预算变化的曲线和两个基准下的下游得分对比。无额外消融实验（如不同MCTS迭代次数、不同模型池组合、不同预算设置下的更多重复等）。
- **充分性与公平性**：
  - 轨迹质量方面，TOFFEE优于单次和Best-of-N，并展示了LCM的作用，但缺乏统计显著性或多次重复的误差线。
  - 下游性能方面，仅比较了少量模型，未报告方差或多次运行结果。
  - 作为演示论文，初步实验足以支持概念验证，但**不够充分**，在实验广度（更多数据环境、更多模型基数）和鲁棒性上存在局限。

## 6. 论文的主要结论与发现

- TOFFEE在轨迹合成效率上显著优于Single-Pass和Best-of-N，能在较低预算下达到高质量。
- LCM（自适应模型选择）比均匀配置（TOFFEE w/o LCM）带来明显提升。
- 使用TOFFEE合成的轨迹进行ICL或SFT后，模型（TOFFEE-27B）在KramaBench和DSBench上超越了OpenAI o3等前沿推理模型。
- 系统在异构数据环境下可扩展生成规模化轨迹数据，支持两种下游用途（微调与演示增强）。

## 7. 优点：方法或实验设计上的亮点

- **自动任务池构建**：解决了手动构造任务不可扩展的难题，通过依赖发现和可解性验证确保了任务质量。
- **MCTS探索错误恢复**：自然捕获多样化执行路径，包括错误修复，产生更丰富的训练数据。
- **LCM预算感知**：动态选择推理配置，避免了“小模型做复杂任务失败、大模型做简单任务浪费”的问题，实现了成本-质量平衡。
- **跨任务前缀重用**：节省了重复的模式发现和初始化步骤，提升效率。
- **端到端系统演示**：提供了交互式Web界面，支持两种实际场景（微调轨迹合成、推理演示）的实时可视化，易于使用和理解。

## 8. 不足与局限

- **实验覆盖有限**：仅报告了两个下游基准和一种预算条件下的曲线，缺乏在更多数据环境（如大型企业数据库、多种文件格式组合）下的验证。
- **潜在偏差风险**：
  - 任务池构建可能偏向于简单或模式化依赖，复杂隐性关联可能未被发现。
  - LCM的训练过程依赖在线数据，可能存在冷启动问题或过拟合到特定环境。
  - 使用了OpenRouter API调用商业模型，结果可能受模型版本和稳定性影响，难以复现。
- **应用限制**：
  - 系统依赖LLM的API调用，延迟和成本可能在高频场景下不可忽视。
  - 轨迹合成仅适用于结构化数据环境和可执行工具（SQL/Python），对于纯文本或非结构化数据场景可能不适用。
  - 没有讨论在真实企业多用户并发下的扩展性和安全隔离问题。
- **未报告微调细节**：TOFFEE-9B/27B的训练配置（学习率、批次、数据量）未给出，影响结果的可复现性。

（完）
