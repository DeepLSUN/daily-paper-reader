---
title: "DepthWeave-KV: Token-Adaptive Cross-Layer Residual Factorization for Long-Context KV Cache Compression"
title_zh: DepthWeave-KV：面向长上下文KV缓存压缩的令牌自适应跨层残差分解
authors: "Anna Cordoba, Adam Puente Tercero, Nerea Angulo Hijo, Mar Linares Tercero, Julia Barrientos, Ainhoa Miranda, Jesus Olivera"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06523v1"
tags: ["query:llm"]
score: 9.0
evidence: 标记自适应的KV缓存压缩方法
tldr: 长上下文语言模型推理受限于KV缓存的内存带宽与容量，现有压缩方法均匀分配预算导致检索精度下降。本文提出DepthWeave-KV，通过跨层低秩分解将相邻层KV状态共享为基矩阵，保留token特定残差，并利用token条件深度路由器对指令关键型token分配更高重构秩，结合注意力输出探针在线误差跟踪实现无校准压缩。在LongBench等基准上，达到8.3倍KV内存压缩，64K上下文推理速度72.8 tokens/s，任务质量接近全缓存，显著优于先前压缩方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06523v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 859, \"height\": 429, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06523v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 481, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06523v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 909, \"height\": 545, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06523v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1115, \"height\": 1033, \"label\": \"Table\"}]"
motivation: 现有KV缓存压缩方法跨层与跨token统一分配预算，无法同时保留词汇线索和语义状态，导致长上下文检索精度下降。
method: 通过跨层低秩基共享分解残差，并使用token条件深度路由器动态分配重构秩，结合注意力探针在线误差跟踪实现无校准压缩。
result: 在多个长上下文基准上实现8.3倍KV内存压缩，64K上下文速度72.8 tokens/s，精度接近全缓存，超越先前方法。
conclusion: DepthWeave-KV通过token自适应跨层分解，实现高效的KV缓存压缩，显著降低内存占用而不牺牲任务性能。
---

## 摘要
长上下文语言模型推理日益受到存储键值缓存所需内存带宽和容量的限制，然而现有压缩方法通常在各层或各令牌上应用统一预算，并在需要不同保留方式的词汇线索和语义状态下降低检索质量。我们提出DepthWeave-KV，一种令牌自适应的缓存压缩方法，该方法使用共享的低秩通道基跨相邻Transformer层分解键和值状态，同时在对注意力行为敏感的令牌处保留轻量级的令牌特定残差。DepthWeave-KV结合了跨深度残差分解与令牌条件深度路由器，该路由器为承载指令和检索关键的令牌分配更高的重建秩，并利用来自注意力输出探针的免校准在线误差跟踪，在无需重新训练基础模型的情况下，在生成过程中自适应压缩。一个融合的CUDA实现联合执行基查找、残差反量化和注意力投影，以减少解码时的内存流量。在LongBench、Needle-in-a-Haystack、L-Eval以及长格式问答和摘要基准测试中，DepthWeave-KV以显著更低的内存使用实现了接近全缓存的任务质量，相比先前的压缩缓存提高了平均得分和检索精度，同时在64K上下文中实现了8.3倍的KV内存减少和每秒72.8个令牌。

## Abstract
Long-context language model inference is increasingly limited by the memory bandwidth and capacity required to store key-value caches, yet existing compression methods often apply uniform budgets across layers or tokens and degrade retrieval when lexical cues and semantic states require different preservation. We introduce DepthWeave-KV, a token-adaptive cache compression method that factorizes key and value states across neighboring transformer layers using shared low-rank channel bases while retaining lightweight token-specific residuals where attention behavior is sensitive. DepthWeave-KV combines cross-depth residual factorization with a token-conditional depth router that allocates higher reconstruction rank to instruction-bearing and retrieval-critical tokens, and uses calibration-free online error tracking from attention-output probes to adapt compression during generation without retraining the base model. A fused CUDA implementation jointly performs basis lookup, residual dequantization, and attention projection to reduce decode-time memory traffic. Across LongBench, Needle-in-a-Haystack, L-Eval, and long-form QA and summarization benchmarks, DepthWeave-KV achieves near-full-cache task quality with substantially lower memory use, improving average score and retrieval accuracy over prior compressed caches while reaching 8.3x KV memory reduction and 72.8 tokens per second at 64K context.

---

## 论文详细总结（自动生成）

# 深度分析总结：DepthWeave-KV

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：长上下文语言模型推理时，KV 缓存成为内存带宽和容量的主要瓶颈。现有压缩方法（如令牌驱逐、量化、层共享）通常对**所有层和所有令牌**施加统一的压缩预算，忽略了不同深度层（词汇层 vs 语义层）以及不同令牌（指令性 vs 流畅性承接）对缓存保真度需求的异质性。
- **后果**：在需要精确检索（如 Needle-in-a-Haystack、多跳问答）的任务中，统一压缩会导致严重的性能下降，而平均困惑度或短上下文指标可能掩盖这种退化。
- **研究含义**：需要一种**同时感知深度和令牌重要性**的压缩机制，能够在线适应生成过程中的注意力行为变化，且不需要重新训练基础模型。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
DepthWeave-KV 用**跨层残差分解**替代逐层独立存储 KV：
- 在相邻 Transformer 层（深度窗口）内，共享一组低秩通道基（low-rank channel bases）。
- 每层每个令牌的 KV 状态由共享基的线性组合加上一个**令牌特定的量化残差**重建，残差的秩由**令牌条件深度路由器**动态分配。
- 通过**注意力输出探针**在线监测重建误差，自适应调整压缩率。

### 关键技术细节
1. **跨深度残差分解**  
   对于深度窗口 \(W_m\) 和注意力头 \(h\)，存储共享基 \(B^{K}_{m,h}, B^{V}_{m,h} \in \mathbb{R}^{r_b \times d}\)。令牌 \(t\) 在层 \(\ell \in W_m\) 的重建键/值：
   \[
   \hat{X}_{\ell,h,t} = A_{\ell,h,t} B_{m,h} + g_{\ell,h,t} R_{\ell,h,t}, \quad X \in \{K,V\}
   \]
   - \(A\)：紧凑系数向量（8-bit 块量化）
   - \(R\)：量化残差（4-bit 分组量化，仅路由到的秩保留）
   - \(g\)：轻量级残差门（0~1）
   - 共享基从第一个前缀块在线初始化，并用指数移动平均更新。

2. **令牌条件深度路由器**  
   每个令牌的残差秩 \(\rho_{m,h,t}\) 由四个在线特征计算：累计注意力质量、最大注意力尖峰、分界符/指令指示器、最近探针窗口的注意力输出重建误差。路由器为浅层参数，使用三个等级（ρ=0,2,4,8），阈值 \(\tau_m\) 根据内存预算自适应调整。

3. **免校准在线误差跟踪**  
   每 \(p\) 个解码步，对部分头/查询位置执行两次注意力计算（一次压缩重建，一次临时高保真缓存）。测量注意力输出的相对误差 \(e_{\ell,h}\)：
   \[
   e = \frac{\|\text{Attn}(Q,K,V) - \text{Attn}(Q,\hat{K},\hat{V})\|_2}{\|\text{Attn}(Q,K,V)\|_2 + \epsilon}
   \]
   若误差超过容忍值，降低阈值 \(\tau_m\)，为更多令牌分配更高残差秩；反之则提高阈值，减少残差存储。

4. **融合 CUDA 内核**  
   在解码时，融合内核一次性完成基查找、残差反量化、系数乘法和注意力投影，避免将重建的 KV 张量写回全局内存，减少内存流量。

## 3. 实验设计

### 数据集与任务
- **检索/提取式**：Needle-in-a-Haystack（16K~128K 上下文）、Qasper、HotpotQA、MultiFieldQA-en、TriviaQA
- **问答/长格式**：LongBench（平均归一化得分）、L-Eval、NarrativeQA
- **摘要**：GovReport、QMSum

### 评估指标
- 平均任务得分（%）、Needle 精确检索准确率（%）
- KV 内存缩减倍数（×）、解码吞吐量（tok/s）、首令牌时间（TTFT, s）
- 困惑度增量（ΔPPL）、注意力输出重建误差

### 对比的基线方法
| 类别 | 方法 |
|------|------|
| 全缓存 | Full KV Cache |
| 令牌驱逐 | StreamingLLM, H2O, SnapKV, PyramidKV |
| 层共享 | MiniCache, KVSharer |
| 语义/混合 | ChunkKV, TailorKV |
| 低秩注意力 | Eigen Attention |

### 实验设置
- 基础模型、分词器、提示格式、解码参数保持一致。
- 默认：64K 预填充长度，贪心解码生成 512 个令牌。
- 压缩方法配置为：在至少 4 倍内存缩减预算下追求最高任务得分。

## 4. 资源与算力

- **文中未明确说明**所使用的 GPU 型号、数量、训练时长（如“fused CUDA implementation”仅提到实现细节，未报告硬件规模）。
- 仅提到训练一次残差门和路由器参数（使用冻结教师缓存目标），但未给出具体硬件或时间。
- 评估部分提到“64K context”下测量，但未注明单卡还是多卡。

## 5. 实验数量与充分性

### 实验数量
- **主实验**（表 1）：10 种方法在 10 个数据集上的对比，报告了 7 项指标。
- **消融实验**（表 2）：6 种变体（去除跨层基共享、去除局部路由器、去除在线误差跟踪、去除残差门、仅共享基无残差、去除融合内核），在平均得分、Needle 精度和内存缩减上对比。
- **任务家族分解**：分别报告提取式/检索任务和摘要任务的平均得分。
- 所有实验在 64K 上下文下进行，消融中允许内存控制器重新调优阈值以追求最佳质量。

### 充分性与公平性
- **充分**：覆盖了主流长上下文基准和多种压缩范式，消融验证了每个组件的必要性。
- **客观**：对比方法均使用相同的基础模型和解码参数，压缩方法均按各自最佳配置运行（至少 4 倍压缩）。
- **潜在不足**：未报告多次运行的标准差或显著性检验；未对不同上下文长度（如 128K）单独列出结果（Needle 精度已覆盖 16K~128K，但其他指标仅在 64K 下报告）。

## 6. 论文的主要结论与发现

1. **质量接近全缓存**：DepthWeave-KV 平均得分 62.9%（仅比 Full KV Cache 低 0.9 分），Needle 准确率 96.1%（比 TailorKV 高 3.5 分）。
2. **显著的内存和吞吐收益**：实现 8.3 倍 KV 内存缩减，解码吞吐 72.8 tok/s（比 TailorKV 高 12%），TTFT 降低至 4.89 s。
3. **检索鲁棒性**：在提取式/检索任务上，DepthWeave-KV 与全缓存的差距仅为 1.4 分，远小于其他压缩方法（MiniCache 5.6 分，TailorKV 3.2 分）。
4. **消融证实关键组件**：去除 token 自适应路由后 Needle 准确率从 96.1% 降至 92.4%；去除在线误差跟踪后降至 89.9%；仅共享基无残差时平均得分下降 4.0 分。

## 7. 优点

- **创新性**：首次将跨层残差分解与 token 自适应残差分配结合，解决了“深度浪费”和“令牌浪费”的双重问题。
- **免校准适应**：利用注意力输出探针在线调整压缩率，无需任务特定校准数据或模型重训练，实用性强。
- **系统协同**：融合 CUDA 内核将重建与注意力计算合并，减少内存流量，带来实际吞吐提升。
- **可解释性**：路由器使用的特征（注意力质量、尖峰、指令指示器等）直观且易于验证。
- **完整性**：在多个公认基准上对标了广泛基线，消融实验充分验证设计选择。

## 8. 不足与局限

- **固定的深度窗口**：假设相邻层冗余局部性，可能不适合层角色异质性强的模型（如混合注意力架构）。未来可学习非连续分组或动态基选择。
- **路由器目标并非直接对齐下游风险**：在线误差基于注意力输出重建，但检索/指令遵循失败可能由罕见令牌引起，仅在后期解码才暴露。路由器可能无法提前预判。
- **未涉及生产环境集成**：未讨论与批处理、前缀复用、预填充/解码分离、多租户缓存池的协调。需要与压缩感知内存控制器配合。
- **实验范围限制**：
  - 仅在 64K 上下文下系统报告所有指标，未展示不同上下文长度（如 32K、128K）下的对比。
  - 未报告多次运行的标准差或统计显著性，无法评估方法稳定性。
  - 未在更大模型（如 70B+）上验证，小模型上的趋势可能不直接外推。
- **训练开销**：虽然不需要重训基础模型，但残差门和路由器参数仍需一次训练（未说明训练成本）。在线探针增加少量计算开销。

（完）
