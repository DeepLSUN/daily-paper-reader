---
title: Harnessing Code Agents for Automatic Software Verification
title_zh: 利用代码代理实现自动软件验证
authors: "Shuangxiang Kan, Shuanglong Kan, Sebastian Ertel"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06341v1"
tags: ["query:agent"]
score: 9.0
evidence: 使用LLM代码代理进行自动形式验证
tldr: 形式化验证能保证软件正确性，但交互式定理证明需要大量专家努力。现有LLM方法采用固定策略，只能证明少量定理。本文提出将整个引理交给通用LLM代码代理（如Claude Code），并封装在验证框架中，让代理自由选择证明策略。在Iris上自动证明了全部4257个引理，在reglang上全部318个，在iris-lean上证明72个未移植引理。该方法更简单高效，实现了完全自动覆盖，无需专家干预。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06341v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 694, \"height\": 622, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06341v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 1068, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06341v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 841, \"height\": 679, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06341v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 784, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06341v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 785, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06341v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 833, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06341v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 911, \"height\": 374, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06341v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06341v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 631, \"height\": 375, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06341v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 682, \"height\": 229, \"label\": \"Table\"}]"
motivation: 现有LLM形式化验证方法受限于固定策略，证明成功率低，需大量专家介入。
method: 将整个引理交给通用LLM代码代理，配合验证框架提供反馈和硬约束，代理自由选择证明策略。
result: 在Iris上自动证明全部4257个引理，在reglang上全部318个，在iris-lean上证明72个未移植引理。
conclusion: 通用代码代理方法更简单有效，无需专家干预，实现完全自动验证，具有通用性。
---

## 摘要
形式化验证提供了最强的软件正确性保证，但它无法扩展：交互式定理证明器（如Coq）所需的证明需要巨大的专家工作量。大语言模型（LLM）有望自动生成这些证明，但现有方法将固定的人工设计的证明策略嵌入系统，并约束模型遵循该策略（逐步检索前提和预测策略，或通过分治法拆分目标），但仍只能证明其目标定理的一小部分。我们表明，强加这样的策略是不必要且具有局限性的。将整个引理交给一个通用的LLM代码代理（例如Claude Code），让其自由选择自己的方法，并将其封装在一个验证框架中，既更简单又更有效，实现了完全覆盖：每个目标引理都被证明，没有失败，也不需要Coq专家干预。代理在框架的反馈和硬约束下编写证明，这些约束确保每个证明都是可靠的（仅当证明器的内核关闭时才接受）、完整的（没有未证明的义务或静默丢弃）和终止的（没有发散的策略）。我们从三个维度评估这个框架加代码代理：（1）核心逻辑：在Iris（用于并发和内存操作程序的最先进的分离逻辑）上，Aria完全自动地证明了四个核心模块的所有4,257个引理以及基于它构建的Rust标准库的217个引理。（2）与先前的LLM证明器比较：在reglang上，先前的证明器仅能证明八分之一，而Aria证明了所有318个。（3）通用性：在iris-lean（Iris未完成的Lean 4移植）上，它证明了72个尚未移植的引理，表明该方法不特定于Coq。最先进的模型（Claude Opus 4.7）可以完全自动地为经过验证的软件开发编写证明。

## Abstract
Formal verification offers the strongest guarantee of software correctness, but it does not scale: the proofs demanded by interactive theorem provers such as Coq require enormous expert effort. Large language models (LLMs) promise to generate these proofs automatically, yet existing approaches wire a fixed, human-designed proof strategy into the system and constrain the model to follow it (retrieving premises and predicting tactics one step at a time, or splitting goals by divide-and-conquer), and still prove only a fraction of their target theorems.   We show that imposing such a strategy is unnecessary and limiting. Handing the whole lemma to a general LLM code agent (for example, Claude Code), free to choose its own approach, and wrapping it in a verification harness is both simpler and more effective, achieving full coverage: every targeted lemma proved, with no failures and no Coq expert intervention. The agent writes the proofs under feedback and hard constraints from the harness that keep each one sound (accepted only when the prover's kernel closes it), complete (no obligation left unproved or silently dropped), and terminating (no divergent tactics).   We evaluate this harness plus code agent along three dimensions. (1) Core logic: on Iris, the state-of-the-art separation logic for concurrent and memory-manipulating programs, Aria proves all 4,257 lemmas of the four core modules and the 217 lemmas verifying Rust's standard libraries built on it, fully automatically. (2) Comparison with prior LLM provers: on reglang, where prior provers manage barely one in eight, Aria proves all 318. (3) Generality: on iris-lean, the unfinished Lean 4 port of Iris, it proves 72 not-yet-ported lemmas, showing the approach is not specific to Coq. A state-of-the-art model (Claude Opus 4.7) can write proofs for verified software development fully and automatically.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：形式化验证能提供最强的软件正确性保证，但交互式定理证明（如Coq）需要高度专业的专家手动编写证明，成本极高，严重限制了形式化验证在工业界的推广。特别是分离逻辑（如Iris）的证明极其困难，一个引理可能花费专家数小时甚至数天。
- **背景**：已有LLM方法尝试自动生成证明，但受限于固定的人为设计的证明策略（如逐步预测策略、分治策略等），仅能证明12%~48%的目标定理，且未涉及最难的并发分离逻辑领域。
- **整体含义**：本文提出将整个引理交给通用LLM代码代理（如Claude Code），由代理自由选择证明策略，配合验证框架的硬约束和反馈循环，实现了对最难的分离逻辑库（Iris）的全覆盖自动证明，首次证明通用代码代理可以替代人工设计的复杂策略，突破形式化验证的可扩展性瓶颈。

## 2. 方法论：核心思想与关键技术细节

- **核心思想**：不预设任何Coq特定的证明策略，将整个引理交给通用LLM代码代理（Claude Code），由代理自主决定如何构建证明。代理在验证框架（harness）的约束下工作：框架使用Coq内核验证每个候选证明，通过精确反馈（错误行号、错误信息、待证明目标）驱动代理迭代修正，直到证明被内核接受（最多30次重试）。
- **关键技术细节**：
    - **三层架构**：
        - 模型层（Model Layer）：LLM后端（如Claude Opus 4.7），生成证明候选。
        - 代理层（Agent Layer）：负责编排、上下文组装、策略决策（选择引理攻击顺序、检索相似证明等），以及重试循环。
        - 框架层（Harness Layer）：信任锚点，包含Coq内核验证、发散策略超时（每个策略300秒）、禁止Admitted/Admit、引理覆盖检查、Iris代码风格检查、Shell命令安全筛选。
    - **HHL语言**：声明式语言（Harness Hook Language），将框架编写为可复用、可审计的工件。通过hook机制在代理工具调用前后插入策略检查（如禁止引入新模块、禁止运行rm命令等）。HHL编译为Python代码并驱动Claude Code SDK。
    - **多代理协作**：提取器（Extractor）找出未证明引理；证明器（Prover）首次尝试；修复器（Fixer）根据错误反馈修正；打磨器（Polish）对已通过的证明进行风格优化（最多2轮），确保不破坏正确性。
    - **会话管理**：同一文件内连续最多6个引理共享一个会话窗口，利用相邻引理的结构相似性；新文件重置会话；打磨器使用独立会话，不与证明器共享历史。
    - **反馈机制**：每次拒绝返回精确错误（行号、错误信息、目标状态），而非简单“失败”，使模型能针对性修复。

## 3. 实验设计

- **数据集/场景**：
    1. **Iris核心模块**：四个核心模块（algebra、bi、base_logic、program_logic），共4,257个引理，113个源文件。这是最难的并发分离逻辑库。
    2. **Rust标准库验证（RustBelt）**：Iris之上的实际软件验证，包括Arc、Rc、Mutex、RwLock、RefCell、Cell等，共217个引理。
    3. **reglang**：正则语言理论库，318个引理。该库是CoqStoq基准中先前LLM证明器表现最差的项目（仅12.5%）。
    4. **iris-lean**：Iris到Lean 4的不完整移植，选取3个尚未移植的文件（72个引理），检测方法对不同证明器的通用性。
- **基准方法**：对比表I中列出的10种学习和LLM方法（ASTactic、Proverbot9001、Graph2Tac、PALM、COPRA、Rango、Cobblestone、ProofCoop等），它们在CoqGym/CoqStoq/CompCert等基准上的成功率（12%~48%）。
- **评估指标**：每个引理是否被证明、首次尝试成功率、平均重试次数、最大重试次数、平均模型时间。

## 4. 资源与算力

- 论文**未明确说明**具体使用的GPU型号、数量或传统意义上的训练算力。由于方法基于闭源商用LLM（Claude Opus 4.7）的推理，而非训练，因此不消耗训练算力。
- 运行时长：Iris核心模块总共约380小时模型时间（约16天），平均每个引理321秒；RustBelt平均706秒；reglang平均365秒；iris-lean平均97秒（总<2小时）。
- 费用模式：使用了Claude Code 20×订阅（而非按token计费的API），大幅降低了成本。
- 开源模型对比：Kimi K2.6（通过Ollama Cloud）在子集40个引理上全部通过，但平均时间长7倍，最大重试次数24次；DeepSeek V4 Pro类似。但长证明（超过50行）时开源模型经常失败。

## 5. 实验数量与充分性

- **实验数量**：共4组主要实验，覆盖三个Coq项目和一个Lean项目。核心实验Iris全量4257个引理（而非抽样）；另外包括217个RustBelt引理、318个reglang引理、72个iris-lean引理。
- **充分性**：
    - 全面覆盖Iris核心模块全部引理，避免抽样偏差。
    - 包含不同难度层次（从简单正则语言到最难分离逻辑）。
    - 包含不同证明器（Coq vs Lean），验证通用性。
    - 包含与先前方法的直接比较（reglang上先前方法仅1/8，本文100%）。
    - 包含私有模型与开源模型对比（40引理子集）。
    - 包含风格打磨实验，展示可优化性。
- **公平性**：所有比较均基于相同协议：删除原始证明，代理从头生成，且代理不能联网、不能从git恢复。但缺少与同期最强模型（如GPT-4、Gemini）在相同数据上的直接横向比较。

## 6. 主要结论与发现

1. **完全覆盖**：Aria在Iris核心4,257个引理、RustBelt 217个引理、reglang 318个引理、iris-lean 72个引理上均实现**零失败**，所有引理全部自动证明。
2. **首次尝试率高**：Iris首次尝试成功率79.2%，RustBelt 72.8%，reglang 75.5%，iris-lean 90.3%；平均重试次数均<1。
3. **通用性**：方法不仅适用于Coq，也适用于Lean（仅需切换内核验证器）；不仅适用于分离逻辑，也适用于普通软件验证（reglang）。
4. **开源模型可用但效率低**：Kimi K2.6和DeepSeek V4 Pro在短证明上能全部完成，但长证明（>50行）经常失败，且耗时显著增加。
5. **证明策略可自动学习**：代理自然地通过模仿附近相似引理的证明来构建新证明，无需人工指定策略。

## 7. 优点

- **简洁性**：放弃复杂的人为设计策略，仅依靠通用代码代理+验证框架的简单循环，却达到更高成功率。
- **完整性保证**：框架不仅验证正确性，还检查引理是否被静默丢弃或削弱（通过引理覆盖检查），防止LLM“作弊”。
- **可审计性**：HHL语言使框架编写为可复用、可读的声明式工件，便于审查和移植。
- **通用性**：支持不同LLM后端、不同证明器（Coq/Lean）、不同软件开发领域。
- **自动化程度高**：从启动到最后完成无需任何人干预（仅需启动一次）。
- **首次证明对Iris的完全自动覆盖**，这是先前所有方法未能达到的。

## 8. 不足与局限

- **依赖商用模型**：主要结果基于Claude Opus 4.7，该模型并非开源，费用和可获取性有限。开源模型虽可行但在长证明上能力不足。
- **计算成本**：380小时模型推理时间对大规模库仍然较高，但相比专家手工编写（可能需要数人年）已有巨大提升。
- **实验对比局限性**：与先前方法的比较主要在reglang（先前方法最差项目）上，未在Iris相同基准上直接对比（因为先前方法从未尝试Iris这样难度的库）。缺乏与最新LLM（如GPT-4、Gemini 2.0）的横向比较。
- **内部偏差风险**：作者可能利用了Claude Code的特定优势（如长上下文、强大推理能力），结果不一定能直接迁移到其他代理框架。
- **覆盖范围有限**：仅针对四个核心模块，未测试Iris全部扩展模块（如heap_lang、proofmode等）。未评估规范生成（specification generation），仅聚焦证明生成。
- **风格打磨仅浅尝辄止**：仅2轮打磨，可能不足以完全匹配人类专家风格；软约束可能无法完全消除冗长的证明。
- **未讨论失败情况**：虽然宣称零失败，但未详细解释reglang中nfa_ofP引理耗时5.8小时（>Iris最难的4.7小时）的原因，也未分析重试次数高的典型模式。
- **可重复性**：商业模型版本变化可能影响结果，闭源模型意味着他人无法精确复现。
- **实际工程应用门槛**：目前需要完整项目环境和opam依赖安装，尚未成为可直接嵌入CI/CD的轻量工具。

（完）
