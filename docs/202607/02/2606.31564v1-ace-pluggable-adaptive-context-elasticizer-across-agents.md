---
title: "ACE: Pluggable Adaptive Context Elasticizer across Agents"
title_zh: ACE：可插拔的跨智能体自适应上下文弹性器
authors: "Ning Liao, Zihao Long, Xiaoxing Wang, Xue Yang, Yaoming Wang, Ziyuan Zhuang, Xunliang Cai, Rongxiang Weng, Junchi Yan"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31564v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向LLM智能体的可插拔上下文弹性扩展模块
tldr: 智能体任务轨迹不断增长，固定上下文窗口面临挑战。现有截断和摘要方法不可逆，一旦丢弃信息便无法恢复。ACE模块通过无损消息维护层存储原始和压缩信息，再由上下文编排层根据当前状态动态分配每一步为原始、摘要或丢弃类型，实现可逆弹性上下文管理。在ReAct、DeepAgent等四个框架上，ACE一致优于截断和摘要基线，显著提升性能，且即插即用无需训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31564v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1502, \"height\": 722, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31564v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1700, \"height\": 563, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31564v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1653, \"height\": 854, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31564v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1653, \"height\": 598, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31564v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1652, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31564v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1639, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31564v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1355, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31564v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 818, \"height\": 600, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31564v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 817, \"height\": 441, \"label\": \"Table\"}]"
motivation: 现有上下文管理方法（如截断、摘要）不可逆，丢弃的信息在后续决策中可能重新需要，导致性能损失。
method: ACE包含无损消息维护层（存储原始和压缩历史）和上下文编排层（按状态动态分配每步类型：原始/摘要/丢弃），实现可逆的弹性上下文生成。
result: 在ReAct、DeepAgent、WebThinker和MiroFlow四个框架上，ACE一致优于截断和摘要基线，带来稳定性能提升。
conclusion: ACE作为即插即用模块，无需训练或架构修改，即可有效提升长轨迹下LLM智能体的上下文利用效率。
---

## 摘要
智能体任务的日益复杂导致轨迹长度迅速增长，这对基于固定上下文窗口的大语言模型智能体构成了重大挑战。现有上下文管理技术（如截断与摘要）存在固有的不灵活性和不可逆性：一旦信息被丢弃或压缩，即使在后来的决策步骤中变得关键相关，也无法恢复。针对这些局限性，我们提出了自适应上下文弹性器（ACE），一种即插即用模块，在每一步决策时弹性地将历史步骤信息编排到智能体的上下文中。ACE维护一个无损消息维护层，存储每个历史步骤的原始消息和压缩抽象，同时一个上下文编排层基于当前任务状态，在每一步决策时自适应地为每个步骤分配弹性类型（原始、抽象或丢弃）。这种可逆设计确保主LLM始终获得紧凑且信息丰富的上下文。我们将ACE适配到四种不同的智能体框架（包括ReAct、DeepAgent、WebThinker和MiroFlow）中，无需训练或架构修改。实验表明，ACE始终优于截断与摘要基线，并在所有四种智能体框架上带来一致的性能提升。

## Abstract
The increasing complexity of agentic tasks has led to rapidly growing trajectory lengths, which poses significant challenges for large language model (LLM) based agents with fixed context windows. Existing context management techniques, such as truncation and summarization, suffer from inherent inflexibility and irreversibility: once information is discarded or compressed, it cannot be recovered even when it becomes critically relevant in later decision steps. To address these limitations, we propose the Adaptive Context Elasticizer (ACE), a plug-and-play module that elastically orchestrates historical step information into the agent's context at each decision step. ACE maintains a lossless message maintenance layer that stores both raw messages and compressed abstractions for each historical step, while a context orchestration layer adaptively assigns each step an elastic type as raw, abstract, or drop, at every decision step based on the current task state. This reversible design ensures that the main LLM always receives a compact yet information-rich context. We adapt ACE to four diverse agent frameworks, including ReAct, DeepAgent, WebThinker, and MiroFlow, without training or architectural modifications. Experiments show that ACE consistently outperforms truncation and summarization baselines, and brings consistent performance gains across all four agent frameworks.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 一、核心问题与整体含义（研究动机和背景）

- **问题**：随着智能体任务复杂度的增加，轨迹长度不断增长。基于固定上下文窗口的大语言模型（LLM）会遭遇信息密度下降、注意力分散、推理质量退化等问题。
- **现有方法缺陷**：截断和摘要方法存在 **不灵活** 和 **不可逆** 的缺陷：一旦信息被丢弃或压缩，即使后续决策步骤中该信息变得关键，也无法恢复。所有压缩方法（阈值触发、循环压缩、智能体控制）都默认一次决策即是最终决定。
- **研究动机**：需要一种能在每个决策步骤根据当前任务状态，自适应地调整历史信息呈现形式的上下文管理机制，实现可逆、灵活的信息编排。
- **整体含义**：ACE 旨在作为即插即用模块，在不改变原智能体框架的前提下，提升上下文的信息密度，从而改善长轨迹下的决策性能。

## 二、方法论

### 核心思想
- **解耦实际上下文与原始消息**：设计两层结构。
    - **消息维护层 (Message Maintenance Layer)**：无损存储每个历史步骤的原始消息 `h_raw_i` 和压缩抽象 `h_abs_i`（由辅助 LLM 生成）。该层永不修改。
    - **上下文编排层 (Context Orchestration Layer)**：在每个决策步骤，根据当前任务状态，为每个历史步骤分配弹性类型（`Raw`, `Abstract`, `Drop`），并据此构建实际上下文 `C_t`。

### 关键技术细节
- **弹性类型决策**：由弹性器 `E`（与主 LLM 相同）执行，输入当前任务描述 `T` 和所有历史步骤的抽象 `{h_abs_i}`，输出每个历史步骤的决策 `d_t^i`。
- **上下文构建**：上下文 = 任务描述 `T` + 非边界步骤弹性编排结果 + 最后一步的原始消息 `h_raw_{t-1}`（固定保留原始形式）。
- **可逆性**：由于消息维护层不变，之前被压缩或丢弃的步骤可以在后来的步骤中重新以原始形式引入。

### 即插即用集成
- ACE 作为外部包装器，不修改智能体的动作空间、消息流或控制流，无需训练。
- 适配了 ReAct、DeepAgent、WebThinker、MiroFlow 四种框架（涵盖单智能体、主-辅助、多智能体架构）。

## 三、实验设计

### 数据集与基准
- **主对比实验**：GAIA（Text/MM/File 子集）、HLE（Text/MM）、WebShop（Success Score）
- **适配其他框架的基准**：
    - DeepAgent：GAIA、HLE、WebShop
    - WebThinker：GAIA、WebWalkerQA、HLE
    - MiroFlow：GAIA、xBench-DS、BrowseComp-ZH

### 主 LLM
- GPT-4.1 和 Gemini-3.1-flash-lite-preview

### 对比方法
- **基线**：无上下文管理（纯 ReAct）、截断（保留最近5步）、摘要（最近5步保留 + 其余摘要）
- **消融设置**：
    - 弹性类型组合：{Raw, Abs}、{Raw, Drop}、{Abs, Drop} vs 完整 {Raw, Abs, Drop}
    - 最后一步是否弹性化 vs 固定保留 raw

## 四、资源与算力

- 论文 **未明确说明** 使用的 GPU 型号、数量、训练时长。
- 所有实验均基于 API 调用（GPT-4.1、Gemini-3.1-flash-lite-preview、GPT-4o 用于辅助摘要），无额外训练。
- 因此无法评估训练资源开销。

## 五、实验数量与充分性

- **充分性**：实验覆盖了 4 种不同架构的智能体框架、多个基准（GAIA, HLE, WebShop, WebWalkerQA, xBench-DS, BrowseComp-ZH），且包含不同主 LLM（2种）。总计 6 张表 + 2 张图的分析。
- **客观性**：对比了三种基线（无管理、截断、摘要），并在相同条件下重复。消融实验验证了各设计成分的必要性。
- **不足**：未在更多样化的长任务场景（如多步工具调用、更复杂的推理链）上验证；基线仅包含简单截断和摘要，未对比更先进的压缩方法（如 ACON, AgentFold）。可能存在选择偏差。

## 六、主要结论与发现

1. ACE **一致优于** 截断和摘要基线，在 GAIA 上提升 3.6~6.6 个点，在 HLE 和 WebShop 上也有稳定提升。
2. ACE **即插即用有效**：在 DeepAgent、WebThinker、MiroFlow 三种框架上均带来性能提升（GAIA 平均提升 2~10 个点）。
3. **消融验证**：
    - 三种弹性类型均必须，缺少任一种都会导致性能下降。
    - 固定保留最后一步为 raw 比弹性化更好（避免误导最新状态）。
4. 输入 token 长度分析显示 ACE 能自适应控制上下文长度，且不会像截断/摘要那样导致信息不可逆丢失。

## 七、优点

1. **创新性**：首次提出“可逆弹性上下文”概念，解决了现有方法不可逆的痛点。
2. **通用性**：适配四种不同架构的智能体框架，无需修改原框架，无需训练。
3. **实用性**：即插即用，可直接集成到现有智能体系统中提升性能，成本低（仅增加辅助摘要的 API 调用）。
4. **实验设计全面**：涵盖多任务、多模型、多框架、多消融。

## 八、不足与局限

1. **实验覆盖有限**：基准主要集中于 GAIA、HLE、WebShop，缺乏对更复杂长期任务（如多轮交互、代码生成等）的评估。
2. **基线不够强**：仅对比简单截断和摘要，未与 ACON、AgentFold（同样可逆/自适应方法）直接对比。
3. **资源消耗未量化**：未报告辅助摘要 LLM（GPT-4o）的调用成本及延迟开销，可能在实际部署中带来额外成本。
4. **弹性器决策可靠性**：弹性器（主 LLM）可能偶尔做出错误决策（如错误地丢弃关键信息），论文未分析这种风险及其影响。
5. **偏差风险**：所有实验使用 GPT-4o 作为辅助 LLM，未验证其他摘要模型的影响；主 LLM 仅两种，泛化性需更多验证。
6. **理论分析缺失**：未形式化证明“信息密度最大化”或“性能保证”，仅依靠实证。

（完）
