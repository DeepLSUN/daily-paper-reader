---
title: "Focus Is All You Need: Adaptive Goal-aware Attention Orchestration for Multi-Agent Graph Systems"
title_zh: 焦点即一切：面向多智能体图系统的自适应目标感知注意力编排
authors: "Mingzhou Fan, Siyuan Xu, Mingxuan Yuan"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23678v1"
tags: ["query:agent"]
score: 9.0
evidence: 面向多智能体系统的自适应注意力编排框架
tldr: 多智能体图系统在任务执行时面临注意力分配问题，现有方法均匀执行所有节点导致资源浪费。为此提出自适应目标感知注意编排（AGAO），通过目标感知注意、拓扑感知注意和资源感知注意动态评估智能体重要性，将计算聚焦于关键推理路径。实验表明，AGAO在提升任务效果的同时，降低了计算开销、延迟和令牌消耗。该工作确立了注意力工程作为可扩展多智能体系统的新方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23678v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1432, \"height\": 454, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23678v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1361, \"height\": 923, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23678v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1400, \"height\": 244, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23678v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1455, \"height\": 286, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 981, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 301, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23678v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1059, \"height\": 304, \"label\": \"Table\"}]"
motivation: 图编排的多智能体系统随工作流增长，均匀执行低相关任务浪费资源，需动态分配注意力。
method: AGAO包含三种注意机制：目标感知注意衡量语义相关性，拓扑感知注意建模依赖，资源感知注意分配预算与优先级。
result: 相比现有策略，AGAO提升任务效果，并减少不必要的计算、延迟和令牌消耗。
conclusion: 注意力工程为构建可扩展、智能的多智能体系统提供了新范式。
---

## 摘要
大语言模型（LLM）使自主智能体能够进行推理、规划和工具使用。最近的系统越来越将这些智能体组织为专门化、互连节点的图。虽然基于图的编排支持灵活的分解和协调，但它创造了一个关键挑战：注意力分配。随着工作流的增长，现有方法通常均匀地执行图组件，浪费资源在与目标无关或低影响的任务上。我们引入了注意力编排，这是一种将Transformer风格的注意力从令牌表示扩展到工作流级智能体协调的范式。我们的框架，自适应目标感知注意力编排（AGAO），根据用户目标、图依赖和计算约束动态估计智能体重要性。AGAO结合了三个组件：（1）目标感知注意力，衡量用户目标与智能体能力之间的语义相关性；（2）拓扑感知注意力，建模智能体图中的结构依赖；（3）资源感知注意力，跨异构智能体分配预算和执行优先级。这些机制共同将静态智能体图转变为自适应系统，将计算集中在目标关键推理路径上。跨多种多智能体工作负载的实验表明，与现有的基于图的执行策略相比，AGAO提高了任务效果，同时减少了不必要的计算、延迟和令牌消耗。我们的工作将注意力工程确立为可扩展、智能多智能体系统的一个方向。代码：https://github.com/MingzhouFan97/AGAO。

## Abstract
Large language models (LLMs) enable autonomous agents for reasoning, planning, and tool use. Recent systems increasingly organize these agents as graphs of specialized, interconnected nodes. Although graph-based orchestration supports flexible decomposition and coordination, it creates a key challenge: \textbf{attention allocation}. As workflows grow, existing approaches often execute graph components uniformly, wasting resources on irrelevant or low-impact tasks. We introduce \textbf{Attention Orchestration}, a paradigm that extends Transformer-style attention from token representations to workflow-level agent coordination. Our framework, \textbf{Adaptive Goal-aware Attention Orchestration (AGAO)}, dynamically estimates agent importance based on user objectives, graph dependencies, and computational constraints. AGAO combines three components: (1) goal-aware attention, measuring semantic relevance between user goals and agent capabilities; (2) topology-aware attention, modeling structural dependencies in agent graphs; and (3) resource-aware attention, allocating budgets and execution priorities across heterogeneous agents. Together, these mechanisms transform static agent graphs into adaptive systems that focus computation on goal-critical reasoning paths. Experiments across diverse multi-agent workloads show that AGAO improves task effectiveness while reducing unnecessary computation, latency, and token consumption compared with existing graph-based execution strategies. Our work establishes \textbf{Attention Engineering} as a direction for scalable, intelligent multi-agent systems. Code: https://github.com/MingzhouFan97/AGAO.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：大语言模型（LLM）驱动的自主智能体系统正从单智能体循环（Loop Engineering）向多智能体图系统（Graph Engineering）演进。图结构虽能显式建模任务分解与依赖关系，但面临一个根本性挑战：**注意力稀释（Attention Dilution）**。随着智能体和工作流节点数量增长，现有系统往往均匀执行图中的所有节点，将计算资源浪费在与目标无关或低影响的任务上。
- **核心问题**：图编排系统能够确定任务“是什么”以及“如何连接”，但缺乏能力确定“哪些任务当前值得关注”。静态的图执行策略无法捕捉用户目标演化过程中智能体重要性的动态变化。
- **整体含义**：作者提出，未来智能体系统不仅需要推理能力，还需要**动态分配计算焦点**的能力。他们借鉴Transformer的注意力机制，将其从令牌级表示学习扩展到工作流级智能体协调，提出“注意力工程（Attention Engineering）”新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将注意力机制引入编排层，实现**自适应目标感知的注意力编排（AGAO）**。AGAO在用户目标与智能体图执行之间插入一个注意力驱动的执行层，动态评估每个智能体的重要性，并据此调整执行路径、资源分配和优先级。
- **关键技术细节**——三个互补的注意力组件：
    1. **目标感知注意力（Goal-aware Attention）**：计算用户目标与智能体能力之间的语义对齐。将目标编码为查询向量 \(Q_g\)，智能体能力描述编码为键值对 \((K_i, V_i)\)，然后通过缩放点积注意力计算注意力分数 \(\alpha_i^{\text{goal}}\)，实现按语义重要性加权。
    2. **拓扑感知注意力（Topology-aware Attention）**：融入图结构信息（依赖距离、前驱/后继影响、关键路径贡献等），生成拓扑偏差矩阵 \(B_G\)，与标准注意力结合：\(A_{\text{topo}} = \text{softmax}\left(\frac{QK^T}{\sqrt{d}} + B_G\right)\)。避免纯语义剪枝导致丢失结构性关键节点。
    3. **资源感知注意力（Resource-aware Attention）**：将综合注意力分数转化为具体执行策略，包括模型路由（大/中/小型模型）、令牌预算分配和优先级调度，在总计算预算 \(B\) 约束下最大化效用。
- **算法流程**（文字描述）：
    1. 初始化执行状态和注意力分布。
    2. 编码用户目标。
    3. 循环直到任务完成：
        - 计算目标感知注意力和拓扑感知注意力。
        - 融合注意力信号。
        - 通过资源分配器分配计算资源。
        - 选择注意力高于阈值 \(\tau_e\) 的智能体为活跃节点。
        - 执行选定智能体并观察反馈。
        - 根据反馈更新注意力状态（自适应图路由），允许注意力动态演化。
    4. 聚合所有输出得到最终响应。
- **自适应图路由**：注意力状态根据中间观测结果动态更新，公式 \(\alpha^{t+1} = \Phi(\alpha^t, o^t, G, q)\)，分解为目标、拓扑和反馈三部分，实现智能体可进入、暂停或剪枝。

## 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法

- **数据集与场景**：
    - **MBPP编程任务**：8个Python代码生成任务。
    - **HotpotQA多跳问答任务**：8个需要多步推理的问答任务。
    - 每个任务使用手写的9个节点的图结构：编程图包含规范分析、实现、测试、调试、审查、完成等角色；问答图包含分解、检索、证据选择、推理、验证、完成等角色。
- **基准测试**：任务清单、图模板、提示、工具、重试行为和每次运行的预算在所有策略中固定。
- **对比方法**（五种执行策略）：
    - **Random**：依赖有效且固定种子的路由器，随机选择最多两个就绪节点。
    - **Static**：按固定图顺序执行所有依赖有效的节点。
    - **Semantic**：根据任务提示与节点能力描述的词汇重叠，选择最多两个就绪节点。
    - **MoE-style**：使用固定的词汇相关性与拓扑分数混合，选择最多两个就绪节点，无反馈或自适应资源路由。
    - **AGAO**（本文方法）：结合词汇目标相关性、拓扑分数和基于规则的反馈，可跳过低分节点，并在低/中/高三级模型容量之间路由。

## 4. 资源与算力

- **明确说明**：论文中**未提及**任何关于GPU型号、数量、训练时长等具体算力信息。实验描述强调是“descriptive pilot means”（描述性试点均值），每种策略对每个任务运行一次，未使用多次重复以提高统计显著性。因此，本文报告的是单次运行的均值，而非经过统计检验的估计。
- **令牌消耗**：文中给出了令牌计数（输入/输出），但未说明底层LLM的具体型号或API，仅提到按“配置定价”输出令牌是输入令牌的6倍价格。

## 5. 实验数量与充分性

- **实验数量**：共16个任务（MBPP 8个 + HotpotQA 8个），每种策略每个任务运行1次。消融实验（Ablation Study）在相同8+8任务上移除不同机制进行测试。
- **充分性与公平性分析**：
    - **优点**：对比了多种基线（Random/Static/Semantic/MoE-style），消融实验覆盖了三种注意力组件和目标感知+反馈的联合消融，实验条件（图结构、任务清单、提示、预算）严格控制一致。
    - **不足与局限**：
        - 每个任务仅运行1次，缺乏多次重复取平均和统计显著性检验（如标准差或置信区间），结果受随机性影响较大。
        - 任务规模小（每种类型仅8个），且均为相对简单的短任务（代码生成和多跳QA），未在更复杂、更大型的多智能体图场景（如长时推理、工具调用链、动态图增长）上验证。
        - 仅覆盖编程和问答两种工作负载，代表性有限。
        - 未与更多最新多智能体框架（如AutoGen、ChatDev、AFlow等）直接对比，而是使用自己实现的简单基线（如Random/Static）。
    - **总体评价**：实验设计合理但不够充分，属于初步探索性验证，需在更大规模、更多样化任务和统计意义上强化。

## 6. 论文的主要结论与发现

- **主要结论**：AGAO通过动态注意力分配，在提升任务效果的同时大幅减少计算开销。在MBPP上，AGAO的pass@1达到0.875（7/8成功），优于所有基线（最高静态0.625）。在HotpotQA上，AGAO的F1为0.340，超过Random（0.285）、Static（0.319）、Semantic（0.244），但低于MoE-style（0.417），说明AGAO效果因工作负载而异。
- **效率发现**：AGAO激活的节点数最少（MBPP 6.5 vs 静态9.0，减少27.8%；HotpotQA 7.25 vs 9.0，减少19.4%），累计智能体时间减少约50-56%。令牌消耗方面，AGAO输入令牌显著增加（由于路由和上下文），但输出令牌大幅减少，由于输出令牌价格更高，总体更具成本效益。
- **消融实验结论**：完整AGAO在编码和问答两个套件上提供了最一致的平衡；移除任一组件都会在某个套件上造成性能下降或效率权衡（如移除目标感知提升编码但降低问答F1并大幅增加时间）。

## 7. 优点：方法或实验设计上的亮点

- **方法创新性**：首次将“注意力工程”作为多智能体系统编排层的新范式，类比Transformer注意力扩展至执行级协调，概念新颖且有启发性。
- **框架完整性**：三个注意力组件（目标、拓扑、资源）相互补充，覆盖了语义、结构和计算资源三个关键维度，且支持自适应路由（执行中动态调整），设计系统且理论清晰。
- **问题形式化**：将多智能体执行形式化为注意力分配优化问题（目标函数包含任务质量、计算成本和延迟），提供数学基础。
- **实验亮点**：不仅报告性能指标，还详细报告执行时间、令牌消耗和成本分析（输入输出令牌价格差异），使效率对比更全面；消融实验系统地验证了每个组件的贡献。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖有限**：
    - 任务规模小（仅16个任务，每类8个），未在更大规模、更多样化的多智能体图上测试（如包含数十个节点的复杂工作流）。
    - 仅覆盖编码和问答，未涉及其他领域（如规划、网页导航、科学研究等）。
    - 每个任务仅运行1次，无多次重复，存在随机偏差风险，实验结果不可统计推断。
- **基线对比不充分**：
    - 未与主流多智能体框架（如AutoGen、ChatDev、MetaGPT、DyLAN等）进行直接比较，而是使用自建的简单基线（Random/Static等），削弱了说服力。
    - MoE-style基线的实现细节不清晰（固定混合比例？），可能不够公平。
- **方法局限性**：
    - 目标感知注意力目前基于词汇重叠（Lexical overlap）而非更先进的语义嵌入，可能限制了语义匹配能力。
    - 拓扑感知注意力使用的手工特征（依赖距离、中心性等）可能不是最优表示；更新机制依赖规则而非学习。
    - 自适应路由的阈值 \(\tau_e, \tau_s\) 需要手动设定，缺乏自适应调节。
    - 本文未在真实动态图（节点/边可增删）场景中验证，仅针对固定图。
- **资源与可重复性**：未报告LLM具体模型、API或本地计算资源，代码虽开源但复现细节可能不完整。

（完）
