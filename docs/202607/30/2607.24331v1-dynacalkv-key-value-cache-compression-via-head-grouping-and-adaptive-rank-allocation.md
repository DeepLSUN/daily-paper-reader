---
title: "DynaCalKV: Key-Value Cache Compression via Head Grouping and Adaptive Rank Allocation"
title_zh: DynaCalKV：通过头部分组和自适应秩分配进行键值缓存压缩
authors: "Tan T. Nguyen, Quan V. Dang"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24331v1"
tags: ["query:llm"]
score: 9.0
evidence: 针对LLM推理的KV缓存压缩
tldr: 大语言模型推理时，长上下文导致KV缓存内存持续增长成为瓶颈。现有低秩压缩方法未区分Key和Value角色，且固定注意力头分组。本文提出DynaCalKV框架，对Key缓存基于CKA相似度动态分组注意力头、并自适应分配秩预算，对Value缓存沿用ReCalKV。实验表明，该方法在减少Key缓存参数的同时保持精度，尤其在多注意力头模型上效果显著，但需谨慎用于分组查询注意力模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24331v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1580, \"height\": 830, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24331v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 916, \"height\": 631, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24331v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 517, \"height\": 607, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24331v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1308, \"height\": 581, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24331v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1607, \"height\": 414, \"label\": \"Table\"}]"
motivation: 现有KV缓存低秩压缩方法忽视Key和Value的差异，且固定分组未充分利用注意力头结构相似性。
method: 基于CKA相似度动态分组Key注意力头，在参数预算下自适应分配秩预算；Value缓存采用ReCalKV离线校准低秩分解。
result: 在三个指令微调LLM上，Key缓存参数量减少且准确率相当；对MHA模型提升明显，GQA模型需保守应用。
conclusion: 提出的动态分组与自适应秩分配策略有效平衡KV缓存压缩与模型性能，尤其适配MHA结构。
---

## 摘要
随着大语言模型推理阶段需要处理长上下文窗口，键值缓存最初似乎解决了这一挑战，但随着上下文窗口的不断增长，它最终成为一个显著的瓶颈。低秩压缩最近被研究作为一种有效的方法，在保持模型性能的同时减少键值缓存内存。然而，现有方法中只有少数对键缓存和值缓存区别对待，尽管它们扮演着不同的角色。此外，这些方法通常采用固定的注意力头分组，这可能无法充分利用注意力头之间的结构相似性。在本文中，我们提出了一种改进的低秩键值缓存压缩框架。对于键缓存，我们基于中心核对齐（CKA）相似性动态分组注意力头，并在参数预算下自适应分配秩预算。对于值缓存，我们采用与ReCalKV相同的方法，通过离线校准改进低秩分解以提升重建质量。在三个指令微调的大语言模型上的实验结果表明，我们的方法在保持竞争性精度的同时减少了键缓存参数的数量。我们进一步观察到，所提出的策略对多头注意力（MHA）模型特别有效，而对于分组查询注意力（GQA）模型，尤其是在长上下文设置中，应该更谨慎地应用。

## Abstract
As the inference phase of Large Language Models (LLMs) requires handling long context windows, the Key-Value (KV) cache initially appears to address this challenge but eventually becomes a significant bottleneck as the context window continues to grow. Low-rank compression has recently been studied as an effective approach to reduce KV cache memory while maintaining model performance. However, only a few existing methods treat the Key and Value caches differently, despite their distinct roles. Moreover, these methods typically employ fixed attention-head grouping, which may not fully exploit the structural similarity among attention heads. In this paper, we propose an improved low-rank KV cache compression framework. For the Key cache, we dynamically group attention heads based on Centered Kernel Alignment (CKA) similarity and allocate the rank budget adaptively under a parameter budget. For the Value cache, we adopt the same approach as ReCalKV, refining the low-rank decomposition through offline calibration to improve reconstruction quality. Experimental results on three instruction-tuned LLMs show that our method reduces the number of Key cache parameters while maintaining competitive accuracy. We further observe that the proposed strategy is particularly effective for Multi-Head Attention (MHA) models, whereas it should be applied more conservatively to Grouped-Query Attention (GQA) models, especially in long-context settings.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：大语言模型推理时，长上下文导致KV缓存内存线性增长，成为性能瓶颈。现有低秩压缩方法（如Palu、ReCalKV）虽能减少内存，但存在两个关键不足：
  - **忽视Key和Value的不同角色**：Key主要用于注意力索引，而Value包含完整信息，两者需要不同的压缩策略。
  - **固定分组不合理**：ReCalKV按固定大小（如4个头一组）分组，无法充分捕捉注意力头间的相似性异质性。
- **研究动机**：提出一种能动态识别相似头并自适应分配秩预算的方法，在保证模型性能的同时进一步压缩Key缓存参数。
- **整体含义**：通过更精细的、架构感知的压缩策略，平衡内存效率与模型精度，尤其关注标准MHA与GQA架构的差异性。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明）

- **核心思想**：对Key缓存采用动态分组+自适应秩分配（基于CKA相似度），对Value缓存沿用ReCalKV的离线校准低秩分解。
- **关键技术细节**：
  - **Key压缩流程**：
    1. 计算每层中所有注意力头对的CKA相似度矩阵。
    2. 采用凝聚层次聚类（Agglomerative Clustering）动态形成头组，组大小可变（而非固定）。
    3. 初始化每组秩：基于该组奇异值的能量（平方和）比例分配总秩预算，取整。
    4. 贪婪调整：在保证总参数不超过ReCalKV（即约束 \(\sum r_i \alpha_i \leq 4r/h\)）的条件下，迭代减少能量损失最小的组的秩。
    5. 通过目标函数（重建误差 + λ × 未使用秩惩罚，λ=1）选择最优聚类数 \(K^*\)。
  - **Value压缩**：将整个Value投影矩阵进行SVD分解后，利用校准数据交替更新左、右因子（类似ReCalKV），最小化重建误差。
- **算法流程**（文字描述）：
  - 输入：Key投影矩阵 \(W^{(k)}\)、相似度矩阵 \(S\)、秩预算 \(r\)、候选聚类数集 \(K\)。
  - 对每个候选 \(K\)：聚类得到头组 → 按能量分配秩 → 贪婪调整 → 计算重建误差 + 惩罚 → 选出最优聚类数 → 输出对应的低秩因子。

#### 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法

- **模型**：
  - Llama-3.2-1B-Instruct（GQA，8个Key头）
  - Qwen1.5-1.8B-Chat（MHA，16头）
  - SmolLM2-1.7B-Instruct（MHA，32头）
- **数据集**：
  - **校准数据**：WikiText-2。
  - **零样本评测**：6个常识/推理QA基准（OpenBookQA, HellaSwag, PIQA, ARC-easy, ARC-challenge, Winogrande）。
  - **长上下文评测**：LongBench中的8个数据集（TriviaQA, Qasper, TREC, SAMSum, LCC, RepoBench-P, QMSum, MultiNews）。
- **对比方法**：
  - 基线：Palu（低秩压缩基础方法）
  - 直接对比：ReCalKV（固定4头分组、均匀分配秩）
  - 自身变体：DynaCalKV（本文方法）
- **评估指标**：零样本准确率（%）和LongBench任务得分。

#### 4. 资源与算力

- **GPU型号**：所有实验在单个NVIDIA T4 GPU上完成。
- **算力规模**：未明确说明训练/蒸馏时长或GPU总数。由于仅对预训练模型进行离线压缩校准，计算量远小于训练；论文指出所有Key压缩计算仅需一次性离线执行，无额外推理开销。
- **模型规模**：仅测试了1B-1.8B参数的小模型，作者提及受GPU资源限制。

#### 5. 实验数量与充分性

- **实验数量**：
  - 零样本：6个任务 × 3个模型 = 18组
  - 长上下文：8个任务 × 2个模型（SmolLM2因性能近乎零未报告）= 16组
  - 参数缩减统计：3个模型分别报告
  - 消融分析：未设置独立消融实验（如固定聚类数、不同相似度度量），但通过比较ReCalKV间接体现了动态分组的效果。
- **充分性评估**：
  - **客观公平**：均基于Palu代码库复现ReCalKV基线，与DynaCalKV在同一框架下比较，超参数一致。
  - **局限**：
    - 仅测试小模型（≤1.8B），大模型（7B+）效果未知。
    - 无消融实验单独验证CKA vs. 其他相似度度量、能量分配 vs. 均匀分配、贪婪调整的必要性。
    - 长上下文场景下SmolLM2因基线过低被排除，丧失一组对比。
    - 未报告延迟或吞吐量等实际推理性能指标。

#### 6. 论文的主要结论与发现

- **参数节省**：DynaCalKV相比ReCalKV显著减少Key缓存参数：Llama-3.2-1B减少18.23%、Qwen1.5减少16.00%、SmolLM2减少65.23%。减少幅度与头维度、头数相关：维度越小、头越多，聚类越容易，节省越多。
- **零样本性能**：在常识类任务（PIQA、ARC-e）上DynaCalKV平均提升+0.87%和+2.44%；在复杂推理任务上略微下降（0.28%~3.00%），整体与ReCalKV持平或略优（SmolLM2上平均+0.44%）。
- **长上下文性能**：对MHA模型Qwen1.5仅下降1.02%，甚至在某些总结任务上略升；但对GQA模型Llama-3.2大幅下降8.03分（尤其检索和总结任务），表明GQA模型头数少，进一步聚类会损失关键信息。
- **架构依赖性**：本文方法最适用于标准MHA（多头独立）；对GQA模型应保守应用，尤其长上下文场景。

#### 7. 优点：方法或实验设计上的亮点

- **动态分组**：利用CKA相似度自动发现头间冗余，优于固定大小分组，更灵活且免去手动设置组大小。
- **自适应秩分配**：基于奇异值能量排序，并引入贪心调整确保参数不超预算，兼顾性能与约束。
- **价值压缩优化**：采用校准数据微调分解因子，提升重建保真度。
- **架构意识**：通过实验明确指出了方法对不同注意力架构的适用性差异，具有实践指导意义。
- **公平对比**：完全基于Palu代码库，与ReCalKV公平比较，避免实现偏差。

#### 8. 不足与局限

- **实验覆盖**：仅测试1B~1.8B小模型，无法直接推广到7B+模型；长上下文评估缺失SmolLM2数据，对比不完整。
- **消融缺失**：未对CKA vs. 其他相似度、聚类算法、能量分配 vs. 均匀分配、贪婪调整、λ值等进行消融，无法确认各组件贡献。
- **偏差风险**：论文未排除因随机种子导致的性能波动，未报告多次运行均值和标准差。
- **应用限制**：对GQA模型的长上下文任务效果不佳，限制了其在现代高效架构（如Llama系列）中的通用性；未评估对生成质量（如BLEU/Rouge）或下游对话等任务的影响。
- **其他资源维度**：仅提到参数缩减，未估算实际内存节省或推理速度提升，也未与量化/剪枝方法正交组合实验。

（完）
