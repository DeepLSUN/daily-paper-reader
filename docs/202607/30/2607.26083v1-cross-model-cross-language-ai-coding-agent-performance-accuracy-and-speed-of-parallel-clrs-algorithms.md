---
title: "Cross-Model Cross-Language AI Coding Agent Performance: Accuracy and Speed of Parallel CLRS Algorithms"
title_zh: 跨模型跨语言AI编码智能体性能：并行CLRS算法的准确性与速度
authors: "Shiqi Cheng, Evelyne Ringoot, Rabab Alomairy, Alan Edelman"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.26083v1"
tags: ["query:agent"]
score: 9.0
evidence: AI编码Agent在并行算法生成上的评估
tldr: AI编码智能体在串行编程中表现优异，但其并行编程能力尚不明确。本文横跨排序、图遍历、搜索算法及C++、Python、Julia语言，评估Cursor Composer 2.0、GPT 5.4、Claude Sonnet 4.6的并行代码生成准确性与加速效果。结果表明，智能体可通过少量提示生成正确并行代码，但加速效果高度依赖算法和语言，其中Sonnet 4.6总性能最佳，GPT 5.4虽正确却无加速，C++在图算法一致性最高，Python和Julia在搜索算法加速最大。研究强调，应纳入运行时性能效率作为LLM关键评估指标，尤其针对并行实现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.26083v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 844, \"height\": 470, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26083v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 899, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26083v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 924, \"height\": 634, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.26083v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 916, \"height\": 917, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.26083v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 875, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26083v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 854, \"height\": 385, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26083v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 654, \"height\": 421, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26083v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 925, \"height\": 451, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26083v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 913, \"height\": 194, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.26083v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 801, \"height\": 388, \"label\": \"Table\"}]"
motivation: 现有研究聚焦AI编码智能体串行性能，对其并行代码生成能力缺乏跨模型、跨语言系统评估。
method: 使用三个智能体对排序、图遍历、搜索算法在C++、Python、Julia上从串行基线生成并行实现，衡量正确性与加速比。
result: Sonnet 4.6性能最强，GPT 5.4无加速；C++在图算法表现一致，Python和Julia在搜索算法加速最大，加速效果因算法和语言而异。
conclusion: 并行代码生成中，运行时性能效率应与准确性并列作为LLM核心评估指标，尤其对于并行实现。
---

## 摘要
AI编码智能体已迅速普及于软件工程。其串行性能，包括准确性和速度，已被广泛研究。然而，最近的初步结果表明，它们的并行编程能力落后于串行编程能力。本文对三种编码智能体——Cursor's Composer 2.0、GPT 5.4和Claude Sonnet 4.6——在三种算法类别（排序、图遍历和搜索）的并行代码生成上进行了跨语言评估，涉及C++、Python和Julia语言。对于每个算法和语言对，我们提示编码智能体从串行基线生成并行实现，跟踪达到功能正确性和性能改进所需的提示工作量，并测量相对于自定义串行基线和第三方库实现的加速比。我们发现，编码智能体可以通过适度的提示工作生成正确的并行实现，但实现有意义的加速比在很大程度上依赖于算法和语言。Sonnet 4.6提供了最强的整体性能提升，而GPT 5.4尽管始终保持正确性，但并未产生可测量的加速比。C++在图算法上最一致地可并行化，而Python和Julia在搜索算法上实现了最大的加速比：没有一种语言在所有类别中占主导地位。Python和Julia在某些图算法上实现了加速，但在其他算法上则退步。这些发现强调了将运行时性能效率作为LLM主要性能指标（除了准确性之外）的重要性，特别是对于并行实现。

## Abstract
AI coding agents have quickly become omnipresent in software engineering. Their serial performance, both in terms of accuracy and speed, has been extensively covered. However, recent initial results suggest their parallel programming capabilities lag behind serial programming capabilities. This paper presents a cross-language evaluation of three coding agents -- Cursor's Composer 2.0, GPT 5.4, and Claude Sonnet 4.6 -- on parallel code generation across three algorithm categories -- sorting, graph traversal, and search -- in C++, Python, and Julia. For each algorithm and language pair, we prompt a coding agent to produce a parallel implementation from a serial baseline, track the prompting effort required to achieve both functional correctness and performance improvements, and measure speedup against both custom serial baselines and third-party library implementations. We find that coding agents can produce correct parallel implementations with modest prompting effort, but that achieving meaningful speedup is heavily algorithm- and language-dependent. Sonnet 4.6 delivers the strongest overall performance gains, whereas GPT 5.4 produces no measurable speedups despite consistent correctness. C++ is most consistently parallelizable for graph algorithms, while Python and Julia achieve the largest speedups on search algorithms: no single language dominates across all categories. Python and Julia each achieve speedup on some graph algorithms but regress on others. These findings underscore the impact of including runtime performance efficiency as a main LLM performance metric, in addition to accuracy, particularly for parallel implementations.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：AI编码智能体（如Cursor Composer 2.0、GPT 5.4、Claude Sonnet 4.6）在软件工程中日益普及，已有大量研究评估其在串行代码生成方面的准确性和速度。
- **核心问题**：尽管串行编程能力已被广泛验证，但AI编码智能体的**并行编程能力**（特别是并行代码生成的**准确性**与**运行时性能效率**）尚未得到系统评估。初步研究表明，其并行编程能力明显落后于串行编程能力。
- **整体含义**：在并行编程场景中，**运行时性能效率**应与**功能正确性**并列为LLM核心评估指标，仅关注正确性无法反映并行实现的实际价值。

## 2. 方法论

### 核心思想
- 以**CLRS基准**中的经典算法（排序、图遍历、搜索）为测试对象，评估三个AI编码智能体在三种编程语言（C++、Python、Julia）中生成并行实现的能力。
- 采用**迭代提示方法**：从串行基线出发，要求智能体生成并行版本，通过多轮交互达到正确性和性能优化，记录每轮交互结果。

### 关键技术细节
- **算法选择**：选自CLRS基准的12种算法，分为三类：
  - 排序：冒泡排序、插入排序、归并排序、快速排序、选择排序
  - 图遍历：BFS、DFS、Dijkstra、Bellman-Ford
  - 搜索：二分搜索、最小值搜索、Quickselect
- **并行化策略**：
  - Python：使用 `ProcessPoolExecutor`（受GIL限制）
  - Julia：使用 `Polyester.jl` 的 `@batch` 并行化（非递归调用）
  - C++：使用 `std::thread` 和 OpenMP
- **迭代流程**：
  1. 初始提示：提供串行实现，要求生成并行版本，并指定并行库。
  2. 正确性迭代：若失败，用错误输出反馈给模型，必要时给出人工假设。
  3. 性能优化迭代：在正确性达标后，通过“优化运行时”提示进行性能调优，持续到性能不再提升或达到上限（最多两次无提升则终止）。

### 公式/算法流程说明（文字描述）
- 无具体公式，核心流程为：**串行基线 → 初始并行生成 → 正确性检查 → 错误反馈迭代 → 性能优化迭代 → 最终评估**。

## 3. 实验设计

### 数据集/场景
- **输入数据**：
  - 排序和搜索：均匀随机整数数组（无重复值），规模从 \(10^3\) 到 \(10^5\)（排序）或 \(10^6\)（搜索）。
  - 图算法：随机图，节点数 \(n=10^5\)，边数 \(O(n \log n)\)，边权重均匀采样。
- **测试环境**：单节点32线程CPU（AMD EPYC 9474F，48核/2 socket，详见Table II）。

### Benchmark
- **CLRS基准**：来自《算法导论》的30种经典算法执行轨迹，本文选取其中的排序、图遍历、搜索类算法。
- **对比基线**：
  - 自定义串行实现（与并行实现使用相同输入）
  - 第三方库（Julia的Graphs.jl用于图算法）
- **对比方法**：
  - 三种AI编码智能体：Cursor Composer 2.0、GPT 5.4、Claude Sonnet 4.6
  - 三种编程语言：C++、Python、Julia
  - 不同并行库：Python的ProcessPoolExecutor、Julia的Polyester.jl（vs Base.Threads）、C++的std::thread/OpenMP

### 超参数与度量
- **正确性测试**：验证算法不变（如BFS与DFS的节点访问顺序区分）。
- **性能度量**：三次运行平均时间（排除编译和数据集构建时间），计算加速比 = 串行时间 / 并行时间。

## 4. 资源与算力

- **计算资源**：在**单节点32线程CPU**上运行（AMD EPYC 9474F 48核处理器，2个socket，每个核心2线程）。
- **未使用GPU**：本文全部在CPU上进行多线程并行，未涉及GPU或大规模HPC集群。
- **未明确提及**：训练或部署AI模型的算力（如GPU型号、数量、训练时间）。文中仅使用现有AI编码智能体的商用服务，未涉及模型训练。

## 5. 实验数量与充分性

### 实验数量
- **算法×语言×智能体**：12种算法 × 3种语言 × 3种智能体 = 108个生成任务（每个任务包括多轮迭代）。
- **具体测试**：
  - 排序：5种算法，规模 \(10^3, 10^4, 10^5\)
  - 图遍历：4种算法，节点 \(10^5\)
  - 搜索：3种算法，规模 \(10^6\)
- **重复次数**：每次测试取3次运行的平均值，排除编译和预热时间。

### 充分性与公平性
- **优点**：
  - 跨模型、跨语言、跨算法类别，覆盖范围广。
  - 使用标准化的CLRS基准，输入数据统一。
  - 迭代提示过程明确，记录了用户级提示次数（非API调用），可复现。
- **不足**：
  - 仅测试了**单节点多线程**并行，未涉及MPI、OpenACC、GPU等更复杂的并行模型。
  - 每次测试的输入规模固定（排序10^4、图10^5、搜索10^6），不覆盖大规模或极端情况。
  - 迭代次数较少（最多5次），可能未充分探索性能优化空间。
  - 仅测试了三个智能体，且版本号（GPT 5.4、Sonnet 4.6）可能是虚构的或未来版本，实际可用性待验证。

## 6. 主要结论与发现

- **正确性容易实现**：GPT 5.4和Sonnet 4.6在首次提示即实现全部语言/算法的正确性；Composer 2.0需要更多迭代（尤其Julia，因类型错误和Polyester.jl使用问题）。
- **性能提升差异巨大**：
  - **Sonnet 4.6**：最强，在7/15个语言-算法组合中实现加速（N=10^4）。
  - **GPT 5.4**：虽正确但无任何加速（所有算法/语言）。
  - **Cursor Composer 2.0**：部分加速（如Julia归并排序、C++冒泡和归并排序）。
- **语言依赖性**：
  - **C++**：在图算法上最一致（全部4种算法实现加速）。
  - **Python**：在搜索算法上加速最大（Quickselect 1.70×），图算法几乎无加速（GIL限制）。
  - **Julia**：在排序（归并、冒泡、选择）和部分图算法（Bellman-Ford）上有加速，但在搜索（最小值、Quickselect）和Dijkstra上退步，归因于Polyester.jl使用问题。
- **算法特异性**：加速效果更依赖算法而非语言。例如，归并排序在所有语言中均易并行化，而二分搜索因O(log n)复杂度难以获益。
- **Polyester.jl优于Base.Threads**：在Julia搜索算法中，Polyester.jl比Base.Threads快1.08×到10.33×。

## 7. 优点

- **跨模型、跨语言的系统评估**：填补了现有研究仅限单语言或单模型的空白，提供了语言和模型选择的实用洞察。
- **将运行时性能作为核心指标**：明确指出正确性不等于有用性，性能效率应作为LLM评估的关键维度，尤其针对并行编程。
- **迭代提示方法**：模拟真实开发流程（反馈-修正循环），比单次提示更能反映智能体在复杂任务中的真实能力。
- **具体库对比**：在Julia中对比了Polyester.jl和Base.Threads，提供了对实际开发有意义的指导。
- **简洁明确的实验设计**：控制变量（输入数据统一、排除编译时间等），结果可解释性强。

## 8. 不足与局限

- **实验覆盖有限**：
  - 仅覆盖**多线程（共享内存）**并行，未测试**分布式并行（MPI）**或**GPU并行（CUDA/OpenACC）**，而这些正是HPC中最关键的并行模型。
  - 输入规模较小（排序10^4、图10^5），无法评估可扩展性或大规模问题下的性能。
  - 仅三类算法（排序、图、搜索），未包括科学计算（矩阵运算、FDTD、CFD等）或AI常用内核。
- **模型版本问题**：GPT 5.4和Claude Sonnet 4.6可能是尚未发布或虚构的版本，实际可用性和性能未知。
- **迭代次数人为限制**：对无性能提升的情况只允许两次优化提示，可能过早收敛，错过更优解。
- **提示工程偏差**：初始提示针对不同语言指定了不同的并行库（如Julia用Polyester），本身可能引导模型走向特定实现，影响泛化结论。
- **未考虑模型内部成本**：未报告API调用次数/成本或推理时间，仅统计用户级提示次数，可能掩盖模型内部的计算开销。
- **硬件配置固定**：仅在一个32线程CPU上测试，未验证在不同核数或架构（如ARM、Intel vs. AMD）上的泛化性。
- **应用限制**：结论限于所选模型、语言、算法和并行库组合，不能直接推广到其他AI编码智能体（如DeepSeek、Llama）或更复杂的并行场景。

（完）
