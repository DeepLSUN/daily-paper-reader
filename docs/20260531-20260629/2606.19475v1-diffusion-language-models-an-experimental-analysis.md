---
title: "Diffusion Language Models: An Experimental Analysis"
title_zh: 扩散语言模型：一项实验分析
authors: "Thomas Bertolani, Davide Bucciarelli, Leonardo Zini, Marcella Cornia, Lorenzo Baraldi"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19475v1"
tags: ["query:llm"]
score: 9.0
evidence: 扩散语言模型的系统实验分析
tldr: 扩散语言模型(DLMs)通过迭代去噪生成文本，为自回归范式提供替代方案。然而，不同模型在评估协议、数据集和推理预算上的差异使得比较困难。本文系统评估了8种先进DLMs在8个基准上的性能与效率，分析了去噪步数等推理因素的影响。结果表明，生成时的设计选择显著影响性能与计算效率的权衡，为实际部署提供了实用指导。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19475v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1622, \"height\": 270, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19475v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1620, \"height\": 270, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19475v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1620, \"height\": 270, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19475v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1616, \"height\": 272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19475v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1601, \"height\": 537, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19475v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1652, \"height\": 387, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19475v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1601, \"height\": 475, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19475v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1603, \"height\": 352, \"label\": \"Table\"}]"
motivation: 现有DLMs评估缺乏统一标准，难以比较其能力与权衡。
method: 系统评估8种DLMs在8个基准上的生成质量与效率，并分析推理关键因素。
result: 发现DLM行为受去噪步数、上下文长度等设计选择影响，存在性能与效率的权衡。
conclusion: 本研究为扩散语言模型的对比分析和实际部署提供了系统性参考。
---

## 摘要
大型语言模型（LLMs）通过自回归生成彻底改变了语言建模，在广泛的任务中展现出强大的性能。近年来，扩散语言模型（DLMs）作为一种替代范式出现，通过迭代去噪而非逐词预测来生成文本，允许对整个序列进行并行细化。尽管已提出众多基于扩散的架构，但评估协议、数据集、推理预算和生成超参数的差异使得比较它们的能力和理解它们提供的权衡变得困难。在这项工作中，我们对现代DLMs进行了系统的实验分析。具体而言，我们在跨越推理、编码、翻译、知识和结构化问题解决的八个基准上评估了八种最先进的DLMs，同时明确考虑了生成质量和计算效率。除了下游评估，我们还分析了关键推理时间因素的影响，包括去噪步数、上下文长度、块大小和并行去掩蔽策略，并通过在相同条件下训练的较小模型的受控比较来补充大规模实验。我们的分析强调了基于扩散的语言建模在不同任务、架构和推理预算下的优缺点。我们表明，DLMs的行为受生成时间设计选择的强烈影响，导致性能和计算效率之间的不同权衡。总体而言，我们的研究为当代DLMs的能力和部署特性提供了实用见解。

## Abstract
Large Language Models (LLMs) have revolutionized language modeling through autoregressive generation, enabling strong performance across a wide range of tasks. Recently, Diffusion Language Models (DLMs) have emerged as an alternative paradigm that generates text through iterative denoising rather than next-token prediction, allowing parallel refinement of entire sequences. While numerous diffusion-based architectures have been proposed, differences in evaluation protocols, datasets, inference budgets, and generation hyperparameters make it difficult to compare their capabilities and understand the trade-offs they offer. In this work, we present a systematic experimental analysis of modern DLMs. Specifically, we evaluate eight state-of-the-art DLMs across eight benchmarks spanning reasoning, coding, translation, knowledge, and structured problem solving, while explicitly considering both generation quality and computational efficiency. Beyond downstream evaluation, we analyze the impact of key inference-time factors, including denoising steps, context length, block size, and parallel unmasking strategies, and complement large-scale experiments with controlled comparisons of smaller models trained under identical conditions. Our analysis highlights the strengths and limitations of diffusion-based language modeling across different tasks, architectures, and inference budgets. We show that the behavior of DLMs is strongly influenced by generation-time design choices, leading to distinct trade-offs between performance and computational efficiency. Overall, our study provides practical insights into the capabilities and deployment characteristics of contemporary DLMs.

---

## 论文详细总结（自动生成）

# 扩散语言模型：一项实验分析 — 详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：大型语言模型（LLMs）主要基于自回归生成（如GPT、LLaMA），逐词预测，性能强大但存在固有局限：推理时严格的左到右依赖限制了并行化和全局内容精炼。扩散语言模型（DLMs）作为替代范式，通过迭代去噪从噪声序列逐步恢复文本，允许双向上下文建模、并行词元精炼，并自然支持填充、编辑等任务。
- **核心问题**：尽管已有大量扩散架构提出，但各论文在评估协议、数据集、推理预算（如去噪步数）、生成超参数上高度碎片化，导致**无法直接比较不同DLMs的能力和权衡**。特别是扩散模型显式引入质量-计算效率的折衷，其行为强烈依赖于推理时的设计选择（去噪步数、块大小、并行去掩蔽比例等），这些因素的影响此前未被系统刻画。
- **研究目标**：提供统一的、系统的实验分析，在一致协议下比较多种先进DLMs，量化推理参数的影响，并揭示性能与效率的折衷，为实际部署提供实用指导。

## 2. 提出的方法论：核心思想、关键技术细节

- **核心思想**：不属于单一创新方法，而是**系统性实验分析框架**，包含两大层次：
  - **大规模下游评估**：在8个基准上评估8种代表性DLMs（包括纯离散扩散和块扩散架构），与强自回归基线（Qwen3）对比，并分析推理参数的影响。
  - **小规模受控实验**：在统一数据集（OpenWebText）上从头训练小模型（~200M参数），在相同数据条件下进行困惑度分析和计算成本对比，隔离架构差异。
- **关键技术细节**：
  - **评估协议标准化**：采用`lm-evaluation-harness`框架，对每个基准使用固定shot数和评价指标（如MMLU用5-shot对数似然得分，GSM8K用4-shot flexible-extract，HumanEval用0-shot pass@1等）。对数似然估计遵循各自论文的方法（Monte Carlo或单次前向）。
  - **推理参数分析**：针对全序列扩散模型（Dream、LLaDa）系统改变：
    - 联合缩放步数与上下文长度（1:1比例）
    - 固定上下文长度（N=1024）下缩放全局并行去掩蔽比例（步数）
    - 块扩散模型中缩放绝对块大小（8-128）和块内去掩蔽比例（1/8到1/1）
  - **计算成本度量**：测量单次前向和完整生成（GSM8K上的100次生成）的峰值GPU显存和浮点运算数（TFLOPS）。
- **对比架构**：包括纯离散扩散（Dream、LLaDa、LLaDa 1.5、MDLM、Duo）、块/混合扩散（Fast-dLLM、SDLM、BD3-LM、E2D2）以及自回归基线（Qwen3-4B/8B、GPT-2）。

## 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **Benchmark套件**（8个任务）：
  - **知识与常识推理**：MMLU（5-shot，对数似然）、MMLU Redux（5-shot，首词生成）、HellaSwag（0-shot，归一化准确率）
  - **数学推理**：GSM8K（4-shot，flexible-extract）
  - **代码生成**：HumanEval（0-shot，pass@1）、MBPP（3-shot，pass@1）
  - **机器翻译**：WMT16 En–De（0-shot，chrF）
  - **结构化逻辑推理**：Sudoku（0-shot，约束满足验证）
- **评估模型**：
  - **大规模模型**：Dream（7B，从Qwen2.5初始化）、LLaDa（8B，从零训练）、LLaDa 1.5（8B，用RL优化）、Fast-dLLM v2（7B，块扩散）、SDLM（3B，混合自回归+扩散）——对比自回归基线Qwen3-4B和Qwen3-8B。
  - **小规模模型**：MDLM（200M）、BD3-LM（200M）、Duo（200M）、E2D2（170M）——在OpenWebText上训练，对比GPT-2（小规模）。
- **实验分组**：
  - 表2：大规模模型在8个基准上的峰值准确率对比。
  - 图1-4：推理参数缩放实验（大规模模型）。
  - 表3：计算成本对比（单次前向 vs 完整生成）。
  - 表4：小规模模型困惑度和计算成本对比。

## 4. 资源与算力

- **论文中明确信息**：
  - 大规模模型训练数据规模：Dream（580B tokens）、LLaDa（2.3T tokens）、LLaDa 1.5（350K指令对）、Fast-dLLM（30M tokens）、SDLM（2.3B tokens）。**未说明训练所用的GPU型号、数量及时长**。
  - 小规模模型统一使用OpenWebText（9B tokens）训练，**也未说明具体硬件和时长**。
  - 推理阶段的计算成本在表3中量化：单次前向时Dream需15.80 GB / 23.23 TFLOPS（Qwen3-8B需16.68 GB / 9.47 TFLOPS），完整生成GSM8K（100次）时纯扩散模型成本飙升（Dream达23726.95 TFLOPS），块扩散Fast-dLLM仅33.38 TFLOPS。**说明推理所需GPU资源，但训练资源未披露**。
- **结论**：论文侧重推理效率分析，训练资源细节缺失，无法完全复现训练成本。

## 5. 实验数量与充分性

- **实验数量**：
  - 大规模对比实验：1个主表格（表2）+ 4个参数缩放图（图1-4）+ 1个计算成本表（表3），涉及5种大规模DLMs和2个自回归基线，共8个基准。
  - 小规模对比实验：1个表格（表4）涵盖困惑度和计算成本，5种模型。
  - 消融/参数分析：专门对Dream和LLaDa做了步数-长度联合缩放、步数单独缩放；对LLaDa、LLaDa 1.5、Fast-dLLM做了块大小和块内比例缩放。共至少 **10组实验**。
- **充分性与客观性**：
  - 统一协议（lm-evaluation-harness）保证了公平性；使用对数似然（而非生成）评价MMLU和HellaSwag避免解码偏差；对生成任务使用标准后处理。
  - 覆盖了知识、推理、编码、翻译、规划（数独）等多维度任务，架构多样性好（纯扩散 vs 块扩散，从零训练 vs 初始化）。
  - 但存在局限：块扩散模型中的SDLM由于参数更少（3B）可能不公平；所有模型并未在同一数据规模下预训练（数据规模差异巨大）；部分模型（如LLaDa 1.5）仅通过微调或RL优化，并非从零扩散训练。
  - **总体实验设计合理，数据充分，但可比性受预训练差异限制。**

## 6. 主要结论与发现

1. **架构范式差异**：纯离散扩散（Dream）在全局约束任务（Sudoku 75%）上显著优于自回归和块扩散；块扩散（Fast-dLLM）在数学（GSM8K 83.39%）和代码生成（HumanEval 69.51%）上表现最强，但牺牲了语言理解（HellaSwag仅30.82%）。
2. **推理参数敏感度**：
   - 步数与长度联合缩放：推理和代码任务先升后饱和或下降；翻译性能**单调恶化**，表明长输出对翻译有害。
   - 全局并行去掩蔽（步数固定）：低步数下性能极差；步数增加后性能持续提升，无明显饱和——证明性能瓶颈源于长度而非步数。
   - 块大小缩放：对大部分任务鲁棒，只有MBPP在块大小16时出现波动，可灵活调整以满足硬件约束。
   - 块内并行度：代码生成需要精细顺序细化（几乎线性依赖并行比）；数学推理容忍度稍高；翻译则依赖具体模型。
3. **质量-效率折衷**：纯扩散模型计算成本比自回归高几个数量级（如Dream生成GSM8K需23726 TFLOPS，Qwen3-8B仅19.47 TFLOPS），但并行潜力仅通过足够多的去噪步数才能释放；块扩散显著降低开销（Fast-dLLM仅33.38 TFLOPS），成为实用的折衷。
4. **小规模模型困惑度**：自回归GPT-2最佳（20.98）；扩散模型中Duo最好（24.36），优于MDLM（28.45）；混合架构BD3-LM和E2D2最差（>36），说明为效率牺牲了似然建模能力。

## 7. 优点

- **统一评估框架**：首次在一致协议下比较多种先进DLMs，包括纯扩散、块扩散、混合架构，解决了碎片化问题。
- **系统推理参数分析**：详细量化了去噪步数、上下文长度、块大小、并行度的影响，揭示了不同任务对参数的敏感度差异，具有很强实用价值。
- **双重实验层次**：大规模下游评估结合小规模受控实验，允许隔离架构差异并分析原生似然能力，增强结论可靠性。
- **计算成本透明化**：明确报告峰值显存和浮点运算数，为实际部署提供量化参考。
- **覆盖任务多样化**：从知识到结构化推理，从生成到判别，评估全面。

## 8. 不足与局限

- **预训练数据与规模不一致**：大规模模型在训练数据量、来源、初始化方式上差异巨大（如Dream从自回归初始化，LLaDa从零训练），导致无法完全归因架构效果。
- **训练资源未披露**：未提供任何训练GPU型号、数量、时间，限制了复现性和成本评估。
- **部分模型参数不匹配**：SDLM仅3B，而Dream/LLaDa/Fast-dLLM均为7-8B，比较公平性存疑。
- **超参数选择可能存在偏倚**：虽然统一了评估框架，但各模型原本论文中的最佳推理配置可能不同，本文使用了各自的最佳配置（表2），但未进行网格搜索确保对所有模型公平。
- **小规模模型泛化性有限**：小模型仅用于困惑度分析，未在下游任务上评估，无法验证架构差异在小规模下的下游表现。
- **缺少对连续扩散模型的比较**：论文只评估了离散扩散和块扩散，未覆盖连续扩散（如Diffusion-LM、DiffuSeq）。
- **应用限制**：结论基于特定基准，真实世界场景（长文本、对话、多轮推理）未测试；数独任务过于狭窄，不能完全代表规划能力。

（完）
