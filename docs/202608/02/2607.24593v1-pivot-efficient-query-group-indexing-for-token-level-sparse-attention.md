---
title: "PIVOT: Efficient Query-Group Indexing for Token-Level Sparse Attention"
title_zh: PIVOT：面向词元级稀疏注意力高效查询组索引
authors: "Hong Liu, Yuan Cheng, Lin Niu, Yi Su, Yufei Xue, Anmin Liu, Guanghua Yu, Jianchen Zhu"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.24593v1"
tags: ["query:llm"]
score: 9.0
evidence: 优化令牌级稀疏注意力索引器，加速大模型推理。
tldr: Token级稀疏注意力中，索引器需为每个查询扫描全部前序token，开销达O(L²)每层。PIVOT利用相邻查询top-k重合度高、分数长尾分布的特点，将查询分组聚合为代理查询，共享一次全前缀扫描，再从候选集中为各查询选top-k。两种变体PIVOT-Reuse与PIVOT-Refine可在速度与精度间权衡。在DeepSeek-V3.2和GLM-5.1上，PIVOT匹配密集索引器精度，索引加速4x，端到端延迟降低1.6x。该方法无需训练，可直接替换DSA索引器，显著提升长上下文推理效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.24593v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 776, \"height\": 461, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1855, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1795, \"height\": 734, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1851, \"height\": 458, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1831, \"height\": 1031, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.24593v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1846, \"height\": 610, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.24593v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 792, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 886, \"height\": 328, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 887, \"height\": 399, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 886, \"height\": 270, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 886, \"height\": 255, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1117, \"height\": 566, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1116, \"height\": 454, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1117, \"height\": 687, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.24593v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1117, \"height\": 631, \"label\": \"Table\"}]"
motivation: 现有token级稀疏注意力索引器需对每个查询独立评分全部前序token，造成O(L²)冗余计算，成为长序列推理瓶颈。
method: 利用相邻查询top-k重叠和分数长尾分布，将查询分组为代理查询共享一次前缀扫描，再通过Reuse或Refine变体选取各查询top-k。
result: 在LongBench和RULER上，PIVOT匹配DSA索引器精度，索引加速达4倍，端到端延迟降低1.6倍。
conclusion: PIVOT作为免训练的即插即用索引器，有效消除冗余扫描，大幅提升长上下文稀疏注意力效率。
---

## 摘要
词元级稀疏注意力（如生产系统中的DeepSeek稀疏注意力DSA）使得下游注意力计算高效，但将瓶颈转移到了为其提供输入的索引器上。为了为每个查询选择top-k词元，索引器仍需对每个前置词元进行评分，对于长度为L的序列，每层产生O(L^2)的成本。我们观察到这种逐查询扫描在很大程度上是冗余的：临近查询选择的top-k词元高度重叠，且索引器的评分沿键轴呈长尾分布。我们在PIVOT（Proxy Indexing Via One full-prefix Traversal，基于一次全前缀遍历的代理索引）中利用了这些特性，这是一种无需训练、可直接替代DSA索引器的方法，它在临近查询组之间共享一次前缀扫描。PIVOT将一组查询聚合成单个代理查询，执行一次共享的全前缀扫描以获得候选集，然后从该集中为每个查询选择top-k。两种变体在速度与保真度之间取舍：PIVOT-Reuse在组内共享代理top-k以获得最大速度，而PIVOT-Refine使用每个查询的索引器对候选集重新评分，然后选择各自的top-k，以较小额外成本匹配密集索引器。一个统一算法覆盖两个推理阶段，仅分组方式不同：预填充阶段采用固定大小的连续查询组，解码阶段则将在一次多词元预测（MTP）步骤中共同解码的查询归为一组。在DeepSeek-V3.2和GLM-5.1上，于LongBench和RULER基准测试中，PIVOT在匹配密集DSA索引器准确度的同时，将其加速最高4倍，并在长上下文场景下将端到端延迟降低最高1.6倍。

## Abstract
Token-level sparse attention, as implemented by DeepSeek Sparse Attention (DSA) in production systems, makes the downstream attention efficient but shifts the bottleneck to the indexer that feeds it. To select the top-k tokens for each query, the indexer must still score every preceding token, incurring a cost of O(L^2) per layer for a sequence of length L. We observe that this per-query scan is largely redundant: nearby queries select highly overlapping top-k tokens, and the indexer scores are long-tailed along the key axis. We exploit these properties in PIVOT, Proxy Indexing Via One full-prefix Traversal, a training-free, drop-in replacement for the DSA indexer that shares one prefix scan across a group of nearby queries. PIVOT aggregates a group into a single proxy query, performs one shared full-prefix scan to obtain a candidate set, and then selects a top-k for each query from that set. Two variants trade speed for fidelity: PIVOT-Reuse shares the proxy top-k across the group for maximum speed, whereas PIVOT-Refine re-scores the candidate set with the indexer of each query and then selects an individual top-k, matching the dense indexer at a small additional cost. A single algorithm covers both inference phases, differing only in how groups are formed: fixed-size groups of consecutive queries in prefill, and the queries decoded together in one multi-token prediction (MTP) step in decode. On DeepSeek-V3.2 and GLM-5.1 across LongBench and RULER, PIVOT matches the accuracy of the dense DSA indexer while accelerating it by up to 4x and reducing end-to-end latency by up to 1.6x at long context.

---

## 论文详细总结（自动生成）

## PIVOT论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：长上下文推理已成为LLM服务的关键挑战。随着上下文窗口向百万token扩展，全注意力的二次方复杂度（O(L²)）在延迟和内存上均构成严重瓶颈。
- **已有方案与其缺陷**：DeepSeek稀疏注意力（DSA）通过让每个查询仅关注最相关的k个token（k ≪ L），将主要注意力成本降为O(Lk)，在DeepSeek-V3.2和GLM-5.1等生产级模型中得到部署。但DSA只是将瓶颈从下游注意力转移到了上游索引器——索引器仍需为每个查询对全部前缀token逐一评分，每层开销仍为O(L²)。在200K上下文下，索引器占据预填充端到端延迟的约81%、解码阶段的41%。
- **已有加速索引器的不足**：现有工作沿token轴（HISA）、head轴（MISA）、layer轴（IndexCache）进行优化，但均未触及query轴——索引器仍为每个查询独立执行、每次调用仍扫描完整前缀。
- **核心洞察**：本文通过观察发现三个关键性质：① 相邻查询选择的top-k token高度重叠；② 一组查询的top-k并集远小于最坏情况；③ 索引器分数沿key轴呈长尾分布。这些冗余和稀疏性表明，共享一次扫描即可服务多个查询，为加速开辟了新的“查询轴”。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

**核心思想**：PIVOT（Proxy Indexing Via One full-prefix Traversal）是一种免训练、可直接替换DSA索引器的模块，通过将一组相邻查询聚合为一个代理查询，共享一次全前缀扫描，再从候选集中为每个查询选取top-k，从而将索引成本从O(gL)降至O(L+gc)或O(L)。

**关键技术细节**：

- **分组策略**：
  - **预填充阶段**：将序列查询位置划分为固定大小g的连续组，`G_P[t] = {q_t, q_{t+1}, ..., q_{t+g-1}}`，成本从O(L²)降至O(L²/g)。
  - **解码阶段**：利用多token预测（MTP）机制，将当前token与其d个草稿token作为一个组，`G_D[t] = {q_t, q̃_{t+1}, ..., q̃_{t+d}}`，g = d+1 = 4，无需额外成本。
- **共享代理扫描**：对组内查询的索引查询向量和门控权重按head取平均，得到代理查询：
  
  `q̄_Ij = (1/g) Σ_{t'∈G} q_{t',j}`，`w̄_Ij = (1/g) Σ_{t'∈G} w_{t',j}`
  
  代理查询执行一次全前缀扫描，对所有前缀key打分：
  
  `Ī_s = Σ_j w̄_Ij ReLU(q̄_Ij · k_s)`
  
- **两种变体**：
  - **PIVOT-Reuse**：直接取代理分数的top-k分配给组内所有查询，无逐查询评分，成本O(L)，速度最快但丢失查询特异性。
  - **PIVOT-Refine**：先取代理分数的top-c（c=2k=4096）形成共享候选集C，再用每个查询的精确索引器对C重新评分：
    
    `I_{t,s} = Σ_j w_{t,j} ReLU(q_{t,j} · k_s), s ∈ C`
    
    然后选各查询自己的top-k，成本O(L+gc)，在长上下文中几乎完全匹配密集索引器精度。
- **附加机制**：候选集C与每个查询的局部窗口（最近W个token）取并集，确保每个查询始终保留局部上下文。

### 3. 实验设计：数据集、基准与对比方法

- **模型**：DeepSeek-V3.2和GLM-5.1两个采用DSA索引器的生产级大模型，均使用MTP深度d=3（解码组大小g=4）。
- **基准**：
  - **RULER**：控制上下文长度（4K至128K）的检索与推理测试。
  - **LongBench**：多领域长文档理解任务，涵盖单文档QA、多文档QA、摘要、少样本学习、合成任务、代码补全等类别。
- **对比方法**：
  - 密集DSA索引器（基线）。
  - HISA（token轴加速）。
  - MISA（head轴加速）。
  - IndexCache（layer轴加速）。
  - PIVOT-Reuse和PIVOT-Refine，以及PIVOT与IndexCache的组合（+IC）。
- **实现细节**：使用vLLM，运行于NVIDIA H20 GPU。默认超参数：k=2048，c=4096，g=4，代理聚合方式为均值池化（局部窗口w=4）。

### 4. 资源与算力

- 论文提到使用NVIDIA H20 GPU和vLLM推理框架，但**未明确披露**GPU数量、训练时长、总计算量等具体算力信息。
- 值得指出的是，PIVOT是免训练方法，不存在训练成本，仅在推理阶段引入额外的前向计算，因此算力开销主要体现在推理实验的规模上（两个大规模模型 × 两个基准 × 多组实验对比和消融）。

### 5. 实验数量与充分性

**实验数量**：

- 主实验：2个模型 × 2个基准（RULER 6种长度 + LongBench 6类任务），对比4种以上方法。
- 四组消融实验：代理聚合方式（均值/首个/末个）、组大小g（4/6/8/16）、候选预算c（3072/4096/6144/8192）、部署阶段（预填充/解码/两者），两个模型都做了完整消融（附录中有完整双模型表格）。

**充分性评价**：

- **充分**：观测验证章节先在DeepSeek-V3.2上对128K序列进行了详尽的冗余性分析（图2、图5），加深了对动机的支撑。主实验覆盖多个上下文长度，消融全面，并检查了该方法在预填充和解码两个阶段的独立与联合效果。
- **客观公平**：与基线在相同设置下运行，统一的vLLM服务框架；对比方法选择了各加速轴的代表工作。
- 但实验是在**单一代际的GPU（H20）**上进行的，且未披露具体运行次数/随机种子等细节，可能影响结果可复现性。

### 6. 论文的主要结论与发现

- **准确性**：在LongBench上所有方法都在密集DSA基准约0.5个百分点的窄幅带内；在RULER上，上下文达到64K–128K时才出现差异。PIVOT-Refine在所有长度上追踪密集DSA（128K时仅差约0.1–0.3个百分点），PIVOT-Reuse在极端长度下下降约4-6个百分点。
- **效率**：索引器kernel加速最高约4.8×（预填充，256K）和4.1×（解码），端到端加速最高约1.6×；加速收益随上下文长度增长而增大，因为被分摊的全前缀扫描正是随前缀增长而占主导的项。
- **变体权衡**：PIVOT-Refine在长上下文中完全匹配密集索引器精度同时仍带来显著加速，是推荐默认变体；PIVOT-Reuse用少量精度换取更高的速度。
- **正交性**：与IndexCache组合（+IC）在中等长度下保持无损，在最长上下文中仅损失小部分精度，验证了查询轴优化与其他轴优化正交可组合。
- **消融结论**：均值池化优于端点查询；g=4是最佳平衡点；c=2k已足够（分数长尾使候选集盈余价值不大）；在预填充、解码或两者中启用PIVOT都几乎无损。

### 7. 优点

- **提出新的优化轴向**：首次从query轴优化DSA索引器，正交于既有token/head/layer三轴的加速方法，开辟了第四维研究方向。
- **强动机支撑**：三个观察（O1相邻查询高度重叠、O2组并集小、O3分数长尾）均有直观图证和数据支撑，方法论直接由观察推导，逻辑清晰自洽。
- **免训练、即时部署**：无需任何训练即可以drop-in方式替换DSA索引器，保留下游Sparse MLA和KV cache不变，接口完全兼容。
- **统一算法覆盖两阶段**：同一算法通过不同分组策略同时适用于预填充和解码，巧妙复用MTP机制为解码阶段提供免费分组。
- **两种变体提供灵活权衡**：Reuse与Refine在速度和精度之间形成明确的操作范围，适应不同部署需求。
- **与正交方法可组合**：与IndexCache组合验证了正交性，为进一步堆叠HISA、MISA等提供了可能。

### 8. 不足与局限

- **PIVOT-Reuse在超长上下文下精度下降**：在128K下RULER分数与密集基线差距达4-6个百分点，限制了高速变体在极端长上下文中的应用。
- **PIVOT-Refine在短上下文的额外开销**：在4K等短序列下Refine慢于密集DSA（论文中标注了该点，声称部署时有guardrail回退到密集扫描，但没有在主要试验中展示这一守护机制的结果）。
- **实验硬件覆盖有限**：仅在NVIDIA H20 GPU单一声明，未在更多硬件平台（如A100/H100）验证可移植性。
- **缺少算力信息披露**：未说明GPU数量、推理实验的总计算量/时间，不利于复现和成本评估。
- **未与所有正交方法充分组合**：论文只展示了与IndexCache的组合，未演示PIVOT与HISA或MISA联合使用的效果。
- **适用模型范围有限**：实验仅在DeepSeek和GLM两个模型上验证，二者均采用MTP和DSA架构，扩展到其他稀疏注意力范式或非MTP模型的有效性仍需验证。
- **局部窗口需调参**：引入了局部窗口宽度W的额外超参数，在附录中说明了对性能的影响，但缺少对其敏感度的系统消融。

（完）
