---
title: "SPADE: Speculative Decoding for Precise and Low Cost Distributed Edge Cloud Inference"
title_zh: SPADE：面向精确低成本分布式边缘云推理的推测解码
authors: "Divya Jyoti Bajpai, Kishan Kumar Upadhyay, Manjesh Kumar Hanawal"
date: 2026-08-13
pdf: "https://arxiv.org/pdf/2608.13076v1"
tags: ["query:llm"]
score: 9.0
evidence: 面向边缘-云分布式大模型推理的投机解码
tldr: "大型语言模型部署面临高计算成本，边缘部署小模型精度不足，云端大模型费用高昂。SPADE 提出边缘-云协作的投机解码框架，边缘草稿模型快速生成候选，云端大模型并行验证，仅对拒绝的 token 进行修正，将大量计算下沉到边缘。实验显示该框架减少76%的云端调用且零精度损失，为低成本、高精度、可扩展的 LLM 部署提供了实用路径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.13076v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1538, \"height\": 579, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.13076v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 660, \"height\": 486, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.13076v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1322, \"height\": 610, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.13076v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1321, \"height\": 462, \"label\": \"Table\"}]"
motivation: 解决云端大模型推理成本高与边缘小模型精度不足的矛盾，需要在不牺牲精度前提下降低推理开销。
method: SPADE 在边缘部署草稿模型快速生成候选 token，云端大模型并行验证，仅对拒绝 token 修正，将主要计算下沉至边缘。
result: "在 SpecBench 和 CNN/Dailymail 等任务上，SPADE 减少76%的云端模型调用，且零精度损失。"
conclusion: 该框架无需重训练即可保持大模型精度，为高效、低成本、可扩展的 LLM 边缘-云协同部署提供了新路径。
---

## 摘要
大语言模型（LLMs）在自然语言理解和生成方面取得了显著成功，但其部署受到高计算需求的制约。直接在边缘部署较小的LLM可以规避这一问题，但会降低准确性。部署基于云的小型大模型可以保持性能，但代价是昂贵的每令牌计算。我们提出了一种分布式推理框架\our{}，在边缘和云之间集成了推测解码（SD）。部署在边缘的紧凑草稿模型快速生成候选令牌，云上的大型验证模型并行验证这些令牌。接受的令牌被保留，只有拒绝才触发验证器校正，从而大幅减少云查询次数。我们的即插即用设计将大部分计算转移到边缘，显著降低推理时间和云成本，并在无需任何重新训练的情况下保持大模型的准确性。我们的方法展示了在现实环境中可扩展、成本高效且准确的LLM部署的实用途径。使用SpecBench和CNN/Dailymail数据集进行的多个自然语言处理任务的实验结果表明，与完整模型相比，\our{}将云模型调用减少了$76\%$，且准确性零损失。

## Abstract
Large Language Models (LLMs) have achieved remarkable success in natural language understanding and generation, but their deployment is constrained by high computational demands. Deploying smaller LLMs directly on the edge can circumvent this, but with degraded accuracy. Deploying smaller cloud-based big LLMs preserves performance, but at the cost of expensive per-token computation. We present a distributed inference framework, \our{}, that integrates speculative decoding (SD) across edge and cloud. A compact draft model deployed on the edge generates candidate tokens rapidly, and a large verifier model on the cloud validates these tokens in parallel. Accepted tokens are retained, while only rejections trigger verifier correction, substantially reducing the number of cloud queries. Our plug-and-play design shifts the bulk of computation to the edge, significantly lowers inference time and cloud cost, and preserves the accuracy of the big model without any retraining requirement. Our approach demonstrates a practical path toward scalable, cost-efficient, and accurate deployment of LLMs in real-world environments. Experimental results across multiple Natural Language Processing tasks using SpecBench and CNN/Dailymail datasets demonstrate that \our{} reduces the cloud model calls by $76\%$ with zero loss in accuracy as compared to the full model.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **背景**：大语言模型（LLM）在自然语言理解与生成上取得了卓越成就，但其部署面临严峻的资源瓶颈——大模型需要大量计算资源和内存，难以在移动设备或边缘端运行。
- **核心矛盾**：
  - 直接在边缘部署小模型：计算开销低，但**精度显著下降**；
  - 完全在云端部署大模型：精度高，但**每个 token 都需要昂贵的计算**，推理时间与成本随序列长度线性增长。
- **核心研究问题**：如何在不牺牲大模型精度的前提下，尽可能减少对云端资源的依赖？即：能否利用边缘设备的部分计算能力，构建一种高效、低成本、高精度的分布式推理方案？
- **论文含义**：SPADE（Speculative Decoding for Precise and Low-Cost Distributed Edge-Cloud Inference）提出将推测解码（SD）技术应用于边缘-云分布式推理场景，以边缘小模型做草稿生成、云端大模型做并行验证，从而在**保持大模型精度**的同时**大幅降低云端调用成本**。

## 2. 论文提出的方法论

### 核心思想
- 采用**双模型推测解码（Speculative Decoding）**架构：边缘部署轻量级草稿模型 \( M_q \)，云端部署大规模验证模型 \( M_p \)。
- 边缘模型快速、自回归地生成一组候选 token 块；云端大模型在**单次前向传播**中并行验证整个 token 块；仅对拒收的 token 进行修正。

### 关键技术细节
- **验证准则**：给定候选 token \( x \sim q(x) \)（草稿模型分布），云端模型以概率：

  \[
  \alpha(x) = \min\left(1, \frac{p(x)}{q(x)}\right)
  \]

  接受该 token，其中 \( p(x) \) 是云端目标模型的分布。

- **拒绝修正**：若 token 被拒绝，则从修正分布中重新采样：

  \[
  p'(x) = \text{norm}\left(\max(0, p(x) - q(x))\right)
  \]

  用于替换被拒绝的 token，保证输出分布与纯云端大模型**统计等价**。

- **算法流程**（伪代码）：
  1. 输入用户 prompt；
  2. 边缘模型 \( M_q \) 自回归生成 \( d \) 个候选 token；
  3. 将候选 token 块发送至云端；
  4. 云端 \( M_p \) 在单次前向传播中并行计算 \( d+1 \) 个位置的分布；
  5. 并行执行接受/拒绝判定；
  6. 接受最长的连续一致前缀；第一个被拒 token 从 \( p' \) 中采样替换；
  7. 更新上下文，边缘模型从新上下文继续生成下一块，直至输出 `<eos>`。

- **关键参数**：草稿 token 块长度 \( d \) 是控制计算与通信平衡的超参数——\( d \) 过小增加验证频率与同步开销，过大则提高拒收概率与无效计算，需经验性地在验证集上调整。

### 核心优势
- **即插即用**：无需任何重训练或微调；
- **精度保证**：据引用文献 [12]，生成分布与云端大模型完全一致；
- **主要计算下沉到边缘**：云端只做并行验证，单次验证可确认多个 token。

## 3. 实验设计

### 数据集
1. **Spec-Bench**：专为评估推测解码方法设计的基准，包含 6 个子任务：
   - 多轮对话（Multi-turn Conversation）
   - 翻译（Translation）
   - 摘要（Summarization）
   - 问答（Question Answering）
   - 数学推理（Mathematical Reasoning）
   - 检索增强生成（Retrieval-Augmented Generation）
2. **CNN/DailyMail**：标准新闻文本摘要数据集。

### 对比基准
- **Target Model（云端大模型）**：LLaMA-3.1-8B，表示输出质量上界；
- **Draft Model（纯边缘小模型）**：LLaMA-3.2-1B，表示低延迟但精度有限；
- **SPADE（本方法）**：两者结合。

### 评估指标
- **Spec-Bench**：使用 Gemini-2.5-Flash-Lite 作为自动裁判，按 1-5 分制从正确性、指令遵循、完整性、清晰连贯性、简洁性、事实性六个维度评分。
- **CNN/DailyMail**：BLEU-1、BLEU-4、ROUGE-1 F1、ROUGE-L F1、CIDEr-D。
- **效率指标**：平均云端调用次数、平均吞吐量、云端运行时间比。

## 4. 资源与算力

- **边缘端**：NVIDIA RTX 3080 GPU（12 GB 显存），运行 LLaMA-3.2-1B 草稿模型。
- **云端**：NVIDIA RTX A6000 GPU（48 GB 显存），运行 LLaMA-3.1-8B 验证模型。
- **未说明的内容**：论文未提及 GPU 数量、训练时长（因为该方法**不需要训练/微调**）、具体的推理延迟测量值（如网络传输时间）、单次推理的能耗或经济成本，也未提供显存占用、模型并行策略等详细信息。

## 5. 实验数量与充分性

### 实验数量
- **Spec-Bench**：6 个子任务的性能评估 + 3 项效率指标；
- **CNN/DailyMail**：5 项质量指标 + 3 项效率指标；
- **超参数分析**：一个关于 \( d \)（草稿 token 数量）对云端调用数影响的实验（图 2）。

### 充分性评估
- **优点**：覆盖了多种 NLP 任务类型（对话、翻译、摘要、推理、RAG、QA），benchmark 具有一定代表性；效率指标设计合理。
- **不足**：
  - 没有**消融实验**（如不同草稿模型大小、不同验证模型大小、不同 \( d \) 值的完整对比）；
  - 没有与已有的分布式推理方法（如 Neurosurgeon、SplitEE、早期退出等）进行**横向对比**；
  - 未报告 token 接受率的具体数值；
  - 未测量实际网络传输延迟对端到端推理时间的影响；
  - 实验规模相对有限，只有一个草稿-验证模型组合。

## 6. 论文的主要结论与发现

- **云调用数大幅降低**：在 Spec-Bench 上云端模型调用从平均 `133.25` 次降至 `30.16` 次（减少约 **77.4%**）；在 CNN/DailyMail 上从 `127.30` 降至 `30.79` 次（减少约 **76%**）。
- **云端运行时间显著降低**：Spec-Bench 上为纯云端部署的 `0.23×`，CNN/DailyMail 上为 `0.24×`。
- **精度几乎无损**：
  - Spec-Bench 整体得分：SPADE 为 `4.38`，目标模型为 `4.45`（差距 `0.07`，并非论文所称的"零损失"）；
  - CNN/DailyMail 各指标与目标模型非常接近，其中 **CIDEr-D 反而高于目标模型**（`3.19` vs `2.50`）。
- **吞吐量提升**：Spec-Bench 上从 `2.43` tokens/s 提升至 `3.25` tokens/s；CNN/DailyMail 从 `1.21` 提升至 `1.95` tokens/s。
- **\( d \) 值影响**：增大 \( d \) 可减少云端调用次数，但依赖草稿与目标模型的对齐程度。
- **总体判断**：SPADE 实现了"以少量云端验证换取大部分边缘生成"的架构目标，在几乎不损失精度的前提下大幅降低云端成本。

## 7. 优点

- **方法设计简洁且优雅**：直接复用成熟的推测解码理论，避免了复杂的任务特定启发式或训练流程。
- **即插即用，部署友好**：无需重训练、无需修改模型结构，适用于现有的任何草稿-验证模型对。
- **理论保证**：基于推测解码的分布等价性证明（引用 [12]），生成结果在统计上与云端大模型一致。
- **实际意义强**：解决了边缘-云分布式推理中"精度 vs 成本"的核心矛盾，为资源受限场景下的 LLM 部署提供了可行路径。
- **评测较为全面**：覆盖多任务、多数据集、多重质量与效率指标；使用 LLM-as-a-judge 评估方式较符合当前主流实践。

## 8. 不足与局限

- **实验覆盖有限**：仅用了 LLaMA-3.2-1B / LLaMA-3.1-8B 一对模型组合，未验证框架在不同模型家族、不同规模组合上的泛化性。
- **缺乏真实网络条件测试**：边缘-云协同的实际性能高度依赖网络往返延迟，但论文未报告任何关于传输时间、带宽影响的数据。
- **无横向对比**：未与 Neurosurgeon、SplitEE 等现有的分布式推理方案进行直接比较，难以判断相对优势。
- **"零精度损失"表述不够严谨**：虽然理论上有分布等价性保证，但实际表中 SPADE 的 Spec-Bench 得分（4.38）仍略低于目标模型（4.45），摘要中"零损失"的说法过于绝对。
- **缺少消融分析**：没有分析草稿模型质量对接受率、效率的具体影响，也没有对不同 \( d \) 值做更细粒度的性能-成本权衡分析。
- **仅关注生成阶段**：未考虑 prompt 处理（prefill）阶段的计算成本，对长上下文场景的适用性未知。
- **安全与隐私问题未讨论**：将 prompt 和生成内容发送到云端验证存在数据隐私风险，论文未提及任何隐私保护措施。

（完）
