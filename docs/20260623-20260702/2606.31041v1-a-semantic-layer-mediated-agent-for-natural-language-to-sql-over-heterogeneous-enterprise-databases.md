---
title: A Semantic-Layer-Mediated Agent for Natural Language to SQL over Heterogeneous Enterprise Databases
title_zh: 语义层中介的智能体：面向异构企业数据库的自然语言到SQL转换
authors: "Ha Jeong Kim, Saksonita Khoeurn, Ye Ji Yoon"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31041v1"
tags: ["query:agent"]
score: 9.0
evidence: 语义层中介的NL2SQL智能体
tldr: "面向企业数据库的自然语言转SQL（NL2SQL）面临模式复杂、方言繁多等挑战。本文提出一种基于语义层中介的智能体，通过紧凑的语义模型查询（SMQ）解耦语义意图与物理执行，利用确定性编译器将SMQ转换为SQLite、BigQuery等方言SQL。在Spider2-snow基准的547项任务上，使用Gemini 3 Pro达到94.15%的执行准确率，排名第三，显著超越纯模式方法。该系统验证了语义层对提升NL2SQL接地性的有效性，但需权衡过拟合风险。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31041v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 846, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31041v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 931, \"height\": 211, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31041v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 673, \"height\": 161, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31041v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 856, \"height\": 380, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31041v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 765, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31041v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 659, \"height\": 230, \"label\": \"Table\"}]"
motivation: 企业数据库包含数百张物理表、晦涩列名和异构SQL方言，传统直接生成SQL的方法难以应对复杂分析任务。
method: 智能体通过语义层推理生成SMQ（语义模型查询），再由确定性编译器将其转换为具体数据库方言的SQL。
result: "在Spider2-snow基准的547个任务上，执行准确率达到94.15%，位列官方排行榜第三。"
conclusion: 语义层中介有效提升了NL2SQL的鲁棒性与可解释性，但高质量语义层构建与过拟合风险需进一步权衡。
---

## 摘要
在真实世界企业数据库上的自然语言到SQL（NL2SQL）仍然比学术基准更具挑战性。企业模式通常包含数百个物理表，这些表具有晦涩的列名、异构的SQL方言以及需要嵌套聚合、时间推理和多表连接的复杂分析工作负载。我们提出了一种语义层中介的NL2SQL智能体，它将语义意图与物理SQL执行解耦。该智能体不是直接在原始模式上生成SQL，而是通过一种称为语义模型查询（SMQ）的紧凑中间表示，在精选的语义层上进行推理。一个确定性编译器将每个SMQ转换为方言特定的SQL，提供经过验证的构建块，智能体将这些构建块组合成最终查询。该系统采用受限的思考-行动循环，支持SQLite、BigQuery和Snowflake后端，并集成到一个端到端评估框架中。使用Gemini 3 Pro，该系统在547任务的Spider2-snow基准上实现了94.15%的执行准确率，在官方排行榜上排名第三，并显著优于仅基于模式的方法。我们描述了系统架构、SMQ表示、智能体工作流程、评估结果，并讨论了语义层质量以及改进的根基与过拟合之间的权衡。

## Abstract
Natural language-to-SQL (NL2SQL) over real-world enterprise databases remains significantly more challenging than on academic benchmarks. Enterprise schemas often contain hundreds of physical tables with cryptic column names, heterogeneous SQL dialects, and complex analytical workloads requiring nested aggregations, temporal reasoning, and multi-table joins. We present a semantic-layer-mediated NL2SQL agent that decouples semantic intent from physical SQL execution. Rather than generating SQL directly over raw schemas, the agent reasons over a curated semantic layer through a compact intermediate representation called the Semantic Model Query (SMQ). A deterministic compiler translates each SMQ into dialect-specific SQL, providing verified building blocks that the agent composes into the final query. The system employs a constrained think-act loop, supports SQLite, BigQuery, and Snowflake backends, and is integrated into an end-to-end evaluation framework. Using Gemini 3 Pro, the system achieves 94.15% execution accuracy on the 547-task Spider2-snow benchmark, ranking third on the official leaderboard and substantially outperforming schema-only approaches. We describe the system architecture, SMQ representation, agent workflow, evaluation results, and discuss semantic-layer quality and the trade-off between improved grounding and overfitting.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：企业级数据库上的自然语言转SQL（NL2SQL）远比学术基准困难。企业模式包含数百张物理表、晦涩的列名、异构SQL方言（如BigQuery、Snowflake、SQLite），且分析问题常需嵌套聚合、时间窗口、多表连接等复杂操作。现有方法直接向LLM提供原始模式文本，导致查询脆弱、准确率低（在Spider2上甚至低于30%）。
- **研究动机**：将“模式理解（grounding）”与“SQL组合（composition）”混在一起是脆弱的根源。论文提出通过语义层解耦这两个子问题，让LLM专注于意图推理，物理映射由确定性编译器完成。
- **整体含义**：提供了一种实用、可部署的企业NL2SQL架构，展示了语义层中介可以大幅提升准确率，同时指出了语义层维护与过拟合之间的内在张力。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在LLM与数据库之间插入一个**语义层**。语义层是精选的、面向业务的数据描述，将物理表包装为“语义模型”，暴露维度（dimensions）、度量（measures）和预定义指标（metrics），每个元素都有人可读的描述和物理列表达式。LLM不直接看到原始schema，而是通过**语义模型查询（SMQ）**进行推理。
- **关键技术细节**：
  - **SMQ**：紧凑的JSON中间表示，包含三个列表：`metrics`（查询目标）、`filters`（WHERE条件）、`group_by`（分组维度）。元素通过`ModelName__elementName`格式引用。
  - **SMQ-to-SQL引擎**：确定性组件，将每个SMQ编译为方言正确的SQL，返回SQL文本和预览结果。该组件是物理名称的唯一可信来源。
  - **智能体工作流程（Algorithm 1）**：
    1. 通过`getModelDataElements`发现相关语义模型的元素。
    2. 循环：编写SMQ → 编译得到SQL和预览 → 提取物理表/列名（作为已验证构建块）。
    3. 当构建块足够时，组合最终SQL（可包含窗口函数、递归CTE等SMQ不支持的结构）。
    4. 调用`execute`执行最终SQL（唯一终止动作）。
  - **约束**：禁止直接引用语义模型名作为物理表名；禁止`INFORMATION_SCHEMA`；每次只能调用一个工具；SMQ编译仅用于探索，最终SQL由agent组合。
  - **后端路由**：根据实例ID前缀路由到SQLite、BigQuery、Snowflake执行器。
- **关键技术细节补充**：
  - 语义层包含：每个语义模型的YAML定义（抽象名称、描述、物理表达式）、跨模型的连接图（join graph）、日期/时间配置等。
  - 智能体采用ReAct风格的think-act循环，但严格限制单工具调用以减少错误传播。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：Spider2-snow基准，包含547个企业级NL2SQL任务，在Snowflake上执行，覆盖SQLite、BigQuery、GA4、及原生Snowflake数据源。
- **基准**：Spider2官方排行榜，执行准确率作为主要指标（结果集与黄金结果精确匹配）。
- **对比方法**：
  - DAIL-SQL + GPT-4o（2.20%）
  - Spider-Agent + o1-preview（23.58%）
  - Spider-Agent + Claude-4-Sonnet（25.78%）
  - ReFoRCE + o1-preview（31.26%）
  - 本方法（QUVI-3 + Gemini 3 Pro）：**94.15%**
- **附加分析**：按数据库来源分类（sf_local、sf_ga、sf_bq、sf_native）给出了每类的准确率，并讨论了失败模式。

## 4. 资源与算力
- 论文**未明确给出计算的GPU型号、数量、训练时长**等细节。
- 智能体使用**Gemini 3 Pro（preview）**作为底层LLM，通过API调用运行，温度0.1，扩展思考（extended thinking）设置为high，输出预算16,384 token，每次实例最高20次SMQ迭代。
- SMQ→SQL编译器为确定性程序，无需训练。
- 整个系统是推理（inference）阶段，不涉及模型训练。所需算力取决于LLM API调用成本。

## 5. 实验数量与充分性
- **实验数量**：仅在一个基准（Spider2-snow）上进行了评估，包含547个实例。按4个数据库来源划分子集。
- **充分性**：不够充分。缺少以下典型实验：
  - 消融实验（如移除语义层、使用不同LLM、不同思考配置、不同工具策略等）。
  - 跨其他数据集（如Spider、BIRD、Spider2-lite）的验证。
  - 对不同语义层质量的手动控制实验。
- **客观性**：对比方法均为官方排行榜公开结果，但本方法使用了经过手动精心构建的语义层，而对比方法没有使用此类辅助知识，比较时作者也承认这反映了“系统”价值而非LLM本身。
- **公平性**：论文指出语义层编码了领域知识，零样本基准没有这些信息，因此对比粒度不对等。但作为端到端系统评估，结果具有参考价值。

## 6. 论文的主要结论与发现
- **主要结论**：通过语义层中介解耦grounding与composition，能够显著提升企业NL2SQL的准确率（从不到30%提升至94.15%），在Spider2-snow上排名第三。
- **发现**：
  - 准确性高度依赖于语义层质量（元素覆盖度、描述清晰度、连接图完整性）。
  - 扩展思考（extended thinking）是解决长复杂查询的关键，关闭后准确率大幅下降。
  - 主要的失败模式来自：SMQ编译缺失连接、agent组合错误、语义层元素不完整。
  - 语义层存在过拟合风险（Goodhart效应），描述应保持可复用性而非针对特定问题。

## 7. 优点：方法或实验设计上的亮点
- **方法原创性强**：首次系统地将商业智能语义层（类似dbt MetricFlow）与LLM智能体结合，通过SMQ中间表示实现解耦。
- **架构清晰**：四层架构（编排、QUVI服务、SMQ编译器、多后端执行器）分离关注点，可扩展。
- **确定性保证**：SMQ编译提供经过验证的物理名称，极大减少了LLM幻觉导致的无效查询。
- **智能体设计稳健**：单工具、单终止动作的约束显著降低了错误传播；SMQ仅用于探索，最终SQL由agent组合，能处理窗口函数等高级结构。
- **评估完整**：在真实企业基准上测试，按数据库来源分类，给出了失败模式分析，并讨论了实践中的过拟合权衡。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制
- **实验覆盖不足**：仅在一个基准（Spider2-snow）上评估，未在Spider2-lite、BIRD、Spider等基准上验证泛化能力；缺少消融实验。
- **偏差风险**：语义层由作者团队对于Spider2任务手工构建，很可能无意中编码了针对该基准的提示（Goodhart风险）。尽管作者声明应避免，但客观上无法排除。
- **应用限制**：
  - SMQ只覆盖了常用分析操作（聚合、筛选、分组、声明连接的JOIN），网络结构、子查询、复杂窗口等需要agent自由生成，此时无确定性保证。
  - 每个数据库需要手工维护语义层，初始成本高，扩展性受限于人力。
  - 对LLM推理能力依赖大（需要Gemini 3 Pro级别的强模型），较弱的模型可能效果差。
  - 小类（如原生Snowflake仅18实例）准确率低且方差大，结果不稳定。
- **效率局限**：每个实例最多20次SMQ迭代，可能耗时较长；未报告平均延迟或token消耗。
- **开放问题**：如何量化语义层质量并防止过拟合，论文未提供解决方案，仅定义为未来工作。

（完）
