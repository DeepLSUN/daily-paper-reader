---
title: LemonHarness Technical Report
title_zh: LemonHarness技术报告
authors: "Kailong Ren, Fubo Sun, Jiachen Liu, Liu Yang, Zimo Yin, Jiaying Li, Congli Yin, Ming He, Yu Huo, Jiawei Liu, Zeping Chen, Yubin Huangfu, Ronghua Li, Yixuan Wu, Xing Su, Yanzhi Xu, Likang Wu, Hongke Zhao, Lei Zhang, Xiaohui Geng, Jianping Fan"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24311v1"
tags: ["query:agent"]
score: 9.0
evidence: 长周期LLM智能体的执行框架
tldr: "长周期LLM agent因缺乏明确执行边界，状态变更操作分散导致难以追踪。LemonHarness通过定义显式工作空间约束状态变更，引入可复用规则知识库，并加入时间感知执行机制使模型动态平衡探索与验证。在Terminal-Bench 2.0上，LemonHarness配合不同骨干模型取得84.49%至86.52%平均准确率。结果表明统一运行时边界、可调用规则知识和时间感知有助于提升长周期agent执行稳定性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24311v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1228, \"height\": 781, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24311v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1420, \"height\": 483, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1008, \"height\": 355, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1013, \"height\": 354, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24311v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1009, \"height\": 277, \"label\": \"Table\"}]"
motivation: LLM agent在长任务中频繁修改工作空间状态，但缺乏边界导致状态变更分散难以追踪。
method: LemonHarness通过显式工作空间约束状态变更，集成结构化工具接口，引入可复用规则知识库和时间感知执行机制。
result: "在Terminal-Bench 2.0上，LemonHarness配合GPT-5.3-CodeX达84.49%准确率，配合GPT-5.5达86.52%。"
conclusion: 统一运行时边界、可调用规则知识和时间感知执行能提升长周期agent执行稳定性。
---

## 摘要
随着大语言模型（LLM）代理应用于更长的任务，它们在多轮迭代中越来越多地修改工作空间状态。然而，代理通常只观察工具输出和日志片段，而实际状态变化发生在文件系统中。没有明确的工作空间边界，诸如文件写入和临时工件生成等状态变更操作可能会将变化分散到各个路径。随着时间的推移，这些弱约束的变化逐渐累积，使得修改后的文件等状态难以追踪。本文提出了LemonHarness，一个用于长周期代理的集成执行框架。LemonHarness通过将状态变更操作约束在明确定义的工作空间内，并将模型调用、工具执行和规则知识纳入单一受控边界内，从而建立了一个明确的执行边界。包括文件写入、依赖安装和临时工件创建在内的状态变更操作通过结构化工具接口执行，执行反馈作为观察结果记录，供后续模型决策使用。该系统还引入了一个可重用的规则知识库，将重复出现的执行规则和验收标准转化为运行时知识。LemonHarness进一步增加了一个时间感知的执行机制，将已用时间和剩余预算暴露给模型，使其能够在时间压力变化时重新平衡探索、实现和验证工作，避免因长时间等待或过度验证而导致超时。在Terminal-Bench 2.0上，LemonHarness_GPT-5.3-CodeX在445次试验中达到了84.49%的准确率；将同一框架与更强的GPT-5.5骨干网络配对，五个作业的平均准确率提高到86.52%。结果表明，统一的运行时边界、可调用的规则知识和时间感知的执行可以提高长周期代理执行的稳定性。

## Abstract
As large language model (LLM) agents are applied to longer tasks, they increasingly modify workspace state across multiple rounds of iteration. However, agents typically observe only tool outputs and log fragments, while the actual state changes occur in the file system. Without explicit workspace boundaries, state-changing operations such as file writes and temporary artifact generation may scatter changes across paths. Over time, these weakly constrained changes accumulate, making states such as modified files difficult to track. This paper presents LemonHarness, an integrated execution framework for long-horizon agents. LemonHarness establishes an explicit execution boundary by constraining state-changing operations within a clearly defined workspace and bringing model invocation, tool execution, and rule knowledge within a single controlled boundary. State-changing operations, including file writes, dependency installation, and temporary artifact creation, are executed through structured tool interfaces, with execution feedback recorded as observations available to subsequent model decisions. The system also introduces a reusable rule knowledge base, which turns recurring execution rules and acceptance criteria into runtime knowledge. LemonHarness further adds a time-aware execution mechanism that exposes elapsed and remaining budget to the model, so it can rebalance exploration, implementation, and validation effort as time pressure shifts and avoid timeouts from long waits or excessive verification. On Terminal-Bench 2.0, LemonHarness_GPT-5.3-CodeX reached 84.49% accuracy over 445 trials; pairing the same framework with the stronger GPT-5.5 backbone raised the average accuracy to 86.52% across five jobs. The results suggest that a unified runtime boundary, callable rule knowledge, and time-aware execution can improve the stability of long-horizon agent execution.

---

## 论文详细总结（自动生成）

# LemonHarness技术报告详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：长周期LLM agent（如需要多轮工具调用、文件修改、依赖安装的任务）在执行过程中，状态变更操作（文件写入、临时文件创建、环境修改等）分散在文件系统和不同路径中，缺乏明确的边界约束。这导致状态漂移（state drift）：模型对当前工作空间的信念与实际环境状态脱节，难以追踪哪些文件被修改、哪些命令改变了环境、哪些验证结果仍然有效。
- **背景与动机**：SABER研究表明，长周期agent的失败集中在少数变异动作（如文件创建），这类步骤仅占14~18%，但一次错误的变异动作可使任务成功率降低57~82%，而非变异动作仅降低7~15%。现有Agent框架（如ReAct、Toolformer、PAL、Voyager）将提示、执行日志、运行时控制作为松散耦合组件，未统一管理。长周期任务还受严格执行预算（timeout）限制，agent可能花费过多时间在开放探索或冗余验证上，导致超时失败。因此，需要一个能够约束状态变更、提供可重用知识并感知时间的集成执行框架。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将模型调用、工具执行、任务知识、工作空间状态和执行日志纳入同一个运行时边界（Unified Runtime Boundary），确保观察、修改、验证一致；引入通用规则知识库作为运行时先验；加入时间感知机制，让模型根据剩余预算动态调整探索、实现和验证的精力分配。
- **关键技术细节**：
  - **集成执行框架**：LemonHarness将系统提示、工具执行器、技能库、执行日志和模型控制循环集成管理。状态变更操作（文件写入、依赖安装、临时工件创建）均通过结构化工具接口在指定的工作空间内执行，反馈作为观察记录供后续决策使用。这避免了模型从分散日志推断环境状态。
  - **可复用规则知识**：将跨任务共享的执行规则、验收标准、常见失败模式组织为运行时知识。涵盖ML工作流、生物设计、视觉媒体、系统恢复、游戏逻辑等领域的规则（表1）。例如：ML任务要求分离训练/验证/提交工件、保持可重复性；系统恢复任务要求工作在受控路径内等。这些知识在任务开始前注入，使代理早期获得有效方向，减少盲目试错。
  - **时间感知执行**：将总时间预算T划分为四个阶段（表2）：探索（0~30%，宽限期5%）、实现（30~60%）、验证（60~90%）、保留（90~100%）。每个阶段有累积预算比例，阶段转换有5%的宽限期避免打断接近完成步骤。模型在每轮都接收当前阶段标签和剩余时间分数，从而从开放探索逐步转向结果保留和针对性验证。硬保留区（最后10%）禁止启动新的状态变更操作，只保留已产生的可接受结果。

## 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：Terminal-Bench 2.0（以及后续2.1版本），覆盖真实的终端任务场景，要求agent遵循自然语言指令，操作环境、修改文件、达到评估器接受的最终状态。共445个试验（89个trial×5个job）。Terminal-Bench 2.1引入了任务修复、持续验证和ATIF轨迹要求，补充验证267个trial。
- **基准测试**：未与其他方法进行直接对比（论文未报告对比基线），仅展现了LemonHarness本身配合不同骨干模型的性能。
- **对比方法**：无外部对比。论文只报告了框架在不同骨干模型下的表现（GPT-5.3-CodeX与GPT-5.5），以及在不同版本Terminal-Bench上的结果。

## 4. 资源与算力

- **明确说明**：论文未提及训练或推理所使用的GPU型号、数量、训练时长等算力信息。仅提到了主模型为GPT-5.3-CodeX（API形式），辅助模型为gemini-3.1-pro-preview（用于图像和视频问答）。因此，无法总结具体计算资源消耗。

## 5. 实验数量与充分性

- **实验数量**：
  - Terminal-Bench 2.0：5个job，每个89 trials，共445 trials。系统解决了376个，失败67个，2个无奖励。
  - Terminal-Bench 2.0 GPT-5.5验证：5个job，共445 trials，解决了385个，平均准确率86.52%。
  - Terminal-Bench 2.1补充验证（GPT-5.5）：3个job，共267 trials，平均准确率91.76%。
  - 总计：约1157 trials（445+445+267），覆盖多个日期不同运行。
- **充分性**：
  - 实验覆盖了终端任务场景的多个方面，但有以下局限：
    - 缺乏消融实验（如去掉规则知识、去掉时间感知、去掉统一边界等），无法量化各组件贡献。
    - 没有与现有方法（如ReAct+反思等）进行直接对比，仅展示绝对性能，缺乏相对比较。
    - 未分析失败案例的具体模式或不同骨干模型对框架各模块的敏感度。
    - 仅使用两种骨干模型（GPT-5.3-CodeX和GPT-5.5），未测试其他模型族（如开源模型），限制了结论的泛化性。
  - 客观性：实验设定清晰，报告了异常（exception）数量（主要是超时），但未提供详细的误差分析或统计显著性检验。缺少交叉验证或重复运行（同一配置多次运行）的结果，仅一次提交或有限次运行。

## 6. 主要结论与发现

- 统一的运行时边界、可调用的规则知识和时间感知执行可以显著提升长周期agent的执行稳定性。
- 在Terminal-Bench 2.0上，LemonHarness_GPT-5.3-CodeX达到84.49%平均准确率；配合更强的GPT-5.5骨干达86.52%；在更新版2.1上达91.76%。
- 失败主要集中在长编译、训练、多模态处理、跨环境适应等需要更长执行链和严格中间验证的任务，超时是主要失败原因。
- 结果表明框架在不同模型和任务版本下表现相对稳定，能从更强大的骨干模型中受益。

## 7. 优点

- **方法亮点**：
  - 将状态变更操作严格约束在显式工作空间内，通过结构化工具接口执行，减少环境状态不可预测性。
  - 引入可复用规则知识库，将跨任务经验直接注入运行时，降低对模型自身知识容量的依赖，提高早期决策质量。
  - 时间感知执行机制将剩余时间作为第一类输入，使模型动态调整行为策略，避免超时，这是一种新颖的“软约束”方式（不同于硬中断）。
- **实验亮点**：
  - 在真实终端任务基准（Terminal-Bench）上系统评估，覆盖数百个试验，结果具有统计意义。
  - 报告了不同骨干模型的对比以及更新版本基准的补充验证，展示了框架的可迁移性和鲁棒性。

## 8. 不足与局限

- **实验覆盖不足**：
  - 缺少与现有方法（如ReAct、Reflexion等）的直接对比，无法评估LemonHarness的相对优势。
  - 未进行消融研究，无法量化统一边界、规则知识、时间感知三个组件的各自贡献。
  - 仅测试了两个闭源模型（GPT-5.3-CodeX和GPT-5.5），未验证在其他模型族（如开源Llama、Mistral）上的通用性。
- **偏差风险**：
  - 规则知识库覆盖了五个领域，但可能未包括所有可能的任务类型，存在领域偏差。
  - 时间感知的阶段划分参数（30%/60%/90%）基于默认设置，未说明选择依据或是否对特定任务最优。
  - 仅报告了平均准确率，缺少方差或置信区间，多次运行结果可能存在波动（jobs间有9个百分点的差异）。
- **应用限制**：
  - 框架设计较紧密地耦合了GPT风格的工具调用和模型行为，对其他模型族的转移性未验证。
  - 需要手动设计规则知识库，维护成本较高，且规则可能存在过时或不完整风险。
  - 时间感知执行假设任务有明确的总预算T，但在部分动态任务中预算可能未知或变化，适用性受限。
- **报告不完整**：
  - 未披露训练或推理算力资源，无法评估计算效率。
  - 未讨论安全性和伦理问题，如agent执行错误命令可能产生危害。

（完）
